// Code generated by alef. DO NOT EDIT.

// E2e tests for category: configuration
package e2e_test

import (
	"strings"
	"testing"

	pkg "liter_llm"
)

func Test_CustomBaseUrl(t *testing.T) {
	// Client configured with a custom base URL routes all requests to that endpoint
	result, err := pkg.chat(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	// TODO: unsupported assertion type: count_equals
	if strings.TrimSpace(result.Choices["0"].Message.Content) != `Hi there!` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].Message.Content)
	}
	if strings.TrimSpace(result.Choices["0"].FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].FinishReason)
	}
	if strings.TrimSpace(result.Model) != `local-model` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}

func Test_ExtraHeaders(t *testing.T) {
	// Client configured with extra custom headers successfully completes a chat request
	result, err := pkg.chat(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	// TODO: unsupported assertion type: count_equals
	if strings.TrimSpace(result.Choices["0"].FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].FinishReason)
	}
}

func Test_LocalProviderLlamacpp(t *testing.T) {
	// llamacpp local provider routes requests via llamacpp/ model prefix with no auth
	result, err := pkg.chat(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	// TODO: unsupported assertion type: count_equals
	if strings.TrimSpace(result.Choices["0"].Message.Content) != `Hi there! I'm running locally.` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].Message.Content)
	}
	if strings.TrimSpace(result.Choices["0"].FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].FinishReason)
	}
	if strings.TrimSpace(result.Model) != `my-model` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}

func Test_LocalProviderOllama(t *testing.T) {
	// Ollama local provider routes requests via ollama/ model prefix with no auth
	result, err := pkg.chat(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	// TODO: unsupported assertion type: count_equals
	if strings.TrimSpace(result.Choices["0"].Message.Content) != `Hello! How can I help you today?` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].Message.Content)
	}
	if strings.TrimSpace(result.Choices["0"].FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].FinishReason)
	}
	if strings.TrimSpace(result.Model) != `qwen2:0.5b` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}

func Test_LocalProviderVllm(t *testing.T) {
	// vLLM local provider routes requests via vllm/ model prefix with no auth
	result, err := pkg.chat(nil)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	// TODO: unsupported assertion type: count_equals
	if strings.TrimSpace(result.Choices["0"].Message.Content) != `Hello! How may I assist you?` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].Message.Content)
	}
	if strings.TrimSpace(result.Choices["0"].FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", result.Choices["0"].FinishReason)
	}
	if strings.TrimSpace(result.Model) != `meta-llama/Llama-3.2-1B` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}
