// This file is auto-generated by alef. DO NOT EDIT.
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using LiterLlm;

namespace Kreuzberg.E2e;

/// <summary>E2e tests for category: smoke.</summary>
public class SmokeTests
{
    private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

    [Fact]
    public async Task Test_AnthropicChat()
    {
        // Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages
        var result = await LiterLlmLib.Chat(null);
        // TODO: unsupported assertion type: count_equals
        Assert.Equal("Hello!", result.Choices["0"].Message.Content.Trim());
        Assert.Equal("stop", result.Choices["0"].FinishReason.Trim());
        Assert.Equal("claude-3-5-sonnet-20241022", result.Model.Trim());
        Assert.Equal(23, result.Usage.TotalTokens);
    }

    [Fact]
    public async Task Test_AzureChat()
    {
        // Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape
        var result = await LiterLlmLib.Chat(null);
        // TODO: unsupported assertion type: count_equals
        Assert.Equal("Hello!", result.Choices["0"].Message.Content.Trim());
        Assert.Equal("stop", result.Choices["0"].FinishReason.Trim());
        Assert.Equal("gpt-4", result.Model.Trim());
        Assert.Equal(13, result.Usage.TotalTokens);
    }

    [Fact]
    public async Task Test_AzureEmbed()
    {
        // Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged
        var result = await LiterLlmLib.Chat("Hello world");
        // TODO: unsupported assertion type: count_equals
        // TODO: unsupported assertion type: count_equals
    }

    [Fact]
    public async Task Test_BasicChat()
    {
        // Basic chat completion with a single user message
        var result = await LiterLlmLib.Chat(null);
        // TODO: unsupported assertion type: count_equals
        Assert.Equal("Hello!", result.Choices["0"].Message.Content.Trim());
        Assert.Equal("stop", result.Choices["0"].FinishReason.Trim());
        Assert.Equal(15, result.Usage.TotalTokens);
        Assert.Equal("gpt-4", result.Model.Trim());
    }

    [Fact]
    public async Task Test_BasicEmbed()
    {
        // Basic embedding request for a single input string
        var result = await LiterLlmLib.Chat("Hello world");
        // TODO: unsupported assertion type: count_equals
        // TODO: unsupported assertion type: count_equals
    }

    [Fact]
    public async Task Test_BasicListModels()
    {
        // List available models from the API
        var result = await LiterLlmLib.Chat(null);
        Assert.True(result.Data.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_BedrockChat()
    {
        // Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape
        var result = await LiterLlmLib.Chat(null);
        // TODO: unsupported assertion type: count_equals
        Assert.Equal("Hello!", result.Choices["0"].Message.Content.Trim());
        Assert.Equal("stop", result.Choices["0"].FinishReason.Trim());
        Assert.Equal("anthropic.claude-3-sonnet-20240229-v1:0", result.Model.Trim());
        Assert.Equal(15, result.Usage.TotalTokens);
    }

    [Fact]
    public async Task Test_GithubCopilotChat()
    {
        // Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message
        var result = await LiterLlmLib.Chat(null);
        // TODO: unsupported assertion type: count_equals
        Assert.Equal("Hello!", result.Choices["0"].Message.Content.Trim());
        Assert.Equal("stop", result.Choices["0"].FinishReason.Trim());
        Assert.Equal("gpt-4o", result.Model.Trim());
        Assert.Equal(14, result.Usage.TotalTokens);
    }

    [Fact]
    public async Task Test_LocalChatOllama()
    {
        // Chat completion against local Ollama with qwen2:0.5b model
        var result = await LiterLlmLib.Chat(null);
        Assert.False(string.IsNullOrEmpty(result.Choices?.ToString()));
    }

    [Fact]
    public async Task Test_LocalListModelsOllama()
    {
        // List models from local Ollama instance
        var result = await LiterLlmLib.Chat(null);
        Assert.True(result.Data.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_SmokeCacheMemory()
    {
        // Test in-memory caching by sending identical requests twice
        var result = await LiterLlmLib.Chat(null);
        Assert.False(string.IsNullOrEmpty(result.Choices?.ToString()));
    }

    [Fact]
    public async Task Test_SmokeChatAnthropic()
    {
        // Basic chat completion against real Anthropic API
        var result = await LiterLlmLib.Chat(null);
        Assert.False(string.IsNullOrEmpty(result.Choices?.ToString()));
        Assert.False(string.IsNullOrEmpty(result.Usage?.ToString()));
    }

    [Fact]
    public async Task Test_SmokeChatGemini()
    {
        // Basic chat completion against real Google Gemini API
        var result = await LiterLlmLib.Chat(null);
        Assert.False(string.IsNullOrEmpty(result.Choices?.ToString()));
        Assert.False(string.IsNullOrEmpty(result.Usage?.ToString()));
    }

    [Fact]
    public async Task Test_SmokeChatOpenai()
    {
        // Basic chat completion against real OpenAI API
        var result = await LiterLlmLib.Chat(null);
        Assert.False(string.IsNullOrEmpty(result.Choices?.ToString()));
        Assert.False(string.IsNullOrEmpty(result.Usage?.ToString()));
    }

    [Fact]
    public async Task Test_SmokeEmbedOpenai()
    {
        // Embeddings request against real OpenAI API
        var result = await LiterLlmLib.Chat(new[] { "Hello world" });
        Assert.False(string.IsNullOrEmpty(result.Data?.ToString()));
    }

    [Fact]
    public async Task Test_SmokeListModelsOpenai()
    {
        // List models against real OpenAI API
        var result = await LiterLlmLib.Chat(null);
        Assert.True(result.Data.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_SmokeProviderRouting()
    {
        // Test provider routing by sending requests to OpenAI and Anthropic
        var result = await LiterLlmLib.Chat(null);
        Assert.False(string.IsNullOrEmpty(result.Choices?.ToString()));
    }

    [Fact]
    public async Task Test_SmokeStreamingOpenai()
    {
        // Chat streaming against real OpenAI API, verifies chunks received
        var result = await LiterLlmLib.Chat(null);
        Assert.True(result.Chunks.Count >= 1, "expected at least 1 elements");
    }

    [Fact]
    public async Task Test_VertexChat()
    {
        // Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape
        var result = await LiterLlmLib.Chat(null);
        // TODO: unsupported assertion type: count_equals
        Assert.Equal("Hello!", result.Choices["0"].Message.Content.Trim());
        Assert.Equal("stop", result.Choices["0"].FinishReason.Trim());
        Assert.Equal("gemini-2.0-flash", result.Model.Trim());
        Assert.Equal(13, result.Usage.TotalTokens);
    }

    [Fact]
    public async Task Test_VertexEmbed()
    {
        // Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape
        var result = await LiterLlmLib.Chat("Hello");
        // TODO: unsupported assertion type: count_equals
        // TODO: unsupported assertion type: count_equals
    }
}
