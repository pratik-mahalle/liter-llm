# This file is auto-generated by alef. DO NOT EDIT.
"""E2e tests for category: proxy."""
import pytest
from liter_llm import chat


@pytest.mark.asyncio
async def test_proxy_auth_invalid() -> None:
    """401 Unauthorized when an invalid API key is provided through the proxy."""
    request = None
    with pytest.raises(Exception) as exc_info:
        await chat(request=request)
    assert "Authentication" in str(exc_info.value)  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_auth_missing() -> None:
    """401 Unauthorized when no API key is provided through the proxy."""
    request = None
    with pytest.raises(Exception) as exc_info:
        await chat(request=request)
    assert "Authentication" in str(exc_info.value)  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_chat_basic() -> None:
    """Basic chat completion request routed through the proxy."""
    request = None
    result = await chat(request=request)
    # TODO: unsupported assertion type: count_equals
    assert result.choices.get("0").message.content.strip() == "Hello!"  # noqa: S101
    assert result.choices.get("0").finish_reason.strip() == "stop"  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_chat_streaming() -> None:
    """Streaming chat completion routed through the proxy."""
    request = None
    result = await chat(request=request)
    assert len(result.chunks) >= 3  # noqa: S101
    assert result.stream_content.strip() == "1 2 3"  # noqa: S101
    # TODO: unsupported assertion type: is_true

@pytest.mark.asyncio
async def test_proxy_embeddings() -> None:
    """Embedding request routed through the proxy."""
    request = "Hello world"
    result = await chat(request=request)
    # TODO: unsupported assertion type: count_equals

@pytest.mark.asyncio
async def test_proxy_health() -> None:
    """Health check verifying proxy connectivity via list models."""
    request = None
    result = await chat(request=request)
    assert len(result.data) >= 1  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_image_generate() -> None:
    """Image generation request routed through the proxy."""
    request = None
    result = await chat(request=request)
    # TODO: unsupported assertion type: count_equals
    assert result.data.get("0").url  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_models_list() -> None:
    """List models request routed through the proxy."""
    request = None
    result = await chat(request=request)
    assert len(result.data) >= 1  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_moderation() -> None:
    """Content moderation request routed through the proxy."""
    request = "The weather is nice today."
    result = await chat(request=request)
    # TODO: unsupported assertion type: count_equals
    assert result.results.get("0").flagged is False  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_rerank() -> None:
    """Document reranking request routed through the proxy."""
    request = None
    result = await chat(request=request)
    # TODO: unsupported assertion type: count_equals
    assert result.results.get("0").relevance_score > 0.9  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_upstream_429() -> None:
    """429 Too Many Requests from upstream provider through the proxy."""
    request = None
    with pytest.raises(Exception) as exc_info:
        await chat(request=request)
    assert "RateLimited" in str(exc_info.value)  # noqa: S101

@pytest.mark.asyncio
async def test_proxy_upstream_500() -> None:
    """500 Internal Server Error from upstream provider through the proxy."""
    request = None
    with pytest.raises(Exception) as exc_info:
        await chat(request=request)
    assert "ServerError" in str(exc_info.value)  # noqa: S101

