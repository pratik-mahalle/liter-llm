# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: error-handling
defmodule E2e.ErrorHandlingTest do
  use ExUnit.Case, async: true

  describe "anthropic_error_auth" do
    test "401 Authentication error returned by the Anthropic API when the API key is invalid" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "auth_401" do
    test "401 Unauthorized error when API key is invalid or missing" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "azure_error_auth" do
    test "Azure OpenAI returns a 401 Unauthorized error when the API key is missing or invalid — uses Azure's error envelope shape with code AccessDenied" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "bad_request_400" do
    test "400 Bad Request error when a parameter value is invalid" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "bedrock_error_auth" do
    test "AWS Bedrock returns 403 Forbidden (not 401) when credentials are missing, expired, or the IAM role lacks bedrock:InvokeModel permission — verifies the error is mapped to Authentication" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "content_policy_violation" do
    test "400 error when a request is rejected due to content policy" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "context_window_exceeded" do
    test "400 error when the prompt exceeds the model's maximum context length" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "forbidden_403" do
    test "403 Forbidden error when the API key does not have access to the requested resource" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "gateway_timeout_504" do
    test "504 Gateway Timeout error when the upstream service times out" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "github_copilot_error_auth" do
    test "401 Authentication error returned by the GitHub Copilot API when the token is invalid or expired" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "not_found_404" do
    test "404 Not Found error when requesting a model that does not exist" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "rate_limit_429" do
    test "429 Too Many Requests error when the rate limit is exceeded" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "server_error_500" do
    test "500 Internal Server Error from the upstream API" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "service_unavailable_502" do
    test "502 Bad Gateway error when the upstream service is unavailable" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end

  describe "vertex_error_auth" do
    test "Google Vertex AI returns 401 Unauthorized when the OAuth2 token is missing, expired, or the service account lacks aiplatform.endpoints.predict permission — verifies the error is mapped to Authentication" do
      assert {:error, _} = LiterLlm.chat_async(nil)
    end
  end
end
