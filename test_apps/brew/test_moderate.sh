#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: moderate
set -euo pipefail

test_edge_moderate_all_categories() {
  # Moderation response with multiple categories flagged
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_results_0__flagged
  val_results_0__flagged=$(echo "$output" | jq -r '.results[0].flagged')
  assert_equals "$val_results_0__flagged" 'true' 'results[0].flagged'
}

test_edge_moderate_empty_input() {
  # Moderation with empty string input
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_results_0__flagged
  val_results_0__flagged=$(echo "$output" | jq -r '.results[0].flagged')
  assert_equals "$val_results_0__flagged" 'false' 'results[0].flagged'
}

test_error_moderate_auth_401() {
  # 401 Unauthorized for moderation with invalid API key
  if liter_llm chat >/dev/null 2>&1; then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
}

test_error_moderate_bad_request() {
  # 400 Bad Request for moderation with invalid model
  if liter_llm chat >/dev/null 2>&1; then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
}

test_smoke_moderate_batch() {
  # Moderate multiple inputs in a single request
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_results_0__flagged
  val_results_0__flagged=$(echo "$output" | jq -r '.results[0].flagged')
  assert_equals "$val_results_0__flagged" 'false' 'results[0].flagged'
}

test_smoke_moderate_flagged() {
  # Moderation detects flagged content
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_results_0__flagged
  val_results_0__flagged=$(echo "$output" | jq -r '.results[0].flagged')
  assert_equals "$val_results_0__flagged" 'true' 'results[0].flagged'
}

test_smoke_moderate_single() {
  # Moderate a single non-flagged input
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_results_0__flagged
  val_results_0__flagged=$(echo "$output" | jq -r '.results[0].flagged')
  assert_equals "$val_results_0__flagged" 'false' 'results[0].flagged'
}

run_tests_moderate() {
  run_test test_edge_moderate_all_categories
  run_test test_edge_moderate_empty_input
  run_test test_error_moderate_auth_401
  run_test test_error_moderate_bad_request
  run_test test_smoke_moderate_batch
  run_test test_smoke_moderate_flagged
  run_test test_smoke_moderate_single
}
