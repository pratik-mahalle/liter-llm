#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: chat
set -euo pipefail

test_developer_message() {
    # Chat request that includes a developer role message alongside user messages
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__message_content
    val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
    assert_equals "$val_choices_0__message_content" 's[::-1]' 'choices[0].message.content'
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
}

test_finish_reason_content_filter() {
    # Chat response stopped by content filter with finish_reason of content_filter and null content
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'content_filter' 'choices[0].finish_reason'
}

test_finish_reason_length() {
    # Chat response truncated due to max_tokens limit with finish_reason of length
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'length' 'choices[0].finish_reason'
}

test_multi_turn_conversation() {
    # Multi-turn conversation with system, user, assistant, and follow-up user messages
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__message_content
    val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
    assert_equals "$val_choices_0__message_content" '4 + 4 equals 8.' 'choices[0].message.content'
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
}

test_parallel_tool_calls() {
    # Chat request that results in parallel tool calls in the response
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__message_tool_calls
    val_choices_0__message_tool_calls=$(echo "$output" | jq -r '.choices[0].message.tool_calls')
    assert_not_empty "$val_choices_0__message_tool_calls" 'choices[0].message.tool_calls'
    # TODO: unsupported assertion type: count_equals
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'tool_calls' 'choices[0].finish_reason'
}

test_response_format_json_object() {
    # Chat request with response_format json_object that returns valid JSON content
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__message_content
    val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
    assert_not_empty "$val_choices_0__message_content" 'choices[0].message.content'
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
}

test_response_format_json_schema() {
    # Chat request with response_format json_schema that validates the output structure
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__message_content
    val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
    assert_not_empty "$val_choices_0__message_content" 'choices[0].message.content'
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
}

test_seed_parameter() {
    # Chat request with seed parameter for deterministic output; response includes system_fingerprint
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
    local val_system_fingerprint
    val_system_fingerprint=$(echo "$output" | jq -r '.system_fingerprint')
    assert_not_empty "$val_system_fingerprint" 'system_fingerprint'
}

test_stop_sequences() {
    # Chat request with custom stop sequences that terminates generation at a stop token
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
}

test_tool_choice_required() {
    # Chat request with tool_choice set to required forces the model to call a tool
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__message_tool_calls
    val_choices_0__message_tool_calls=$(echo "$output" | jq -r '.choices[0].message.tool_calls')
    assert_not_empty "$val_choices_0__message_tool_calls" 'choices[0].message.tool_calls'
    local val_choices_0__message_tool_calls_0__function_name
    val_choices_0__message_tool_calls_0__function_name=$(echo "$output" | jq -r '.choices[0].message.tool_calls[0].function.name')
    assert_equals "$val_choices_0__message_tool_calls_0__function_name" 'get_weather' 'choices[0].message.tool_calls[0].function.name'
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'tool_calls' 'choices[0].finish_reason'
}

test_tool_choice_specific() {
    # Chat request with tool_choice specifying a particular function to call
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: count_equals
    local val_choices_0__message_tool_calls
    val_choices_0__message_tool_calls=$(echo "$output" | jq -r '.choices[0].message.tool_calls')
    assert_not_empty "$val_choices_0__message_tool_calls" 'choices[0].message.tool_calls'
    local val_choices_0__message_tool_calls_0__function_name
    val_choices_0__message_tool_calls_0__function_name=$(echo "$output" | jq -r '.choices[0].message.tool_calls[0].function.name')
    assert_equals "$val_choices_0__message_tool_calls_0__function_name" 'get_weather' 'choices[0].message.tool_calls[0].function.name'
    local val_choices_0__finish_reason
    val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
    assert_equals "$val_choices_0__finish_reason" 'tool_calls' 'choices[0].finish_reason'
}

run_tests_chat() {
    run_test test_developer_message
    run_test test_finish_reason_content_filter
    run_test test_finish_reason_length
    run_test test_multi_turn_conversation
    run_test test_parallel_tool_calls
    run_test test_response_format_json_object
    run_test test_response_format_json_schema
    run_test test_seed_parameter
    run_test test_stop_sequences
    run_test test_tool_choice_required
    run_test test_tool_choice_specific
}
