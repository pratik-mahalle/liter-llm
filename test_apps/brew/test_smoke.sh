#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: smoke
set -euo pipefail

test_anthropic_chat() {
  # Basic chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) with system and user messages
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_choices_0__message_content
  val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
  assert_equals "$val_choices_0__message_content" 'Hello!' 'choices[0].message.content'
  local val_choices_0__finish_reason
  val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
  assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
  local val_model
  val_model=$(echo "$output" | jq -r '.model')
  assert_equals "$val_model" 'claude-3-5-sonnet-20241022' 'model'
  local val_usage_total_tokens
  val_usage_total_tokens=$(echo "$output" | jq -r '.usage.total_tokens')
  assert_equals "$val_usage_total_tokens" '23' 'usage.total_tokens'
}

test_azure_chat() {
  # Chat completion via Azure OpenAI with the azure/ prefix for provider routing — verifies the prefix is stripped before dispatching and the response is normalised to the standard OpenAI chat completion shape
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_choices_0__message_content
  val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
  assert_equals "$val_choices_0__message_content" 'Hello!' 'choices[0].message.content'
  local val_choices_0__finish_reason
  val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
  assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
  local val_model
  val_model=$(echo "$output" | jq -r '.model')
  assert_equals "$val_model" 'gpt-4' 'model'
  local val_usage_total_tokens
  val_usage_total_tokens=$(echo "$output" | jq -r '.usage.total_tokens')
  assert_equals "$val_usage_total_tokens" '13' 'usage.total_tokens'
}

test_azure_embed() {
  # Embedding request via Azure OpenAI using the azure/ provider prefix — response follows the standard OpenAI embeddings shape that Azure returns unchanged
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  # TODO: unsupported assertion type: count_equals
}

test_basic_chat() {
  # Basic chat completion with a single user message
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_choices_0__message_content
  val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
  assert_equals "$val_choices_0__message_content" 'Hello!' 'choices[0].message.content'
  local val_choices_0__finish_reason
  val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
  assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
  local val_usage_total_tokens
  val_usage_total_tokens=$(echo "$output" | jq -r '.usage.total_tokens')
  assert_equals "$val_usage_total_tokens" '15' 'usage.total_tokens'
  local val_model
  val_model=$(echo "$output" | jq -r '.model')
  assert_equals "$val_model" 'gpt-4' 'model'
}

test_basic_embed() {
  # Basic embedding request for a single input string
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  # TODO: unsupported assertion type: count_equals
}

test_basic_list_models() {
  # List available models from the API
  local output
  output=$(liter_llm chat)

  local count_data
  count_data=$(echo "$output" | jq '.data | length')
  assert_count_min "$count_data" 1 'data'
}

test_bedrock_chat() {
  # Basic chat completion via the AWS Bedrock provider using the bedrock/ prefix for routing — verifies the prefix is stripped before dispatching and the Converse API response is normalised to the standard OpenAI chat completion shape
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_choices_0__message_content
  val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
  assert_equals "$val_choices_0__message_content" 'Hello!' 'choices[0].message.content'
  local val_choices_0__finish_reason
  val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
  assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
  local val_model
  val_model=$(echo "$output" | jq -r '.model')
  assert_equals "$val_model" 'anthropic.claude-3-sonnet-20240229-v1:0' 'model'
  local val_usage_total_tokens
  val_usage_total_tokens=$(echo "$output" | jq -r '.usage.total_tokens')
  assert_equals "$val_usage_total_tokens" '15' 'usage.total_tokens'
}

test_github_copilot_chat() {
  # Basic chat completion via the GitHub Copilot provider (gpt-4o) with a single user message
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_choices_0__message_content
  val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
  assert_equals "$val_choices_0__message_content" 'Hello!' 'choices[0].message.content'
  local val_choices_0__finish_reason
  val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
  assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
  local val_model
  val_model=$(echo "$output" | jq -r '.model')
  assert_equals "$val_model" 'gpt-4o' 'model'
  local val_usage_total_tokens
  val_usage_total_tokens=$(echo "$output" | jq -r '.usage.total_tokens')
  assert_equals "$val_usage_total_tokens" '14' 'usage.total_tokens'
}

test_local_chat_ollama() {
  # Chat completion against local Ollama with qwen2:0.5b model
  local output
  output=$(liter_llm chat)

  local val_choices
  val_choices=$(echo "$output" | jq -r '.choices')
  assert_not_empty "$val_choices" 'choices'
}

test_local_list_models_ollama() {
  # List models from local Ollama instance
  local output
  output=$(liter_llm chat)

  local count_data
  count_data=$(echo "$output" | jq '.data | length')
  assert_count_min "$count_data" 1 'data'
}

test_smoke_cache_memory() {
  # Test in-memory caching by sending identical requests twice
  local output
  output=$(liter_llm chat)

  local val_choices
  val_choices=$(echo "$output" | jq -r '.choices')
  assert_not_empty "$val_choices" 'choices'
}

test_smoke_chat_anthropic() {
  # Basic chat completion against real Anthropic API
  local output
  output=$(liter_llm chat)

  local val_choices
  val_choices=$(echo "$output" | jq -r '.choices')
  assert_not_empty "$val_choices" 'choices'
  local val_usage
  val_usage=$(echo "$output" | jq -r '.usage')
  assert_not_empty "$val_usage" 'usage'
}

test_smoke_chat_gemini() {
  # Basic chat completion against real Google Gemini API
  local output
  output=$(liter_llm chat)

  local val_choices
  val_choices=$(echo "$output" | jq -r '.choices')
  assert_not_empty "$val_choices" 'choices'
  local val_usage
  val_usage=$(echo "$output" | jq -r '.usage')
  assert_not_empty "$val_usage" 'usage'
}

test_smoke_chat_openai() {
  # Basic chat completion against real OpenAI API
  local output
  output=$(liter_llm chat)

  local val_choices
  val_choices=$(echo "$output" | jq -r '.choices')
  assert_not_empty "$val_choices" 'choices'
  local val_usage
  val_usage=$(echo "$output" | jq -r '.usage')
  assert_not_empty "$val_usage" 'usage'
}

test_smoke_embed_openai() {
  # Embeddings request against real OpenAI API
  local output
  output=$(liter_llm chat)

  local val_data
  val_data=$(echo "$output" | jq -r '.data')
  assert_not_empty "$val_data" 'data'
}

test_smoke_list_models_openai() {
  # List models against real OpenAI API
  local output
  output=$(liter_llm chat)

  local count_data
  count_data=$(echo "$output" | jq '.data | length')
  assert_count_min "$count_data" 1 'data'
}

test_smoke_provider_routing() {
  # Test provider routing by sending requests to OpenAI and Anthropic
  local output
  output=$(liter_llm chat)

  local val_choices
  val_choices=$(echo "$output" | jq -r '.choices')
  assert_not_empty "$val_choices" 'choices'
}

test_smoke_streaming_openai() {
  # Chat streaming against real OpenAI API, verifies chunks received
  local output
  output=$(liter_llm chat)

  local count_chunks
  count_chunks=$(echo "$output" | jq '.chunks | length')
  assert_count_min "$count_chunks" 1 'chunks'
}

test_vertex_chat() {
  # Basic chat completion via the Google Vertex AI provider using the vertex_ai/ prefix for routing — verifies the prefix is stripped before dispatching and the Gemini response is normalised to the standard OpenAI chat completion shape
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  local val_choices_0__message_content
  val_choices_0__message_content=$(echo "$output" | jq -r '.choices[0].message.content')
  assert_equals "$val_choices_0__message_content" 'Hello!' 'choices[0].message.content'
  local val_choices_0__finish_reason
  val_choices_0__finish_reason=$(echo "$output" | jq -r '.choices[0].finish_reason')
  assert_equals "$val_choices_0__finish_reason" 'stop' 'choices[0].finish_reason'
  local val_model
  val_model=$(echo "$output" | jq -r '.model')
  assert_equals "$val_model" 'gemini-2.0-flash' 'model'
  local val_usage_total_tokens
  val_usage_total_tokens=$(echo "$output" | jq -r '.usage.total_tokens')
  assert_equals "$val_usage_total_tokens" '13' 'usage.total_tokens'
}

test_vertex_embed() {
  # Embedding request via Google Vertex AI using the vertex_ai/ provider prefix and the text-embedding-005 model — response follows the standard OpenAI embeddings shape
  local output
  output=$(liter_llm chat)

  # TODO: unsupported assertion type: count_equals
  # TODO: unsupported assertion type: count_equals
}

run_tests_smoke() {
  run_test test_anthropic_chat
  run_test test_azure_chat
  run_test test_azure_embed
  run_test test_basic_chat
  run_test test_basic_embed
  run_test test_basic_list_models
  run_test test_bedrock_chat
  run_test test_github_copilot_chat
  run_test test_local_chat_ollama
  run_test test_local_list_models_ollama
  run_test test_smoke_cache_memory
  run_test test_smoke_chat_anthropic
  run_test test_smoke_chat_gemini
  run_test test_smoke_chat_openai
  run_test test_smoke_embed_openai
  run_test test_smoke_list_models_openai
  run_test test_smoke_provider_routing
  run_test test_smoke_streaming_openai
  run_test test_vertex_chat
  run_test test_vertex_embed
}
