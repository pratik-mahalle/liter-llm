#ifndef LITERLLM_H
#define LITERLLM_H

#pragma once

/* This file is auto-generated by alef. DO NOT EDIT. */

#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdlib.h>
/* Opaque type forward declarations */
typedef struct LITERLLMAssistantMessage LITERLLMAssistantMessage;
typedef struct LITERLLMAudioContent LITERLLMAudioContent;
typedef struct LITERLLMAuthHeaderFormat LITERLLMAuthHeaderFormat;
typedef struct LITERLLMChatCompletionChunk LITERLLMChatCompletionChunk;
typedef struct LITERLLMChatCompletionRequest LITERLLMChatCompletionRequest;
typedef struct LITERLLMChatCompletionResponse LITERLLMChatCompletionResponse;
typedef struct LITERLLMChatCompletionTool LITERLLMChatCompletionTool;
typedef struct LITERLLMChoice LITERLLMChoice;
typedef struct LITERLLMContentPart LITERLLMContentPart;
typedef struct LITERLLMCreateImageRequest LITERLLMCreateImageRequest;
typedef struct LITERLLMCreateSpeechRequest LITERLLMCreateSpeechRequest;
typedef struct LITERLLMCreateTranscriptionRequest LITERLLMCreateTranscriptionRequest;
typedef struct LITERLLMCustomProviderConfig LITERLLMCustomProviderConfig;
typedef struct LITERLLMDefaultClient LITERLLMDefaultClient;
typedef struct LITERLLMDeveloperMessage LITERLLMDeveloperMessage;
typedef struct LITERLLMDocumentContent LITERLLMDocumentContent;
typedef struct LITERLLMEmbeddingFormat LITERLLMEmbeddingFormat;
typedef struct LITERLLMEmbeddingInput LITERLLMEmbeddingInput;
typedef struct LITERLLMEmbeddingObject LITERLLMEmbeddingObject;
typedef struct LITERLLMEmbeddingRequest LITERLLMEmbeddingRequest;
typedef struct LITERLLMEmbeddingResponse LITERLLMEmbeddingResponse;
typedef struct LITERLLMFinishReason LITERLLMFinishReason;
typedef struct LITERLLMFunctionCall LITERLLMFunctionCall;
typedef struct LITERLLMFunctionDefinition LITERLLMFunctionDefinition;
typedef struct LITERLLMFunctionMessage LITERLLMFunctionMessage;
typedef struct LITERLLMImage LITERLLMImage;
typedef struct LITERLLMImageDetail LITERLLMImageDetail;
typedef struct LITERLLMImageUrl LITERLLMImageUrl;
typedef struct LITERLLMImagesResponse LITERLLMImagesResponse;
typedef struct LITERLLMJsonSchemaFormat LITERLLMJsonSchemaFormat;
typedef struct LITERLLMLiterLlmError LITERLLMLiterLlmError;
typedef struct LITERLLMMessage LITERLLMMessage;
typedef struct LITERLLMModelObject LITERLLMModelObject;
typedef struct LITERLLMModelsListResponse LITERLLMModelsListResponse;
typedef struct LITERLLMModerationCategories LITERLLMModerationCategories;
typedef struct LITERLLMModerationCategoryScores LITERLLMModerationCategoryScores;
typedef struct LITERLLMModerationInput LITERLLMModerationInput;
typedef struct LITERLLMModerationRequest LITERLLMModerationRequest;
typedef struct LITERLLMModerationResponse LITERLLMModerationResponse;
typedef struct LITERLLMModerationResult LITERLLMModerationResult;
typedef struct LITERLLMOcrDocument LITERLLMOcrDocument;
typedef struct LITERLLMOcrImage LITERLLMOcrImage;
typedef struct LITERLLMOcrPage LITERLLMOcrPage;
typedef struct LITERLLMOcrRequest LITERLLMOcrRequest;
typedef struct LITERLLMOcrResponse LITERLLMOcrResponse;
typedef struct LITERLLMPageDimensions LITERLLMPageDimensions;
typedef struct LITERLLMReasoningEffort LITERLLMReasoningEffort;
typedef struct LITERLLMRerankDocument LITERLLMRerankDocument;
typedef struct LITERLLMRerankRequest LITERLLMRerankRequest;
typedef struct LITERLLMRerankResponse LITERLLMRerankResponse;
typedef struct LITERLLMRerankResult LITERLLMRerankResult;
typedef struct LITERLLMRerankResultDocument LITERLLMRerankResultDocument;
typedef struct LITERLLMResponseFormat LITERLLMResponseFormat;
typedef struct LITERLLMSearchRequest LITERLLMSearchRequest;
typedef struct LITERLLMSearchResponse LITERLLMSearchResponse;
typedef struct LITERLLMSearchResult LITERLLMSearchResult;
typedef struct LITERLLMSpecificFunction LITERLLMSpecificFunction;
typedef struct LITERLLMSpecificToolChoice LITERLLMSpecificToolChoice;
typedef struct LITERLLMStopSequence LITERLLMStopSequence;
typedef struct LITERLLMStreamChoice LITERLLMStreamChoice;
typedef struct LITERLLMStreamDelta LITERLLMStreamDelta;
typedef struct LITERLLMStreamFunctionCall LITERLLMStreamFunctionCall;
typedef struct LITERLLMStreamOptions LITERLLMStreamOptions;
typedef struct LITERLLMStreamToolCall LITERLLMStreamToolCall;
typedef struct LITERLLMSystemMessage LITERLLMSystemMessage;
typedef struct LITERLLMToolCall LITERLLMToolCall;
typedef struct LITERLLMToolChoice LITERLLMToolChoice;
typedef struct LITERLLMToolChoiceMode LITERLLMToolChoiceMode;
typedef struct LITERLLMToolMessage LITERLLMToolMessage;
typedef struct LITERLLMToolType LITERLLMToolType;
typedef struct LITERLLMTranscriptionResponse LITERLLMTranscriptionResponse;
typedef struct LITERLLMTranscriptionSegment LITERLLMTranscriptionSegment;
typedef struct LITERLLMUsage LITERLLMUsage;
typedef struct LITERLLMUserContent LITERLLMUserContent;
typedef struct LITERLLMUserMessage LITERLLMUserMessage;


/**
 * Return the last error code (0 means no error).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_last_error_code(void);

/**
 * Return the last error message. The pointer is valid until the next FFI call on this thread.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *literllm_last_error_context(void);

/**
 * Free a string previously returned by this library.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_free_string(char *ptr);

/**
 * Return the library version string. The pointer is static and must NOT be freed.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
const char *literllm_version(void);

/**
 * Free a `LiterLlmError` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_liter_llm_error_free(LITERLLMLiterLlmError *ptr);

/**
 * Create a `SystemMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_system_message_free`.
 */
LITERLLMSystemMessage *literllm_system_message_from_json(const char *json);

/**
 * Serialize a `SystemMessage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_system_message_to_json(const LITERLLMSystemMessage *ptr);

/**
 * Free a `SystemMessage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_system_message_free(LITERLLMSystemMessage *ptr);

/**
 * Get the `content` field from a `SystemMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_system_message_content(const LITERLLMSystemMessage *ptr);

/**
 * Get the `name` field from a `SystemMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_system_message_name(const LITERLLMSystemMessage *ptr);

/**
 * Create a `UserMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_user_message_free`.
 */
LITERLLMUserMessage *literllm_user_message_from_json(const char *json);

/**
 * Serialize a `UserMessage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_user_message_to_json(const LITERLLMUserMessage *ptr);

/**
 * Free a `UserMessage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_user_message_free(LITERLLMUserMessage *ptr);

/**
 * Get the `content` field from a `UserMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMUserContent *literllm_user_message_content(const LITERLLMUserMessage *ptr);

/**
 * Get the `name` field from a `UserMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_user_message_name(const LITERLLMUserMessage *ptr);

/**
 * Create a `ImageUrl` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_image_url_free`.
 */
LITERLLMImageUrl *literllm_image_url_from_json(const char *json);

/**
 * Serialize a `ImageUrl` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_image_url_to_json(const LITERLLMImageUrl *ptr);

/**
 * Free a `ImageUrl` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_image_url_free(LITERLLMImageUrl *ptr);

/**
 * Get the `url` field from a `ImageUrl`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_image_url_url(const LITERLLMImageUrl *ptr);

/**
 * Get the `detail` field from a `ImageUrl`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMImageDetail *literllm_image_url_detail(const LITERLLMImageUrl *ptr);

/**
 * Create a `DocumentContent` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_document_content_free`.
 */
LITERLLMDocumentContent *literllm_document_content_from_json(const char *json);

/**
 * Serialize a `DocumentContent` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_document_content_to_json(const LITERLLMDocumentContent *ptr);

/**
 * Free a `DocumentContent` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_document_content_free(LITERLLMDocumentContent *ptr);

/**
 * Get the `data` field from a `DocumentContent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_document_content_data(const LITERLLMDocumentContent *ptr);

/**
 * Get the `media_type` field from a `DocumentContent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_document_content_media_type(const LITERLLMDocumentContent *ptr);

/**
 * Create a `AudioContent` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_audio_content_free`.
 */
LITERLLMAudioContent *literllm_audio_content_from_json(const char *json);

/**
 * Serialize a `AudioContent` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_audio_content_to_json(const LITERLLMAudioContent *ptr);

/**
 * Free a `AudioContent` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_audio_content_free(LITERLLMAudioContent *ptr);

/**
 * Get the `data` field from a `AudioContent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_audio_content_data(const LITERLLMAudioContent *ptr);

/**
 * Get the `format` field from a `AudioContent`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_audio_content_format(const LITERLLMAudioContent *ptr);

/**
 * Create a `AssistantMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_assistant_message_free`.
 */
LITERLLMAssistantMessage *literllm_assistant_message_from_json(const char *json);

/**
 * Serialize a `AssistantMessage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_assistant_message_to_json(const LITERLLMAssistantMessage *ptr);

/**
 * Free a `AssistantMessage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_assistant_message_free(LITERLLMAssistantMessage *ptr);

/**
 * Get the `content` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_assistant_message_content(const LITERLLMAssistantMessage *ptr);

/**
 * Get the `name` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_assistant_message_name(const LITERLLMAssistantMessage *ptr);

/**
 * Get the `tool_calls` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_assistant_message_tool_calls(const LITERLLMAssistantMessage *ptr);

/**
 * Get the `refusal` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_assistant_message_refusal(const LITERLLMAssistantMessage *ptr);

/**
 * Get the `function_call` field from a `AssistantMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMFunctionCall *literllm_assistant_message_function_call(const LITERLLMAssistantMessage *ptr);

/**
 * Create a `ToolMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_tool_message_free`.
 */
LITERLLMToolMessage *literllm_tool_message_from_json(const char *json);

/**
 * Serialize a `ToolMessage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_tool_message_to_json(const LITERLLMToolMessage *ptr);

/**
 * Free a `ToolMessage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_tool_message_free(LITERLLMToolMessage *ptr);

/**
 * Get the `content` field from a `ToolMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_tool_message_content(const LITERLLMToolMessage *ptr);

/**
 * Get the `tool_call_id` field from a `ToolMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_tool_message_tool_call_id(const LITERLLMToolMessage *ptr);

/**
 * Get the `name` field from a `ToolMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_tool_message_name(const LITERLLMToolMessage *ptr);

/**
 * Create a `DeveloperMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_developer_message_free`.
 */
LITERLLMDeveloperMessage *literllm_developer_message_from_json(const char *json);

/**
 * Serialize a `DeveloperMessage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_developer_message_to_json(const LITERLLMDeveloperMessage *ptr);

/**
 * Free a `DeveloperMessage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_developer_message_free(LITERLLMDeveloperMessage *ptr);

/**
 * Get the `content` field from a `DeveloperMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_developer_message_content(const LITERLLMDeveloperMessage *ptr);

/**
 * Get the `name` field from a `DeveloperMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_developer_message_name(const LITERLLMDeveloperMessage *ptr);

/**
 * Create a `FunctionMessage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_function_message_free`.
 */
LITERLLMFunctionMessage *literllm_function_message_from_json(const char *json);

/**
 * Serialize a `FunctionMessage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_function_message_to_json(const LITERLLMFunctionMessage *ptr);

/**
 * Free a `FunctionMessage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_function_message_free(LITERLLMFunctionMessage *ptr);

/**
 * Get the `content` field from a `FunctionMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_function_message_content(const LITERLLMFunctionMessage *ptr);

/**
 * Get the `name` field from a `FunctionMessage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_function_message_name(const LITERLLMFunctionMessage *ptr);

/**
 * Create a `ChatCompletionTool` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_chat_completion_tool_free`.
 */
LITERLLMChatCompletionTool *literllm_chat_completion_tool_from_json(const char *json);

/**
 * Serialize a `ChatCompletionTool` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_chat_completion_tool_to_json(const LITERLLMChatCompletionTool *ptr);

/**
 * Free a `ChatCompletionTool` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_chat_completion_tool_free(LITERLLMChatCompletionTool *ptr);

/**
 * Get the `tool_type` field from a `ChatCompletionTool`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMToolType *literllm_chat_completion_tool_tool_type(const LITERLLMChatCompletionTool *ptr);

/**
 * Get the `function` field from a `ChatCompletionTool`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMFunctionDefinition *literllm_chat_completion_tool_function(const LITERLLMChatCompletionTool *ptr);

/**
 * Create a `FunctionDefinition` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_function_definition_free`.
 */
LITERLLMFunctionDefinition *literllm_function_definition_from_json(const char *json);

/**
 * Serialize a `FunctionDefinition` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_function_definition_to_json(const LITERLLMFunctionDefinition *ptr);

/**
 * Free a `FunctionDefinition` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_function_definition_free(LITERLLMFunctionDefinition *ptr);

/**
 * Get the `name` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_function_definition_name(const LITERLLMFunctionDefinition *ptr);

/**
 * Get the `description` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_function_definition_description(const LITERLLMFunctionDefinition *ptr);

/**
 * Get the `parameters` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_function_definition_parameters(const LITERLLMFunctionDefinition *ptr);

/**
 * Get the `strict` field from a `FunctionDefinition`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_function_definition_strict(const LITERLLMFunctionDefinition *ptr);

/**
 * Create a `ToolCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_tool_call_free`.
 */
LITERLLMToolCall *literllm_tool_call_from_json(const char *json);

/**
 * Serialize a `ToolCall` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_tool_call_to_json(const LITERLLMToolCall *ptr);

/**
 * Free a `ToolCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_tool_call_free(LITERLLMToolCall *ptr);

/**
 * Get the `id` field from a `ToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_tool_call_id(const LITERLLMToolCall *ptr);

/**
 * Get the `call_type` field from a `ToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMToolType *literllm_tool_call_call_type(const LITERLLMToolCall *ptr);

/**
 * Get the `function` field from a `ToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMFunctionCall *literllm_tool_call_function(const LITERLLMToolCall *ptr);

/**
 * Create a `FunctionCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_function_call_free`.
 */
LITERLLMFunctionCall *literllm_function_call_from_json(const char *json);

/**
 * Serialize a `FunctionCall` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_function_call_to_json(const LITERLLMFunctionCall *ptr);

/**
 * Free a `FunctionCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_function_call_free(LITERLLMFunctionCall *ptr);

/**
 * Get the `name` field from a `FunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_function_call_name(const LITERLLMFunctionCall *ptr);

/**
 * Get the `arguments` field from a `FunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_function_call_arguments(const LITERLLMFunctionCall *ptr);

/**
 * Create a `SpecificToolChoice` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_specific_tool_choice_free`.
 */
LITERLLMSpecificToolChoice *literllm_specific_tool_choice_from_json(const char *json);

/**
 * Serialize a `SpecificToolChoice` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_specific_tool_choice_to_json(const LITERLLMSpecificToolChoice *ptr);

/**
 * Free a `SpecificToolChoice` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_specific_tool_choice_free(LITERLLMSpecificToolChoice *ptr);

/**
 * Get the `choice_type` field from a `SpecificToolChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMToolType *literllm_specific_tool_choice_choice_type(const LITERLLMSpecificToolChoice *ptr);

/**
 * Get the `function` field from a `SpecificToolChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMSpecificFunction *literllm_specific_tool_choice_function(const LITERLLMSpecificToolChoice *ptr);

/**
 * Create a `SpecificFunction` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_specific_function_free`.
 */
LITERLLMSpecificFunction *literllm_specific_function_from_json(const char *json);

/**
 * Serialize a `SpecificFunction` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_specific_function_to_json(const LITERLLMSpecificFunction *ptr);

/**
 * Free a `SpecificFunction` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_specific_function_free(LITERLLMSpecificFunction *ptr);

/**
 * Get the `name` field from a `SpecificFunction`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_specific_function_name(const LITERLLMSpecificFunction *ptr);

/**
 * Create a `JsonSchemaFormat` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_json_schema_format_free`.
 */
LITERLLMJsonSchemaFormat *literllm_json_schema_format_from_json(const char *json);

/**
 * Serialize a `JsonSchemaFormat` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_json_schema_format_to_json(const LITERLLMJsonSchemaFormat *ptr);

/**
 * Free a `JsonSchemaFormat` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_json_schema_format_free(LITERLLMJsonSchemaFormat *ptr);

/**
 * Get the `name` field from a `JsonSchemaFormat`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_json_schema_format_name(const LITERLLMJsonSchemaFormat *ptr);

/**
 * Get the `description` field from a `JsonSchemaFormat`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_json_schema_format_description(const LITERLLMJsonSchemaFormat *ptr);

/**
 * Get the `schema` field from a `JsonSchemaFormat`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_json_schema_format_schema(const LITERLLMJsonSchemaFormat *ptr);

/**
 * Get the `strict` field from a `JsonSchemaFormat`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_json_schema_format_strict(const LITERLLMJsonSchemaFormat *ptr);

/**
 * Create a `Usage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_usage_free`.
 */
LITERLLMUsage *literllm_usage_from_json(const char *json);

/**
 * Serialize a `Usage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_usage_to_json(const LITERLLMUsage *ptr);

/**
 * Free a `Usage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_usage_free(LITERLLMUsage *ptr);

/**
 * Get the `prompt_tokens` field from a `Usage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_usage_prompt_tokens(const LITERLLMUsage *ptr);

/**
 * Get the `completion_tokens` field from a `Usage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_usage_completion_tokens(const LITERLLMUsage *ptr);

/**
 * Get the `total_tokens` field from a `Usage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_usage_total_tokens(const LITERLLMUsage *ptr);

/**
 * Create a `ChatCompletionRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_chat_completion_request_free`.
 */
LITERLLMChatCompletionRequest *literllm_chat_completion_request_from_json(const char *json);

/**
 * Serialize a `ChatCompletionRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_chat_completion_request_to_json(const LITERLLMChatCompletionRequest *ptr);

/**
 * Free a `ChatCompletionRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_chat_completion_request_free(LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `model` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_request_model(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `messages` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_request_messages(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `temperature` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_chat_completion_request_temperature(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `top_p` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_chat_completion_request_top_p(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `n` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_chat_completion_request_n(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `stream` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_chat_completion_request_stream(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `stop` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMStopSequence *literllm_chat_completion_request_stop(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `max_tokens` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_chat_completion_request_max_tokens(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `presence_penalty` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_chat_completion_request_presence_penalty(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `frequency_penalty` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_chat_completion_request_frequency_penalty(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `logit_bias` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_request_logit_bias(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `user` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_request_user(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `tools` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_request_tools(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `tool_choice` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMToolChoice *literllm_chat_completion_request_tool_choice(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `parallel_tool_calls` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_chat_completion_request_parallel_tool_calls(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `response_format` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMResponseFormat *literllm_chat_completion_request_response_format(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `stream_options` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMStreamOptions *literllm_chat_completion_request_stream_options(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `seed` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int64_t literllm_chat_completion_request_seed(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `reasoning_effort` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMReasoningEffort *literllm_chat_completion_request_reasoning_effort(const LITERLLMChatCompletionRequest *ptr);

/**
 * Get the `extra_body` field from a `ChatCompletionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_request_extra_body(const LITERLLMChatCompletionRequest *ptr);

/**
 * Create a `StreamOptions` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_stream_options_free`.
 */
LITERLLMStreamOptions *literllm_stream_options_from_json(const char *json);

/**
 * Serialize a `StreamOptions` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_stream_options_to_json(const LITERLLMStreamOptions *ptr);

/**
 * Free a `StreamOptions` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_stream_options_free(LITERLLMStreamOptions *ptr);

/**
 * Get the `include_usage` field from a `StreamOptions`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_stream_options_include_usage(const LITERLLMStreamOptions *ptr);

/**
 * Create a `ChatCompletionResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_chat_completion_response_free`.
 */
LITERLLMChatCompletionResponse *literllm_chat_completion_response_from_json(const char *json);

/**
 * Serialize a `ChatCompletionResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_chat_completion_response_to_json(const LITERLLMChatCompletionResponse *ptr);

/**
 * Free a `ChatCompletionResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_chat_completion_response_free(LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `id` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_response_id(const LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `object` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_response_object(const LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `created` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_chat_completion_response_created(const LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `model` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_response_model(const LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `choices` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_response_choices(const LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `usage` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMUsage *literllm_chat_completion_response_usage(const LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `system_fingerprint` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_response_system_fingerprint(const LITERLLMChatCompletionResponse *ptr);

/**
 * Get the `service_tier` field from a `ChatCompletionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_response_service_tier(const LITERLLMChatCompletionResponse *ptr);

/**
 * Create a `Choice` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_choice_free`.
 */
LITERLLMChoice *literllm_choice_from_json(const char *json);

/**
 * Serialize a `Choice` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_choice_to_json(const LITERLLMChoice *ptr);

/**
 * Free a `Choice` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_choice_free(LITERLLMChoice *ptr);

/**
 * Get the `index` field from a `Choice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_choice_index(const LITERLLMChoice *ptr);

/**
 * Get the `message` field from a `Choice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMAssistantMessage *literllm_choice_message(const LITERLLMChoice *ptr);

/**
 * Get the `finish_reason` field from a `Choice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMFinishReason *literllm_choice_finish_reason(const LITERLLMChoice *ptr);

/**
 * Create a `ChatCompletionChunk` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_chat_completion_chunk_free`.
 */
LITERLLMChatCompletionChunk *literllm_chat_completion_chunk_from_json(const char *json);

/**
 * Serialize a `ChatCompletionChunk` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_chat_completion_chunk_to_json(const LITERLLMChatCompletionChunk *ptr);

/**
 * Free a `ChatCompletionChunk` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_chat_completion_chunk_free(LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `id` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_chunk_id(const LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `object` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_chunk_object(const LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `created` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_chat_completion_chunk_created(const LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `model` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_chunk_model(const LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `choices` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_chunk_choices(const LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `usage` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMUsage *literllm_chat_completion_chunk_usage(const LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `system_fingerprint` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_chunk_system_fingerprint(const LITERLLMChatCompletionChunk *ptr);

/**
 * Get the `service_tier` field from a `ChatCompletionChunk`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_chat_completion_chunk_service_tier(const LITERLLMChatCompletionChunk *ptr);

/**
 * Create a `StreamChoice` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_stream_choice_free`.
 */
LITERLLMStreamChoice *literllm_stream_choice_from_json(const char *json);

/**
 * Serialize a `StreamChoice` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_stream_choice_to_json(const LITERLLMStreamChoice *ptr);

/**
 * Free a `StreamChoice` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_stream_choice_free(LITERLLMStreamChoice *ptr);

/**
 * Get the `index` field from a `StreamChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_stream_choice_index(const LITERLLMStreamChoice *ptr);

/**
 * Get the `delta` field from a `StreamChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMStreamDelta *literllm_stream_choice_delta(const LITERLLMStreamChoice *ptr);

/**
 * Get the `finish_reason` field from a `StreamChoice`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMFinishReason *literllm_stream_choice_finish_reason(const LITERLLMStreamChoice *ptr);

/**
 * Create a `StreamDelta` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_stream_delta_free`.
 */
LITERLLMStreamDelta *literllm_stream_delta_from_json(const char *json);

/**
 * Serialize a `StreamDelta` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_stream_delta_to_json(const LITERLLMStreamDelta *ptr);

/**
 * Free a `StreamDelta` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_stream_delta_free(LITERLLMStreamDelta *ptr);

/**
 * Get the `role` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_stream_delta_role(const LITERLLMStreamDelta *ptr);

/**
 * Get the `content` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_stream_delta_content(const LITERLLMStreamDelta *ptr);

/**
 * Get the `tool_calls` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_stream_delta_tool_calls(const LITERLLMStreamDelta *ptr);

/**
 * Get the `function_call` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMStreamFunctionCall *literllm_stream_delta_function_call(const LITERLLMStreamDelta *ptr);

/**
 * Get the `refusal` field from a `StreamDelta`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_stream_delta_refusal(const LITERLLMStreamDelta *ptr);

/**
 * Create a `StreamToolCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_stream_tool_call_free`.
 */
LITERLLMStreamToolCall *literllm_stream_tool_call_from_json(const char *json);

/**
 * Serialize a `StreamToolCall` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_stream_tool_call_to_json(const LITERLLMStreamToolCall *ptr);

/**
 * Free a `StreamToolCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_stream_tool_call_free(LITERLLMStreamToolCall *ptr);

/**
 * Get the `index` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_stream_tool_call_index(const LITERLLMStreamToolCall *ptr);

/**
 * Get the `id` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_stream_tool_call_id(const LITERLLMStreamToolCall *ptr);

/**
 * Get the `call_type` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMToolType *literllm_stream_tool_call_call_type(const LITERLLMStreamToolCall *ptr);

/**
 * Get the `function` field from a `StreamToolCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMStreamFunctionCall *literllm_stream_tool_call_function(const LITERLLMStreamToolCall *ptr);

/**
 * Create a `StreamFunctionCall` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_stream_function_call_free`.
 */
LITERLLMStreamFunctionCall *literllm_stream_function_call_from_json(const char *json);

/**
 * Serialize a `StreamFunctionCall` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_stream_function_call_to_json(const LITERLLMStreamFunctionCall *ptr);

/**
 * Free a `StreamFunctionCall` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_stream_function_call_free(LITERLLMStreamFunctionCall *ptr);

/**
 * Get the `name` field from a `StreamFunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_stream_function_call_name(const LITERLLMStreamFunctionCall *ptr);

/**
 * Get the `arguments` field from a `StreamFunctionCall`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_stream_function_call_arguments(const LITERLLMStreamFunctionCall *ptr);

/**
 * Create a `EmbeddingRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_embedding_request_free`.
 */
LITERLLMEmbeddingRequest *literllm_embedding_request_from_json(const char *json);

/**
 * Serialize a `EmbeddingRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_embedding_request_to_json(const LITERLLMEmbeddingRequest *ptr);

/**
 * Free a `EmbeddingRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_embedding_request_free(LITERLLMEmbeddingRequest *ptr);

/**
 * Get the `model` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_embedding_request_model(const LITERLLMEmbeddingRequest *ptr);

/**
 * Get the `input` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMEmbeddingInput *literllm_embedding_request_input(const LITERLLMEmbeddingRequest *ptr);

/**
 * Get the `encoding_format` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMEmbeddingFormat *literllm_embedding_request_encoding_format(const LITERLLMEmbeddingRequest *ptr);

/**
 * Get the `dimensions` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_embedding_request_dimensions(const LITERLLMEmbeddingRequest *ptr);

/**
 * Get the `user` field from a `EmbeddingRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_embedding_request_user(const LITERLLMEmbeddingRequest *ptr);

/**
 * Create a `EmbeddingResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_embedding_response_free`.
 */
LITERLLMEmbeddingResponse *literllm_embedding_response_from_json(const char *json);

/**
 * Serialize a `EmbeddingResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_embedding_response_to_json(const LITERLLMEmbeddingResponse *ptr);

/**
 * Free a `EmbeddingResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_embedding_response_free(LITERLLMEmbeddingResponse *ptr);

/**
 * Get the `object` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_embedding_response_object(const LITERLLMEmbeddingResponse *ptr);

/**
 * Get the `data` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_embedding_response_data(const LITERLLMEmbeddingResponse *ptr);

/**
 * Get the `model` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_embedding_response_model(const LITERLLMEmbeddingResponse *ptr);

/**
 * Get the `usage` field from a `EmbeddingResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMUsage *literllm_embedding_response_usage(const LITERLLMEmbeddingResponse *ptr);

/**
 * Create a `EmbeddingObject` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_embedding_object_free`.
 */
LITERLLMEmbeddingObject *literllm_embedding_object_from_json(const char *json);

/**
 * Serialize a `EmbeddingObject` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_embedding_object_to_json(const LITERLLMEmbeddingObject *ptr);

/**
 * Free a `EmbeddingObject` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_embedding_object_free(LITERLLMEmbeddingObject *ptr);

/**
 * Get the `object` field from a `EmbeddingObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_embedding_object_object(const LITERLLMEmbeddingObject *ptr);

/**
 * Get the `embedding` field from a `EmbeddingObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_embedding_object_embedding(const LITERLLMEmbeddingObject *ptr);

/**
 * Get the `index` field from a `EmbeddingObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_embedding_object_index(const LITERLLMEmbeddingObject *ptr);

/**
 * Create a `CreateImageRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_create_image_request_free`.
 */
LITERLLMCreateImageRequest *literllm_create_image_request_from_json(const char *json);

/**
 * Serialize a `CreateImageRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_create_image_request_to_json(const LITERLLMCreateImageRequest *ptr);

/**
 * Free a `CreateImageRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_create_image_request_free(LITERLLMCreateImageRequest *ptr);

/**
 * Get the `prompt` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_image_request_prompt(const LITERLLMCreateImageRequest *ptr);

/**
 * Get the `model` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_image_request_model(const LITERLLMCreateImageRequest *ptr);

/**
 * Get the `n` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_create_image_request_n(const LITERLLMCreateImageRequest *ptr);

/**
 * Get the `size` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_image_request_size(const LITERLLMCreateImageRequest *ptr);

/**
 * Get the `quality` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_image_request_quality(const LITERLLMCreateImageRequest *ptr);

/**
 * Get the `style` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_image_request_style(const LITERLLMCreateImageRequest *ptr);

/**
 * Get the `response_format` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_image_request_response_format(const LITERLLMCreateImageRequest *ptr);

/**
 * Get the `user` field from a `CreateImageRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_image_request_user(const LITERLLMCreateImageRequest *ptr);

/**
 * Create a `ImagesResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_images_response_free`.
 */
LITERLLMImagesResponse *literllm_images_response_from_json(const char *json);

/**
 * Serialize a `ImagesResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_images_response_to_json(const LITERLLMImagesResponse *ptr);

/**
 * Free a `ImagesResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_images_response_free(LITERLLMImagesResponse *ptr);

/**
 * Get the `created` field from a `ImagesResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_images_response_created(const LITERLLMImagesResponse *ptr);

/**
 * Get the `data` field from a `ImagesResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_images_response_data(const LITERLLMImagesResponse *ptr);

/**
 * Create a `Image` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_image_free`.
 */
LITERLLMImage *literllm_image_from_json(const char *json);

/**
 * Serialize a `Image` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_image_to_json(const LITERLLMImage *ptr);

/**
 * Free a `Image` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_image_free(LITERLLMImage *ptr);

/**
 * Get the `url` field from a `Image`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_image_url(const LITERLLMImage *ptr);

/**
 * Get the `b64_json` field from a `Image`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_image_b64_json(const LITERLLMImage *ptr);

/**
 * Get the `revised_prompt` field from a `Image`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_image_revised_prompt(const LITERLLMImage *ptr);

/**
 * Create a `CreateSpeechRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_create_speech_request_free`.
 */
LITERLLMCreateSpeechRequest *literllm_create_speech_request_from_json(const char *json);

/**
 * Serialize a `CreateSpeechRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_create_speech_request_to_json(const LITERLLMCreateSpeechRequest *ptr);

/**
 * Free a `CreateSpeechRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_create_speech_request_free(LITERLLMCreateSpeechRequest *ptr);

/**
 * Get the `model` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_speech_request_model(const LITERLLMCreateSpeechRequest *ptr);

/**
 * Get the `input` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_speech_request_input(const LITERLLMCreateSpeechRequest *ptr);

/**
 * Get the `voice` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_speech_request_voice(const LITERLLMCreateSpeechRequest *ptr);

/**
 * Get the `response_format` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_speech_request_response_format(const LITERLLMCreateSpeechRequest *ptr);

/**
 * Get the `speed` field from a `CreateSpeechRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_create_speech_request_speed(const LITERLLMCreateSpeechRequest *ptr);

/**
 * Create a `CreateTranscriptionRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_create_transcription_request_free`.
 */
LITERLLMCreateTranscriptionRequest *literllm_create_transcription_request_from_json(const char *json);

/**
 * Serialize a `CreateTranscriptionRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_create_transcription_request_to_json(const LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Free a `CreateTranscriptionRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_create_transcription_request_free(LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Get the `model` field from a `CreateTranscriptionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_transcription_request_model(const LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Get the `file` field from a `CreateTranscriptionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_transcription_request_file(const LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Get the `language` field from a `CreateTranscriptionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_transcription_request_language(const LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Get the `prompt` field from a `CreateTranscriptionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_transcription_request_prompt(const LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Get the `response_format` field from a `CreateTranscriptionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_create_transcription_request_response_format(const LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Get the `temperature` field from a `CreateTranscriptionRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_create_transcription_request_temperature(const LITERLLMCreateTranscriptionRequest *ptr);

/**
 * Create a `TranscriptionResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_transcription_response_free`.
 */
LITERLLMTranscriptionResponse *literllm_transcription_response_from_json(const char *json);

/**
 * Serialize a `TranscriptionResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_transcription_response_to_json(const LITERLLMTranscriptionResponse *ptr);

/**
 * Free a `TranscriptionResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_transcription_response_free(LITERLLMTranscriptionResponse *ptr);

/**
 * Get the `text` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_transcription_response_text(const LITERLLMTranscriptionResponse *ptr);

/**
 * Get the `language` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_transcription_response_language(const LITERLLMTranscriptionResponse *ptr);

/**
 * Get the `duration` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_transcription_response_duration(const LITERLLMTranscriptionResponse *ptr);

/**
 * Get the `segments` field from a `TranscriptionResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_transcription_response_segments(const LITERLLMTranscriptionResponse *ptr);

/**
 * Create a `TranscriptionSegment` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_transcription_segment_free`.
 */
LITERLLMTranscriptionSegment *literllm_transcription_segment_from_json(const char *json);

/**
 * Serialize a `TranscriptionSegment` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_transcription_segment_to_json(const LITERLLMTranscriptionSegment *ptr);

/**
 * Free a `TranscriptionSegment` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_transcription_segment_free(LITERLLMTranscriptionSegment *ptr);

/**
 * Get the `id` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_transcription_segment_id(const LITERLLMTranscriptionSegment *ptr);

/**
 * Get the `start` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_transcription_segment_start(const LITERLLMTranscriptionSegment *ptr);

/**
 * Get the `end` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_transcription_segment_end(const LITERLLMTranscriptionSegment *ptr);

/**
 * Get the `text` field from a `TranscriptionSegment`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_transcription_segment_text(const LITERLLMTranscriptionSegment *ptr);

/**
 * Create a `ModerationRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_moderation_request_free`.
 */
LITERLLMModerationRequest *literllm_moderation_request_from_json(const char *json);

/**
 * Serialize a `ModerationRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_moderation_request_to_json(const LITERLLMModerationRequest *ptr);

/**
 * Free a `ModerationRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_moderation_request_free(LITERLLMModerationRequest *ptr);

/**
 * Get the `input` field from a `ModerationRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMModerationInput *literllm_moderation_request_input(const LITERLLMModerationRequest *ptr);

/**
 * Get the `model` field from a `ModerationRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_moderation_request_model(const LITERLLMModerationRequest *ptr);

/**
 * Create a `ModerationResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_moderation_response_free`.
 */
LITERLLMModerationResponse *literllm_moderation_response_from_json(const char *json);

/**
 * Serialize a `ModerationResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_moderation_response_to_json(const LITERLLMModerationResponse *ptr);

/**
 * Free a `ModerationResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_moderation_response_free(LITERLLMModerationResponse *ptr);

/**
 * Get the `id` field from a `ModerationResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_moderation_response_id(const LITERLLMModerationResponse *ptr);

/**
 * Get the `model` field from a `ModerationResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_moderation_response_model(const LITERLLMModerationResponse *ptr);

/**
 * Get the `results` field from a `ModerationResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_moderation_response_results(const LITERLLMModerationResponse *ptr);

/**
 * Create a `ModerationResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_moderation_result_free`.
 */
LITERLLMModerationResult *literllm_moderation_result_from_json(const char *json);

/**
 * Serialize a `ModerationResult` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_moderation_result_to_json(const LITERLLMModerationResult *ptr);

/**
 * Free a `ModerationResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_moderation_result_free(LITERLLMModerationResult *ptr);

/**
 * Get the `flagged` field from a `ModerationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_result_flagged(const LITERLLMModerationResult *ptr);

/**
 * Get the `categories` field from a `ModerationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMModerationCategories *literllm_moderation_result_categories(const LITERLLMModerationResult *ptr);

/**
 * Get the `category_scores` field from a `ModerationResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMModerationCategoryScores *literllm_moderation_result_category_scores(const LITERLLMModerationResult *ptr);

/**
 * Create a `ModerationCategories` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_moderation_categories_free`.
 */
LITERLLMModerationCategories *literllm_moderation_categories_from_json(const char *json);

/**
 * Serialize a `ModerationCategories` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_moderation_categories_to_json(const LITERLLMModerationCategories *ptr);

/**
 * Free a `ModerationCategories` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_moderation_categories_free(LITERLLMModerationCategories *ptr);

/**
 * Get the `sexual` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_sexual(const LITERLLMModerationCategories *ptr);

/**
 * Get the `hate` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_hate(const LITERLLMModerationCategories *ptr);

/**
 * Get the `harassment` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_harassment(const LITERLLMModerationCategories *ptr);

/**
 * Get the `self_harm` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_self_harm(const LITERLLMModerationCategories *ptr);

/**
 * Get the `sexual_minors` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_sexual_minors(const LITERLLMModerationCategories *ptr);

/**
 * Get the `hate_threatening` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_hate_threatening(const LITERLLMModerationCategories *ptr);

/**
 * Get the `violence_graphic` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_violence_graphic(const LITERLLMModerationCategories *ptr);

/**
 * Get the `self_harm_intent` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_self_harm_intent(const LITERLLMModerationCategories *ptr);

/**
 * Get the `self_harm_instructions` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_self_harm_instructions(const LITERLLMModerationCategories *ptr);

/**
 * Get the `harassment_threatening` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_harassment_threatening(const LITERLLMModerationCategories *ptr);

/**
 * Get the `violence` field from a `ModerationCategories`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_moderation_categories_violence(const LITERLLMModerationCategories *ptr);

/**
 * Create a `ModerationCategoryScores` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_moderation_category_scores_free`.
 */
LITERLLMModerationCategoryScores *literllm_moderation_category_scores_from_json(const char *json);

/**
 * Serialize a `ModerationCategoryScores` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_moderation_category_scores_to_json(const LITERLLMModerationCategoryScores *ptr);

/**
 * Free a `ModerationCategoryScores` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_moderation_category_scores_free(LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `sexual` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_sexual(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `hate` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_hate(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `harassment` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_harassment(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `self_harm` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_self_harm(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `sexual_minors` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_sexual_minors(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `hate_threatening` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_hate_threatening(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `violence_graphic` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_violence_graphic(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `self_harm_intent` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_self_harm_intent(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `self_harm_instructions` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_self_harm_instructions(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `harassment_threatening` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_harassment_threatening(const LITERLLMModerationCategoryScores *ptr);

/**
 * Get the `violence` field from a `ModerationCategoryScores`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_moderation_category_scores_violence(const LITERLLMModerationCategoryScores *ptr);

/**
 * Create a `RerankRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_rerank_request_free`.
 */
LITERLLMRerankRequest *literllm_rerank_request_from_json(const char *json);

/**
 * Serialize a `RerankRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_rerank_request_to_json(const LITERLLMRerankRequest *ptr);

/**
 * Free a `RerankRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_rerank_request_free(LITERLLMRerankRequest *ptr);

/**
 * Get the `model` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_rerank_request_model(const LITERLLMRerankRequest *ptr);

/**
 * Get the `query` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_rerank_request_query(const LITERLLMRerankRequest *ptr);

/**
 * Get the `documents` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_rerank_request_documents(const LITERLLMRerankRequest *ptr);

/**
 * Get the `top_n` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_rerank_request_top_n(const LITERLLMRerankRequest *ptr);

/**
 * Get the `return_documents` field from a `RerankRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_rerank_request_return_documents(const LITERLLMRerankRequest *ptr);

/**
 * Create a `RerankResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_rerank_response_free`.
 */
LITERLLMRerankResponse *literllm_rerank_response_from_json(const char *json);

/**
 * Serialize a `RerankResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_rerank_response_to_json(const LITERLLMRerankResponse *ptr);

/**
 * Free a `RerankResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_rerank_response_free(LITERLLMRerankResponse *ptr);

/**
 * Get the `id` field from a `RerankResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_rerank_response_id(const LITERLLMRerankResponse *ptr);

/**
 * Get the `results` field from a `RerankResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_rerank_response_results(const LITERLLMRerankResponse *ptr);

/**
 * Get the `meta` field from a `RerankResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_rerank_response_meta(const LITERLLMRerankResponse *ptr);

/**
 * Create a `RerankResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_rerank_result_free`.
 */
LITERLLMRerankResult *literllm_rerank_result_from_json(const char *json);

/**
 * Serialize a `RerankResult` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_rerank_result_to_json(const LITERLLMRerankResult *ptr);

/**
 * Free a `RerankResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_rerank_result_free(LITERLLMRerankResult *ptr);

/**
 * Get the `index` field from a `RerankResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_rerank_result_index(const LITERLLMRerankResult *ptr);

/**
 * Get the `relevance_score` field from a `RerankResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
double literllm_rerank_result_relevance_score(const LITERLLMRerankResult *ptr);

/**
 * Get the `document` field from a `RerankResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMRerankResultDocument *literllm_rerank_result_document(const LITERLLMRerankResult *ptr);

/**
 * Create a `RerankResultDocument` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_rerank_result_document_free`.
 */
LITERLLMRerankResultDocument *literllm_rerank_result_document_from_json(const char *json);

/**
 * Serialize a `RerankResultDocument` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_rerank_result_document_to_json(const LITERLLMRerankResultDocument *ptr);

/**
 * Free a `RerankResultDocument` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_rerank_result_document_free(LITERLLMRerankResultDocument *ptr);

/**
 * Get the `text` field from a `RerankResultDocument`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_rerank_result_document_text(const LITERLLMRerankResultDocument *ptr);

/**
 * Create a `SearchRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_search_request_free`.
 */
LITERLLMSearchRequest *literllm_search_request_from_json(const char *json);

/**
 * Serialize a `SearchRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_search_request_to_json(const LITERLLMSearchRequest *ptr);

/**
 * Free a `SearchRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_search_request_free(LITERLLMSearchRequest *ptr);

/**
 * Get the `model` field from a `SearchRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_request_model(const LITERLLMSearchRequest *ptr);

/**
 * Get the `query` field from a `SearchRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_request_query(const LITERLLMSearchRequest *ptr);

/**
 * Get the `max_results` field from a `SearchRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_search_request_max_results(const LITERLLMSearchRequest *ptr);

/**
 * Get the `search_domain_filter` field from a `SearchRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_request_search_domain_filter(const LITERLLMSearchRequest *ptr);

/**
 * Get the `country` field from a `SearchRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_request_country(const LITERLLMSearchRequest *ptr);

/**
 * Create a `SearchResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_search_response_free`.
 */
LITERLLMSearchResponse *literllm_search_response_from_json(const char *json);

/**
 * Serialize a `SearchResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_search_response_to_json(const LITERLLMSearchResponse *ptr);

/**
 * Free a `SearchResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_search_response_free(LITERLLMSearchResponse *ptr);

/**
 * Get the `results` field from a `SearchResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_response_results(const LITERLLMSearchResponse *ptr);

/**
 * Get the `model` field from a `SearchResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_response_model(const LITERLLMSearchResponse *ptr);

/**
 * Create a `SearchResult` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_search_result_free`.
 */
LITERLLMSearchResult *literllm_search_result_from_json(const char *json);

/**
 * Serialize a `SearchResult` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_search_result_to_json(const LITERLLMSearchResult *ptr);

/**
 * Free a `SearchResult` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_search_result_free(LITERLLMSearchResult *ptr);

/**
 * Get the `title` field from a `SearchResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_result_title(const LITERLLMSearchResult *ptr);

/**
 * Get the `url` field from a `SearchResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_result_url(const LITERLLMSearchResult *ptr);

/**
 * Get the `snippet` field from a `SearchResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_result_snippet(const LITERLLMSearchResult *ptr);

/**
 * Get the `date` field from a `SearchResult`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_search_result_date(const LITERLLMSearchResult *ptr);

/**
 * Create a `OcrRequest` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_ocr_request_free`.
 */
LITERLLMOcrRequest *literllm_ocr_request_from_json(const char *json);

/**
 * Serialize a `OcrRequest` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_ocr_request_to_json(const LITERLLMOcrRequest *ptr);

/**
 * Free a `OcrRequest` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_ocr_request_free(LITERLLMOcrRequest *ptr);

/**
 * Get the `model` field from a `OcrRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_request_model(const LITERLLMOcrRequest *ptr);

/**
 * Get the `document` field from a `OcrRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMOcrDocument *literllm_ocr_request_document(const LITERLLMOcrRequest *ptr);

/**
 * Get the `pages` field from a `OcrRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_request_pages(const LITERLLMOcrRequest *ptr);

/**
 * Get the `include_image_base64` field from a `OcrRequest`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
int32_t literllm_ocr_request_include_image_base64(const LITERLLMOcrRequest *ptr);

/**
 * Create a `OcrResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_ocr_response_free`.
 */
LITERLLMOcrResponse *literllm_ocr_response_from_json(const char *json);

/**
 * Serialize a `OcrResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_ocr_response_to_json(const LITERLLMOcrResponse *ptr);

/**
 * Free a `OcrResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_ocr_response_free(LITERLLMOcrResponse *ptr);

/**
 * Get the `pages` field from a `OcrResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_response_pages(const LITERLLMOcrResponse *ptr);

/**
 * Get the `model` field from a `OcrResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_response_model(const LITERLLMOcrResponse *ptr);

/**
 * Get the `usage` field from a `OcrResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMUsage *literllm_ocr_response_usage(const LITERLLMOcrResponse *ptr);

/**
 * Create a `OcrPage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_ocr_page_free`.
 */
LITERLLMOcrPage *literllm_ocr_page_from_json(const char *json);

/**
 * Serialize a `OcrPage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_ocr_page_to_json(const LITERLLMOcrPage *ptr);

/**
 * Free a `OcrPage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_ocr_page_free(LITERLLMOcrPage *ptr);

/**
 * Get the `index` field from a `OcrPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_ocr_page_index(const LITERLLMOcrPage *ptr);

/**
 * Get the `markdown` field from a `OcrPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_page_markdown(const LITERLLMOcrPage *ptr);

/**
 * Get the `images` field from a `OcrPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_page_images(const LITERLLMOcrPage *ptr);

/**
 * Get the `dimensions` field from a `OcrPage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMPageDimensions *literllm_ocr_page_dimensions(const LITERLLMOcrPage *ptr);

/**
 * Create a `OcrImage` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_ocr_image_free`.
 */
LITERLLMOcrImage *literllm_ocr_image_from_json(const char *json);

/**
 * Serialize a `OcrImage` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_ocr_image_to_json(const LITERLLMOcrImage *ptr);

/**
 * Free a `OcrImage` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_ocr_image_free(LITERLLMOcrImage *ptr);

/**
 * Get the `id` field from a `OcrImage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_image_id(const LITERLLMOcrImage *ptr);

/**
 * Get the `image_base64` field from a `OcrImage`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_ocr_image_image_base64(const LITERLLMOcrImage *ptr);

/**
 * Create a `PageDimensions` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_page_dimensions_free`.
 */
LITERLLMPageDimensions *literllm_page_dimensions_from_json(const char *json);

/**
 * Serialize a `PageDimensions` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_page_dimensions_to_json(const LITERLLMPageDimensions *ptr);

/**
 * Free a `PageDimensions` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_page_dimensions_free(LITERLLMPageDimensions *ptr);

/**
 * Get the `width` field from a `PageDimensions`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_page_dimensions_width(const LITERLLMPageDimensions *ptr);

/**
 * Get the `height` field from a `PageDimensions`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint32_t literllm_page_dimensions_height(const LITERLLMPageDimensions *ptr);

/**
 * Create a `ModelsListResponse` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_models_list_response_free`.
 */
LITERLLMModelsListResponse *literllm_models_list_response_from_json(const char *json);

/**
 * Serialize a `ModelsListResponse` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_models_list_response_to_json(const LITERLLMModelsListResponse *ptr);

/**
 * Free a `ModelsListResponse` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_models_list_response_free(LITERLLMModelsListResponse *ptr);

/**
 * Get the `object` field from a `ModelsListResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_models_list_response_object(const LITERLLMModelsListResponse *ptr);

/**
 * Get the `data` field from a `ModelsListResponse`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_models_list_response_data(const LITERLLMModelsListResponse *ptr);

/**
 * Create a `ModelObject` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_model_object_free`.
 */
LITERLLMModelObject *literllm_model_object_from_json(const char *json);

/**
 * Serialize a `ModelObject` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_model_object_to_json(const LITERLLMModelObject *ptr);

/**
 * Free a `ModelObject` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_model_object_free(LITERLLMModelObject *ptr);

/**
 * Get the `id` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_model_object_id(const LITERLLMModelObject *ptr);

/**
 * Get the `object` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_model_object_object(const LITERLLMModelObject *ptr);

/**
 * Get the `created` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
uint64_t literllm_model_object_created(const LITERLLMModelObject *ptr);

/**
 * Get the `owned_by` field from a `ModelObject`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_model_object_owned_by(const LITERLLMModelObject *ptr);

/**
 * Free a `DefaultClient` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_default_client_free(LITERLLMDefaultClient *ptr);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMChatCompletionResponse *literllm_default_client_chat(const LITERLLMDefaultClient *this_,
                                                             const LITERLLMChatCompletionRequest *req);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
char *literllm_default_client_chat_stream(const LITERLLMDefaultClient *_this,
                                          const LITERLLMChatCompletionRequest *_req);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMEmbeddingResponse *literllm_default_client_embed(const LITERLLMDefaultClient *this_,
                                                         const LITERLLMEmbeddingRequest *req);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMModelsListResponse *literllm_default_client_list_models(const LITERLLMDefaultClient *this_);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMImagesResponse *literllm_default_client_image_generate(const LITERLLMDefaultClient *this_,
                                                               const LITERLLMCreateImageRequest *req);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMTranscriptionResponse *literllm_default_client_transcribe(const LITERLLMDefaultClient *this_,
                                                                  const LITERLLMCreateTranscriptionRequest *req);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMModerationResponse *literllm_default_client_moderate(const LITERLLMDefaultClient *this_,
                                                             const LITERLLMModerationRequest *req);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMRerankResponse *literllm_default_client_rerank(const LITERLLMDefaultClient *this_,
                                                       const LITERLLMRerankRequest *req);

/**
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMSearchResponse *literllm_default_client_search(const LITERLLMDefaultClient *this_,
                                                       const LITERLLMSearchRequest *req);

/**
 * Create a `CustomProviderConfig` from a JSON string. Returns null on failure.
 * # Safety
 * JSON string must be valid UTF-8 and null-terminated.
 * Returned handle must be freed with `literllm_custom_provider_config_free`.
 */
LITERLLMCustomProviderConfig *literllm_custom_provider_config_from_json(const char *json);

/**
 * Serialize a `CustomProviderConfig` to a JSON string. Returns null on failure.
 * # Safety
 * `ptr` must be a valid, non-null pointer returned by a `literllm` function.
 * The returned string must be freed with `literllm_free_string`.
 */
char *literllm_custom_provider_config_to_json(const LITERLLMCustomProviderConfig *ptr);

/**
 * Free a `CustomProviderConfig` handle.
 * # Safety
 * Pointer must have been returned by this library, or be null.
 */
void literllm_custom_provider_config_free(LITERLLMCustomProviderConfig *ptr);

/**
 * Get the `name` field from a `CustomProviderConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_custom_provider_config_name(const LITERLLMCustomProviderConfig *ptr);

/**
 * Get the `base_url` field from a `CustomProviderConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_custom_provider_config_base_url(const LITERLLMCustomProviderConfig *ptr);

/**
 * Get the `auth_header` field from a `CustomProviderConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
LITERLLMAuthHeaderFormat *literllm_custom_provider_config_auth_header(const LITERLLMCustomProviderConfig *ptr);

/**
 * Get the `model_prefixes` field from a `CustomProviderConfig`.
 * # Safety
 * Pointer must be a valid handle returned by this library.
 */
char *literllm_custom_provider_config_model_prefixes(const LITERLLMCustomProviderConfig *ptr);

/**
 * Convert an integer to a `Message` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_message_from_i32(int32_t value);

/**
 * Convert a `Message` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_message_from_str(const char *name);

/**
 * Convert an integer to a `UserContent` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_user_content_from_i32(int32_t value);

/**
 * Convert a `UserContent` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_user_content_from_str(const char *name);

/**
 * Convert an integer to a `ContentPart` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_content_part_from_i32(int32_t value);

/**
 * Convert a `ContentPart` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_content_part_from_str(const char *name);

/**
 * Convert an integer to a `ImageDetail` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_image_detail_from_i32(int32_t value);

/**
 * Convert a `ImageDetail` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_image_detail_from_str(const char *name);

/**
 * Convert an integer to a `ToolType` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_tool_type_from_i32(int32_t value);

/**
 * Convert a `ToolType` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_tool_type_from_str(const char *name);

/**
 * Convert an integer to a `ToolChoice` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_tool_choice_from_i32(int32_t value);

/**
 * Convert a `ToolChoice` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_tool_choice_from_str(const char *name);

/**
 * Convert an integer to a `ToolChoiceMode` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_tool_choice_mode_from_i32(int32_t value);

/**
 * Convert a `ToolChoiceMode` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_tool_choice_mode_from_str(const char *name);

/**
 * Convert an integer to a `ResponseFormat` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_response_format_from_i32(int32_t value);

/**
 * Convert a `ResponseFormat` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_response_format_from_str(const char *name);

/**
 * Convert an integer to a `StopSequence` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_stop_sequence_from_i32(int32_t value);

/**
 * Convert a `StopSequence` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_stop_sequence_from_str(const char *name);

/**
 * Convert an integer to a `FinishReason` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_finish_reason_from_i32(int32_t value);

/**
 * Convert a `FinishReason` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_finish_reason_from_str(const char *name);

/**
 * Convert an integer to a `ReasoningEffort` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_reasoning_effort_from_i32(int32_t value);

/**
 * Convert a `ReasoningEffort` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_reasoning_effort_from_str(const char *name);

/**
 * Convert an integer to a `EmbeddingFormat` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_embedding_format_from_i32(int32_t value);

/**
 * Convert a `EmbeddingFormat` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_embedding_format_from_str(const char *name);

/**
 * Convert an integer to a `EmbeddingInput` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_embedding_input_from_i32(int32_t value);

/**
 * Convert a `EmbeddingInput` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_embedding_input_from_str(const char *name);

/**
 * Convert an integer to a `ModerationInput` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_moderation_input_from_i32(int32_t value);

/**
 * Convert a `ModerationInput` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_moderation_input_from_str(const char *name);

/**
 * Convert an integer to a `RerankDocument` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_rerank_document_from_i32(int32_t value);

/**
 * Convert a `RerankDocument` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_rerank_document_from_str(const char *name);

/**
 * Convert an integer to a `OcrDocument` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_ocr_document_from_i32(int32_t value);

/**
 * Convert a `OcrDocument` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_ocr_document_from_str(const char *name);

/**
 * Convert an integer to a `AuthHeaderFormat` variant. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_auth_header_format_from_i32(int32_t value);

/**
 * Convert a `AuthHeaderFormat` variant name (C string) to its integer value. Returns -1 on invalid input.
 * # Safety
 * Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
 */
int32_t literllm_auth_header_format_from_str(const char *name);

/**
 * Create a new LLM client with simple scalar configuration.
 *
 * This is the primary binding entry-point. All parameters except `api_key`
 * are optional — omitting them uses the same defaults as
 * [`ClientConfigBuilder`].
 *
 * # Errors
 *
 * Returns [`LiterLlmError`] if the underlying HTTP client cannot be
 * constructed, or if the resolved provider configuration is invalid.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMDefaultClient *literllm_create_client(const char *api_key,
                                              const char *base_url,
                                              uint64_t timeout_secs,
                                              uint32_t max_retries,
                                              const char *model_hint);

/**
 * Create a new LLM client from a JSON string.
 *
 * The JSON object accepts the same fields as `liter-llm.toml` (snake_case).
 *
 * # Errors
 *
 * Returns [`LiterLlmError::BadRequest`] if `json` is not valid JSON or
 * contains unknown fields.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
LITERLLMDefaultClient *literllm_create_client_from_json(const char *json);

/**
 * Register a custom provider in the global runtime registry.
 *
 * The provider will be checked **before** all built-in providers during model
 * detection. If a provider with the same `name` already exists it is replaced.
 *
 * # Errors
 *
 * Returns an error if the config is invalid (empty name, empty base_url, or
 * no model prefixes).
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_register_custom_provider(const LITERLLMCustomProviderConfig *config);

/**
 * Remove a previously registered custom provider by name.
 *
 * Returns `true` if a provider with the given name was found and removed,
 * `false` if no such provider existed.
 *
 * # Errors
 *
 * Returns an error only if the internal lock is poisoned.
 * # Safety
 * Caller must ensure all pointer arguments are valid or null.
 * Returned pointers must be freed with the appropriate free function.
 */
int32_t literllm_unregister_custom_provider(const char *name);

#endif  /* LITERLLM_H */
