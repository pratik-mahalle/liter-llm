// This file is auto-generated by alef. DO NOT EDIT.
// Re-generate with: alef generate
#![allow(missing_docs)]
#![allow(deprecated, dead_code, unused_imports, unused_variables)]
#![allow(
    clippy::default_trait_access,
    clippy::cast_possible_wrap,
    clippy::cast_possible_truncation,
    clippy::cast_sign_loss,
    clippy::just_underscores_and_digits,
    clippy::unused_unit,
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::too_many_arguments
)]

use liter_llm::client::LlmClient;
use pyo3::exceptions::PyRuntimeError;
use pyo3::prelude::*;
use std::collections::HashMap;
use std::sync::Arc;

#[pyclass]
pub struct ChatStreamIterator {
    inner: Arc<
        tokio::sync::Mutex<
            futures::stream::BoxStream<'static, Result<liter_llm::ChatCompletionChunk, liter_llm::LiterLlmError>>,
        >,
    >,
}

#[pymethods]
impl ChatStreamIterator {
    fn __aiter__(slf: PyRef<'_, Self>) -> PyRef<'_, Self> {
        slf
    }

    fn __anext__<'py>(&self, py: Python<'py>) -> PyResult<Option<Bound<'py, PyAny>>> {
        let inner = self.inner.clone();
        pyo3_async_runtimes::tokio::future_into_py(py, async move {
            let mut stream = inner.lock().await;
            match futures::StreamExt::next(&mut *stream).await {
                Some(Ok(chunk)) => Ok(Some(ChatCompletionChunk::from(chunk))),
                Some(Err(e)) => Err(PyErr::new::<pyo3::exceptions::PyRuntimeError, _>(e.to_string())),
                None => Ok(None), // StopAsyncIteration
            }
        })
        .map(Some)
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SystemMessage {
    #[pyo3(get)]
    pub content: String,
    #[pyo3(get)]
    pub name: Option<String>,
}

#[pymethods]
impl SystemMessage {
    #[must_use]
    #[pyo3(signature = (content=None, name=None))]
    #[new]
    pub fn new(content: Option<String>, name: Option<String>) -> Self {
        Self {
            content: content.unwrap_or_default(),
            name,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct UserMessage {
    #[pyo3(get)]
    pub content: UserContent,
    #[pyo3(get)]
    pub name: Option<String>,
}

#[pymethods]
impl UserMessage {
    #[must_use]
    #[pyo3(signature = (content=None, name=None))]
    #[new]
    pub fn new(content: Option<UserContent>, name: Option<String>) -> Self {
        Self {
            content: content.unwrap_or_default(),
            name,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ImageUrl {
    #[pyo3(get)]
    pub url: String,
    #[pyo3(get)]
    pub detail: Option<ImageDetail>,
}

#[pymethods]
impl ImageUrl {
    #[must_use]
    #[pyo3(signature = (url=None, detail=None))]
    #[new]
    pub fn new(url: Option<String>, detail: Option<ImageDetail>) -> Self {
        Self {
            url: url.unwrap_or_default(),
            detail,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct DocumentContent {
    /// Base64-encoded document data or URL.
    #[pyo3(get)]
    pub data: String,
    /// MIME type (e.g., "application/pdf", "text/csv").
    #[pyo3(get)]
    pub media_type: String,
}

#[pymethods]
impl DocumentContent {
    #[must_use]
    #[pyo3(signature = (data=None, media_type=None))]
    #[new]
    pub fn new(data: Option<String>, media_type: Option<String>) -> Self {
        Self {
            data: data.unwrap_or_default(),
            media_type: media_type.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct AudioContent {
    /// Base64-encoded audio data.
    #[pyo3(get)]
    pub data: String,
    /// Audio format (e.g., "wav", "mp3", "ogg").
    #[pyo3(get)]
    pub format: String,
}

#[pymethods]
impl AudioContent {
    #[must_use]
    #[pyo3(signature = (data=None, format=None))]
    #[new]
    pub fn new(data: Option<String>, format: Option<String>) -> Self {
        Self {
            data: data.unwrap_or_default(),
            format: format.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct AssistantMessage {
    #[pyo3(get)]
    pub content: Option<String>,
    #[pyo3(get)]
    pub name: Option<String>,
    #[pyo3(get)]
    pub tool_calls: Option<Vec<ToolCall>>,
    #[pyo3(get)]
    pub refusal: Option<String>,
    /// Deprecated legacy function_call field; retained for API compatibility.
    #[pyo3(get)]
    pub function_call: Option<FunctionCall>,
}

#[pymethods]
impl AssistantMessage {
    #[must_use]
    #[pyo3(signature = (content=None, name=None, tool_calls=None, refusal=None, function_call=None))]
    #[new]
    pub fn new(
        content: Option<String>,
        name: Option<String>,
        tool_calls: Option<Vec<ToolCall>>,
        refusal: Option<String>,
        function_call: Option<FunctionCall>,
    ) -> Self {
        Self {
            content,
            name,
            tool_calls,
            refusal,
            function_call,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ToolMessage {
    #[pyo3(get)]
    pub content: String,
    #[pyo3(get)]
    pub tool_call_id: String,
    #[pyo3(get)]
    pub name: Option<String>,
}

#[pymethods]
impl ToolMessage {
    #[must_use]
    #[pyo3(signature = (content=None, tool_call_id=None, name=None))]
    #[new]
    pub fn new(content: Option<String>, tool_call_id: Option<String>, name: Option<String>) -> Self {
        Self {
            content: content.unwrap_or_default(),
            tool_call_id: tool_call_id.unwrap_or_default(),
            name,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct DeveloperMessage {
    #[pyo3(get)]
    pub content: String,
    #[pyo3(get)]
    pub name: Option<String>,
}

#[pymethods]
impl DeveloperMessage {
    #[must_use]
    #[pyo3(signature = (content=None, name=None))]
    #[new]
    pub fn new(content: Option<String>, name: Option<String>) -> Self {
        Self {
            content: content.unwrap_or_default(),
            name,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct FunctionMessage {
    #[pyo3(get)]
    pub content: String,
    #[pyo3(get)]
    pub name: String,
}

#[pymethods]
impl FunctionMessage {
    #[must_use]
    #[pyo3(signature = (content=None, name=None))]
    #[new]
    pub fn new(content: Option<String>, name: Option<String>) -> Self {
        Self {
            content: content.unwrap_or_default(),
            name: name.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ChatCompletionTool {
    #[pyo3(get)]
    pub tool_type: ToolType,
    #[pyo3(get)]
    pub function: FunctionDefinition,
}

#[pymethods]
impl ChatCompletionTool {
    #[must_use]
    #[pyo3(signature = (tool_type, function))]
    #[new]
    pub fn new(tool_type: ToolType, function: FunctionDefinition) -> Self {
        Self { tool_type, function }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct FunctionDefinition {
    #[pyo3(get)]
    pub name: String,
    #[pyo3(get)]
    pub description: Option<String>,
    #[pyo3(get)]
    pub parameters: Option<String>,
    #[pyo3(get)]
    pub strict: Option<bool>,
}

#[pymethods]
impl FunctionDefinition {
    #[must_use]
    #[pyo3(signature = (name, description=None, parameters=None, strict=None))]
    #[new]
    pub fn new(name: String, description: Option<String>, parameters: Option<String>, strict: Option<bool>) -> Self {
        Self {
            name,
            description,
            parameters,
            strict,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ToolCall {
    #[pyo3(get)]
    pub id: String,
    #[pyo3(get)]
    pub call_type: ToolType,
    #[pyo3(get)]
    pub function: FunctionCall,
}

#[pymethods]
impl ToolCall {
    #[must_use]
    #[pyo3(signature = (id, call_type, function))]
    #[new]
    pub fn new(id: String, call_type: ToolType, function: FunctionCall) -> Self {
        Self {
            id,
            call_type,
            function,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct FunctionCall {
    #[pyo3(get)]
    pub name: String,
    #[pyo3(get)]
    pub arguments: String,
}

#[pymethods]
impl FunctionCall {
    #[must_use]
    #[pyo3(signature = (name, arguments))]
    #[new]
    pub fn new(name: String, arguments: String) -> Self {
        Self { name, arguments }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SpecificToolChoice {
    #[pyo3(get)]
    pub choice_type: ToolType,
    #[pyo3(get)]
    pub function: SpecificFunction,
}

#[pymethods]
impl SpecificToolChoice {
    #[must_use]
    #[pyo3(signature = (choice_type=None, function=None))]
    #[new]
    pub fn new(choice_type: Option<ToolType>, function: Option<SpecificFunction>) -> Self {
        Self {
            choice_type: choice_type.unwrap_or_default(),
            function: function.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SpecificFunction {
    #[pyo3(get)]
    pub name: String,
}

#[pymethods]
impl SpecificFunction {
    #[must_use]
    #[pyo3(signature = (name=None))]
    #[new]
    pub fn new(name: Option<String>) -> Self {
        Self {
            name: name.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct JsonSchemaFormat {
    #[pyo3(get)]
    pub name: String,
    #[pyo3(get)]
    pub description: Option<String>,
    #[pyo3(get)]
    pub schema: String,
    #[pyo3(get)]
    pub strict: Option<bool>,
}

#[pymethods]
impl JsonSchemaFormat {
    #[must_use]
    #[pyo3(signature = (name=None, schema=None, description=None, strict=None))]
    #[new]
    pub fn new(
        name: Option<String>,
        schema: Option<String>,
        description: Option<String>,
        strict: Option<bool>,
    ) -> Self {
        Self {
            name: name.unwrap_or_default(),
            description,
            schema: schema.unwrap_or_default(),
            strict,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct Usage {
    /// Prompt tokens used. Defaults to 0 when absent (some providers omit this).
    #[pyo3(get)]
    pub prompt_tokens: u64,
    /// Completion tokens used. Defaults to 0 when absent (e.g. embedding responses).
    #[pyo3(get)]
    pub completion_tokens: u64,
    /// Total tokens used. Defaults to 0 when absent (some providers omit this).
    #[pyo3(get)]
    pub total_tokens: u64,
}

#[pymethods]
impl Usage {
    #[must_use]
    #[pyo3(signature = (prompt_tokens=None, completion_tokens=None, total_tokens=None))]
    #[new]
    pub fn new(prompt_tokens: Option<u64>, completion_tokens: Option<u64>, total_tokens: Option<u64>) -> Self {
        Self {
            prompt_tokens: prompt_tokens.unwrap_or_default(),
            completion_tokens: completion_tokens.unwrap_or_default(),
            total_tokens: total_tokens.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ChatCompletionRequest {
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub messages: Vec<Message>,
    #[pyo3(get)]
    pub temperature: Option<f64>,
    #[pyo3(get)]
    pub top_p: Option<f64>,
    #[pyo3(get)]
    pub n: Option<u32>,
    /// Whether to stream the response.
    ///
    /// Managed by the client layer — do not set directly.
    #[pyo3(get)]
    pub stream: Option<bool>,
    #[pyo3(get)]
    pub stop: Option<StopSequence>,
    #[pyo3(get)]
    pub max_tokens: Option<u64>,
    #[pyo3(get)]
    pub presence_penalty: Option<f64>,
    #[pyo3(get)]
    pub frequency_penalty: Option<f64>,
    /// Token bias map.  Uses `BTreeMap` (sorted keys) for deterministic
    /// serialization order — important when hashing or signing requests.
    #[pyo3(get)]
    pub logit_bias: Option<HashMap<String, f64>>,
    #[pyo3(get)]
    pub user: Option<String>,
    #[pyo3(get)]
    pub tools: Option<Vec<ChatCompletionTool>>,
    #[pyo3(get)]
    pub tool_choice: Option<ToolChoice>,
    #[pyo3(get)]
    pub parallel_tool_calls: Option<bool>,
    #[pyo3(get)]
    pub response_format: Option<ResponseFormat>,
    #[pyo3(get)]
    pub stream_options: Option<StreamOptions>,
    #[pyo3(get)]
    pub seed: Option<i64>,
    #[pyo3(get)]
    pub reasoning_effort: Option<ReasoningEffort>,
    /// Provider-specific extra parameters merged into the request body.
    /// Use for guardrails, safety settings, grounding config, etc.
    #[pyo3(get)]
    pub extra_body: Option<String>,
}

#[pymethods]
impl ChatCompletionRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (model=None, messages=None, temperature=None, top_p=None, n=None, stream=None, stop=None, max_tokens=None, presence_penalty=None, frequency_penalty=None, logit_bias=None, user=None, tools=None, tool_choice=None, parallel_tool_calls=None, response_format=None, stream_options=None, seed=None, reasoning_effort=None, extra_body=None))]
    #[new]
    pub fn new(
        model: Option<String>,
        messages: Option<Vec<Message>>,
        temperature: Option<f64>,
        top_p: Option<f64>,
        n: Option<u32>,
        stream: Option<bool>,
        stop: Option<StopSequence>,
        max_tokens: Option<u64>,
        presence_penalty: Option<f64>,
        frequency_penalty: Option<f64>,
        logit_bias: Option<HashMap<String, f64>>,
        user: Option<String>,
        tools: Option<Vec<ChatCompletionTool>>,
        tool_choice: Option<ToolChoice>,
        parallel_tool_calls: Option<bool>,
        response_format: Option<ResponseFormat>,
        stream_options: Option<StreamOptions>,
        seed: Option<i64>,
        reasoning_effort: Option<ReasoningEffort>,
        extra_body: Option<String>,
    ) -> Self {
        Self {
            model: model.unwrap_or_default(),
            messages: messages.unwrap_or_default(),
            temperature,
            top_p,
            n,
            stream,
            stop,
            max_tokens,
            presence_penalty,
            frequency_penalty,
            logit_bias,
            user,
            tools,
            tool_choice,
            parallel_tool_calls,
            response_format,
            stream_options,
            seed,
            reasoning_effort,
            extra_body,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct StreamOptions {
    #[pyo3(get)]
    pub include_usage: Option<bool>,
}

#[pymethods]
impl StreamOptions {
    #[must_use]
    #[pyo3(signature = (include_usage=None))]
    #[new]
    pub fn new(include_usage: Option<bool>) -> Self {
        Self { include_usage }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ChatCompletionResponse {
    #[pyo3(get)]
    pub id: String,
    /// Always `"chat.completion"` from OpenAI-compatible APIs.  Stored as a
    /// plain `String` so non-standard provider values do not break deserialization.
    #[pyo3(get)]
    pub object: String,
    #[pyo3(get)]
    pub created: u64,
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub choices: Vec<Choice>,
    #[pyo3(get)]
    pub usage: Option<Usage>,
    #[pyo3(get)]
    pub system_fingerprint: Option<String>,
    #[pyo3(get)]
    pub service_tier: Option<String>,
}

#[pymethods]
impl ChatCompletionResponse {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (id=None, object=None, created=None, model=None, choices=None, usage=None, system_fingerprint=None, service_tier=None))]
    #[new]
    pub fn new(
        id: Option<String>,
        object: Option<String>,
        created: Option<u64>,
        model: Option<String>,
        choices: Option<Vec<Choice>>,
        usage: Option<Usage>,
        system_fingerprint: Option<String>,
        service_tier: Option<String>,
    ) -> Self {
        Self {
            id: id.unwrap_or_default(),
            object: object.unwrap_or_default(),
            created: created.unwrap_or_default(),
            model: model.unwrap_or_default(),
            choices: choices.unwrap_or_default(),
            usage,
            system_fingerprint,
            service_tier,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct Choice {
    #[pyo3(get)]
    pub index: u32,
    #[pyo3(get)]
    pub message: AssistantMessage,
    #[pyo3(get)]
    pub finish_reason: Option<FinishReason>,
}

#[pymethods]
impl Choice {
    #[must_use]
    #[pyo3(signature = (index=None, message=None, finish_reason=None))]
    #[new]
    pub fn new(index: Option<u32>, message: Option<AssistantMessage>, finish_reason: Option<FinishReason>) -> Self {
        Self {
            index: index.unwrap_or_default(),
            message: message.unwrap_or_default(),
            finish_reason,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ChatCompletionChunk {
    #[pyo3(get)]
    pub id: String,
    /// Always `"chat.completion.chunk"` from OpenAI-compatible APIs.  Stored
    /// as a plain `String` so non-standard provider values do not fail parsing.
    #[pyo3(get)]
    pub object: String,
    #[pyo3(get)]
    pub created: u64,
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub choices: Vec<StreamChoice>,
    #[pyo3(get)]
    pub usage: Option<Usage>,
    #[pyo3(get)]
    pub system_fingerprint: Option<String>,
    #[pyo3(get)]
    pub service_tier: Option<String>,
}

#[pymethods]
impl ChatCompletionChunk {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (id=None, object=None, created=None, model=None, choices=None, usage=None, system_fingerprint=None, service_tier=None))]
    #[new]
    pub fn new(
        id: Option<String>,
        object: Option<String>,
        created: Option<u64>,
        model: Option<String>,
        choices: Option<Vec<StreamChoice>>,
        usage: Option<Usage>,
        system_fingerprint: Option<String>,
        service_tier: Option<String>,
    ) -> Self {
        Self {
            id: id.unwrap_or_default(),
            object: object.unwrap_or_default(),
            created: created.unwrap_or_default(),
            model: model.unwrap_or_default(),
            choices: choices.unwrap_or_default(),
            usage,
            system_fingerprint,
            service_tier,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct StreamChoice {
    #[pyo3(get)]
    pub index: u32,
    #[pyo3(get)]
    pub delta: StreamDelta,
    #[pyo3(get)]
    pub finish_reason: Option<FinishReason>,
}

#[pymethods]
impl StreamChoice {
    #[must_use]
    #[pyo3(signature = (index=None, delta=None, finish_reason=None))]
    #[new]
    pub fn new(index: Option<u32>, delta: Option<StreamDelta>, finish_reason: Option<FinishReason>) -> Self {
        Self {
            index: index.unwrap_or_default(),
            delta: delta.unwrap_or_default(),
            finish_reason,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct StreamDelta {
    #[pyo3(get)]
    pub role: Option<String>,
    #[pyo3(get)]
    pub content: Option<String>,
    #[pyo3(get)]
    pub tool_calls: Option<Vec<StreamToolCall>>,
    /// Deprecated legacy function_call delta; retained for API compatibility.
    #[pyo3(get)]
    pub function_call: Option<StreamFunctionCall>,
    #[pyo3(get)]
    pub refusal: Option<String>,
}

#[pymethods]
impl StreamDelta {
    #[must_use]
    #[pyo3(signature = (role=None, content=None, tool_calls=None, function_call=None, refusal=None))]
    #[new]
    pub fn new(
        role: Option<String>,
        content: Option<String>,
        tool_calls: Option<Vec<StreamToolCall>>,
        function_call: Option<StreamFunctionCall>,
        refusal: Option<String>,
    ) -> Self {
        Self {
            role,
            content,
            tool_calls,
            function_call,
            refusal,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct StreamToolCall {
    #[pyo3(get)]
    pub index: u32,
    #[pyo3(get)]
    pub id: Option<String>,
    #[pyo3(get)]
    pub call_type: Option<ToolType>,
    #[pyo3(get)]
    pub function: Option<StreamFunctionCall>,
}

#[pymethods]
impl StreamToolCall {
    #[must_use]
    #[pyo3(signature = (index=None, id=None, call_type=None, function=None))]
    #[new]
    pub fn new(
        index: Option<u32>,
        id: Option<String>,
        call_type: Option<ToolType>,
        function: Option<StreamFunctionCall>,
    ) -> Self {
        Self {
            index: index.unwrap_or_default(),
            id,
            call_type,
            function,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct StreamFunctionCall {
    #[pyo3(get)]
    pub name: Option<String>,
    #[pyo3(get)]
    pub arguments: Option<String>,
}

#[pymethods]
impl StreamFunctionCall {
    #[must_use]
    #[pyo3(signature = (name=None, arguments=None))]
    #[new]
    pub fn new(name: Option<String>, arguments: Option<String>) -> Self {
        Self { name, arguments }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct EmbeddingRequest {
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub input: EmbeddingInput,
    #[pyo3(get)]
    pub encoding_format: Option<EmbeddingFormat>,
    #[pyo3(get)]
    pub dimensions: Option<u32>,
    #[pyo3(get)]
    pub user: Option<String>,
}

#[pymethods]
impl EmbeddingRequest {
    #[must_use]
    #[pyo3(signature = (model, input, encoding_format=None, dimensions=None, user=None))]
    #[new]
    pub fn new(
        model: String,
        input: EmbeddingInput,
        encoding_format: Option<EmbeddingFormat>,
        dimensions: Option<u32>,
        user: Option<String>,
    ) -> Self {
        Self {
            model,
            input,
            encoding_format,
            dimensions,
            user,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct EmbeddingResponse {
    /// Always `"list"` from OpenAI-compatible APIs.  Stored as a plain
    /// `String` so non-standard provider values do not break deserialization.
    #[pyo3(get)]
    pub object: String,
    #[pyo3(get)]
    pub data: Vec<EmbeddingObject>,
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub usage: Option<Usage>,
}

#[pymethods]
impl EmbeddingResponse {
    #[must_use]
    #[pyo3(signature = (object, data, model, usage=None))]
    #[new]
    pub fn new(object: String, data: Vec<EmbeddingObject>, model: String, usage: Option<Usage>) -> Self {
        Self {
            object,
            data,
            model,
            usage,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct EmbeddingObject {
    /// Always `"embedding"` from OpenAI-compatible APIs.  Stored as a plain
    /// `String` so non-standard provider values do not break deserialization.
    #[pyo3(get)]
    pub object: String,
    #[pyo3(get)]
    pub embedding: Vec<f64>,
    #[pyo3(get)]
    pub index: u32,
}

#[pymethods]
impl EmbeddingObject {
    #[must_use]
    #[pyo3(signature = (object, embedding, index))]
    #[new]
    pub fn new(object: String, embedding: Vec<f64>, index: u32) -> Self {
        Self {
            object,
            embedding,
            index,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CreateImageRequest {
    #[pyo3(get)]
    pub prompt: String,
    #[pyo3(get)]
    pub model: Option<String>,
    #[pyo3(get)]
    pub n: Option<u32>,
    #[pyo3(get)]
    pub size: Option<String>,
    #[pyo3(get)]
    pub quality: Option<String>,
    #[pyo3(get)]
    pub style: Option<String>,
    #[pyo3(get)]
    pub response_format: Option<String>,
    #[pyo3(get)]
    pub user: Option<String>,
}

#[pymethods]
impl CreateImageRequest {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (prompt=None, model=None, n=None, size=None, quality=None, style=None, response_format=None, user=None))]
    #[new]
    pub fn new(
        prompt: Option<String>,
        model: Option<String>,
        n: Option<u32>,
        size: Option<String>,
        quality: Option<String>,
        style: Option<String>,
        response_format: Option<String>,
        user: Option<String>,
    ) -> Self {
        Self {
            prompt: prompt.unwrap_or_default(),
            model,
            n,
            size,
            quality,
            style,
            response_format,
            user,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ImagesResponse {
    #[pyo3(get)]
    pub created: u64,
    #[pyo3(get)]
    pub data: Vec<Image>,
}

#[pymethods]
impl ImagesResponse {
    #[must_use]
    #[pyo3(signature = (created=None, data=None))]
    #[new]
    pub fn new(created: Option<u64>, data: Option<Vec<Image>>) -> Self {
        Self {
            created: created.unwrap_or_default(),
            data: data.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct Image {
    #[pyo3(get)]
    pub url: Option<String>,
    #[pyo3(get)]
    pub b64_json: Option<String>,
    #[pyo3(get)]
    pub revised_prompt: Option<String>,
}

#[pymethods]
impl Image {
    #[must_use]
    #[pyo3(signature = (url=None, b64_json=None, revised_prompt=None))]
    #[new]
    pub fn new(url: Option<String>, b64_json: Option<String>, revised_prompt: Option<String>) -> Self {
        Self {
            url,
            b64_json,
            revised_prompt,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CreateSpeechRequest {
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub input: String,
    #[pyo3(get)]
    pub voice: String,
    #[pyo3(get)]
    pub response_format: Option<String>,
    #[pyo3(get)]
    pub speed: Option<f64>,
}

#[pymethods]
impl CreateSpeechRequest {
    #[must_use]
    #[pyo3(signature = (model=None, input=None, voice=None, response_format=None, speed=None))]
    #[new]
    pub fn new(
        model: Option<String>,
        input: Option<String>,
        voice: Option<String>,
        response_format: Option<String>,
        speed: Option<f64>,
    ) -> Self {
        Self {
            model: model.unwrap_or_default(),
            input: input.unwrap_or_default(),
            voice: voice.unwrap_or_default(),
            response_format,
            speed,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CreateTranscriptionRequest {
    #[pyo3(get)]
    pub model: String,
    /// Base64-encoded audio file data.
    #[pyo3(get)]
    pub file: String,
    #[pyo3(get)]
    pub language: Option<String>,
    #[pyo3(get)]
    pub prompt: Option<String>,
    #[pyo3(get)]
    pub response_format: Option<String>,
    #[pyo3(get)]
    pub temperature: Option<f64>,
}

#[pymethods]
impl CreateTranscriptionRequest {
    #[must_use]
    #[pyo3(signature = (model=None, file=None, language=None, prompt=None, response_format=None, temperature=None))]
    #[new]
    pub fn new(
        model: Option<String>,
        file: Option<String>,
        language: Option<String>,
        prompt: Option<String>,
        response_format: Option<String>,
        temperature: Option<f64>,
    ) -> Self {
        Self {
            model: model.unwrap_or_default(),
            file: file.unwrap_or_default(),
            language,
            prompt,
            response_format,
            temperature,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct TranscriptionResponse {
    #[pyo3(get)]
    pub text: String,
    #[pyo3(get)]
    pub language: Option<String>,
    #[pyo3(get)]
    pub duration: Option<f64>,
    #[pyo3(get)]
    pub segments: Option<Vec<TranscriptionSegment>>,
}

#[pymethods]
impl TranscriptionResponse {
    #[must_use]
    #[pyo3(signature = (text=None, language=None, duration=None, segments=None))]
    #[new]
    pub fn new(
        text: Option<String>,
        language: Option<String>,
        duration: Option<f64>,
        segments: Option<Vec<TranscriptionSegment>>,
    ) -> Self {
        Self {
            text: text.unwrap_or_default(),
            language,
            duration,
            segments,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct TranscriptionSegment {
    #[pyo3(get)]
    pub id: u32,
    #[pyo3(get)]
    pub start: f64,
    #[pyo3(get)]
    pub end: f64,
    #[pyo3(get)]
    pub text: String,
}

#[pymethods]
impl TranscriptionSegment {
    #[must_use]
    #[pyo3(signature = (id=None, start=None, end=None, text=None))]
    #[new]
    pub fn new(id: Option<u32>, start: Option<f64>, end: Option<f64>, text: Option<String>) -> Self {
        Self {
            id: id.unwrap_or_default(),
            start: start.unwrap_or_default(),
            end: end.unwrap_or_default(),
            text: text.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ModerationRequest {
    #[pyo3(get)]
    pub input: ModerationInput,
    #[pyo3(get)]
    pub model: Option<String>,
}

#[pymethods]
impl ModerationRequest {
    #[must_use]
    #[pyo3(signature = (input, model=None))]
    #[new]
    pub fn new(input: ModerationInput, model: Option<String>) -> Self {
        Self { input, model }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ModerationResponse {
    #[pyo3(get)]
    pub id: String,
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub results: Vec<ModerationResult>,
}

#[pymethods]
impl ModerationResponse {
    #[must_use]
    #[pyo3(signature = (id, model, results))]
    #[new]
    pub fn new(id: String, model: String, results: Vec<ModerationResult>) -> Self {
        Self { id, model, results }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ModerationResult {
    #[pyo3(get)]
    pub flagged: bool,
    #[pyo3(get)]
    pub categories: ModerationCategories,
    #[pyo3(get)]
    pub category_scores: ModerationCategoryScores,
}

#[pymethods]
impl ModerationResult {
    #[must_use]
    #[pyo3(signature = (flagged, categories, category_scores))]
    #[new]
    pub fn new(flagged: bool, categories: ModerationCategories, category_scores: ModerationCategoryScores) -> Self {
        Self {
            flagged,
            categories,
            category_scores,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ModerationCategories {
    #[pyo3(get)]
    pub sexual: bool,
    #[pyo3(get)]
    pub hate: bool,
    #[pyo3(get)]
    pub harassment: bool,
    #[pyo3(get)]
    pub self_harm: bool,
    #[pyo3(get)]
    pub sexual_minors: bool,
    #[pyo3(get)]
    pub hate_threatening: bool,
    #[pyo3(get)]
    pub violence_graphic: bool,
    #[pyo3(get)]
    pub self_harm_intent: bool,
    #[pyo3(get)]
    pub self_harm_instructions: bool,
    #[pyo3(get)]
    pub harassment_threatening: bool,
    #[pyo3(get)]
    pub violence: bool,
}

#[pymethods]
impl ModerationCategories {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (sexual, hate, harassment, self_harm, sexual_minors, hate_threatening, violence_graphic, self_harm_intent, self_harm_instructions, harassment_threatening, violence))]
    #[new]
    pub fn new(
        sexual: bool,
        hate: bool,
        harassment: bool,
        self_harm: bool,
        sexual_minors: bool,
        hate_threatening: bool,
        violence_graphic: bool,
        self_harm_intent: bool,
        self_harm_instructions: bool,
        harassment_threatening: bool,
        violence: bool,
    ) -> Self {
        Self {
            sexual,
            hate,
            harassment,
            self_harm,
            sexual_minors,
            hate_threatening,
            violence_graphic,
            self_harm_intent,
            self_harm_instructions,
            harassment_threatening,
            violence,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ModerationCategoryScores {
    #[pyo3(get)]
    pub sexual: f64,
    #[pyo3(get)]
    pub hate: f64,
    #[pyo3(get)]
    pub harassment: f64,
    #[pyo3(get)]
    pub self_harm: f64,
    #[pyo3(get)]
    pub sexual_minors: f64,
    #[pyo3(get)]
    pub hate_threatening: f64,
    #[pyo3(get)]
    pub violence_graphic: f64,
    #[pyo3(get)]
    pub self_harm_intent: f64,
    #[pyo3(get)]
    pub self_harm_instructions: f64,
    #[pyo3(get)]
    pub harassment_threatening: f64,
    #[pyo3(get)]
    pub violence: f64,
}

#[pymethods]
impl ModerationCategoryScores {
    #[allow(clippy::too_many_arguments)]
    #[must_use]
    #[pyo3(signature = (sexual, hate, harassment, self_harm, sexual_minors, hate_threatening, violence_graphic, self_harm_intent, self_harm_instructions, harassment_threatening, violence))]
    #[new]
    pub fn new(
        sexual: f64,
        hate: f64,
        harassment: f64,
        self_harm: f64,
        sexual_minors: f64,
        hate_threatening: f64,
        violence_graphic: f64,
        self_harm_intent: f64,
        self_harm_instructions: f64,
        harassment_threatening: f64,
        violence: f64,
    ) -> Self {
        Self {
            sexual,
            hate,
            harassment,
            self_harm,
            sexual_minors,
            hate_threatening,
            violence_graphic,
            self_harm_intent,
            self_harm_instructions,
            harassment_threatening,
            violence,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct RerankRequest {
    #[pyo3(get)]
    pub model: String,
    #[pyo3(get)]
    pub query: String,
    #[pyo3(get)]
    pub documents: Vec<RerankDocument>,
    #[pyo3(get)]
    pub top_n: Option<u32>,
    #[pyo3(get)]
    pub return_documents: Option<bool>,
}

#[pymethods]
impl RerankRequest {
    #[must_use]
    #[pyo3(signature = (model, query, documents, top_n=None, return_documents=None))]
    #[new]
    pub fn new(
        model: String,
        query: String,
        documents: Vec<RerankDocument>,
        top_n: Option<u32>,
        return_documents: Option<bool>,
    ) -> Self {
        Self {
            model,
            query,
            documents,
            top_n,
            return_documents,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct RerankResponse {
    #[pyo3(get)]
    pub id: Option<String>,
    #[pyo3(get)]
    pub results: Vec<RerankResult>,
    #[pyo3(get)]
    pub meta: Option<String>,
}

#[pymethods]
impl RerankResponse {
    #[must_use]
    #[pyo3(signature = (results, id=None, meta=None))]
    #[new]
    pub fn new(results: Vec<RerankResult>, id: Option<String>, meta: Option<String>) -> Self {
        Self { id, results, meta }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct RerankResult {
    #[pyo3(get)]
    pub index: u32,
    #[pyo3(get)]
    pub relevance_score: f64,
    #[pyo3(get)]
    pub document: Option<RerankResultDocument>,
}

#[pymethods]
impl RerankResult {
    #[must_use]
    #[pyo3(signature = (index, relevance_score, document=None))]
    #[new]
    pub fn new(index: u32, relevance_score: f64, document: Option<RerankResultDocument>) -> Self {
        Self {
            index,
            relevance_score,
            document,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct RerankResultDocument {
    #[pyo3(get)]
    pub text: String,
}

#[pymethods]
impl RerankResultDocument {
    #[must_use]
    #[pyo3(signature = (text))]
    #[new]
    pub fn new(text: String) -> Self {
        Self { text }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SearchRequest {
    /// The model/provider to use (e.g. `"brave/web-search"`, `"tavily/search"`).
    #[pyo3(get)]
    pub model: String,
    /// The search query.
    #[pyo3(get)]
    pub query: String,
    /// Maximum number of results to return.
    #[pyo3(get)]
    pub max_results: Option<u32>,
    /// Domain filter — restrict results to specific domains.
    #[pyo3(get)]
    pub search_domain_filter: Option<Vec<String>>,
    /// Country code for localized results (ISO 3166-1 alpha-2).
    #[pyo3(get)]
    pub country: Option<String>,
}

#[pymethods]
impl SearchRequest {
    #[must_use]
    #[pyo3(signature = (model=None, query=None, max_results=None, search_domain_filter=None, country=None))]
    #[new]
    pub fn new(
        model: Option<String>,
        query: Option<String>,
        max_results: Option<u32>,
        search_domain_filter: Option<Vec<String>>,
        country: Option<String>,
    ) -> Self {
        Self {
            model: model.unwrap_or_default(),
            query: query.unwrap_or_default(),
            max_results,
            search_domain_filter,
            country,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SearchResponse {
    /// The search results.
    #[pyo3(get)]
    pub results: Vec<SearchResult>,
    /// The model used.
    #[pyo3(get)]
    pub model: String,
}

#[pymethods]
impl SearchResponse {
    #[must_use]
    #[pyo3(signature = (results, model))]
    #[new]
    pub fn new(results: Vec<SearchResult>, model: String) -> Self {
        Self { results, model }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct SearchResult {
    /// Title of the result.
    #[pyo3(get)]
    pub title: String,
    /// URL of the result.
    #[pyo3(get)]
    pub url: String,
    /// Text snippet / excerpt.
    #[pyo3(get)]
    pub snippet: String,
    /// Publication or last-updated date, if available.
    #[pyo3(get)]
    pub date: Option<String>,
}

#[pymethods]
impl SearchResult {
    #[must_use]
    #[pyo3(signature = (title, url, snippet, date=None))]
    #[new]
    pub fn new(title: String, url: String, snippet: String, date: Option<String>) -> Self {
        Self {
            title,
            url,
            snippet,
            date,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct OcrRequest {
    /// The model/provider to use (e.g. `"mistral/mistral-ocr-latest"`).
    #[pyo3(get)]
    pub model: String,
    /// The document to process.
    #[pyo3(get)]
    pub document: OcrDocument,
    /// Specific pages to process (1-indexed). `None` means all pages.
    #[pyo3(get)]
    pub pages: Option<Vec<u32>>,
    /// Whether to include base64-encoded images of each page.
    #[pyo3(get)]
    pub include_image_base64: Option<bool>,
}

#[pymethods]
impl OcrRequest {
    #[must_use]
    #[pyo3(signature = (model, document, pages=None, include_image_base64=None))]
    #[new]
    pub fn new(
        model: String,
        document: OcrDocument,
        pages: Option<Vec<u32>>,
        include_image_base64: Option<bool>,
    ) -> Self {
        Self {
            model,
            document,
            pages,
            include_image_base64,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct OcrResponse {
    /// Extracted pages.
    #[pyo3(get)]
    pub pages: Vec<OcrPage>,
    /// The model used.
    #[pyo3(get)]
    pub model: String,
    /// Token usage, if reported by the provider.
    #[pyo3(get)]
    pub usage: Option<Usage>,
}

#[pymethods]
impl OcrResponse {
    #[must_use]
    #[pyo3(signature = (pages, model, usage=None))]
    #[new]
    pub fn new(pages: Vec<OcrPage>, model: String, usage: Option<Usage>) -> Self {
        Self { pages, model, usage }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct OcrPage {
    /// Page index (0-based).
    #[pyo3(get)]
    pub index: u32,
    /// Extracted content as Markdown.
    #[pyo3(get)]
    pub markdown: String,
    /// Extracted images, if `include_image_base64` was set.
    #[pyo3(get)]
    pub images: Option<Vec<OcrImage>>,
    /// Page dimensions in pixels, if available.
    #[pyo3(get)]
    pub dimensions: Option<PageDimensions>,
}

#[pymethods]
impl OcrPage {
    #[must_use]
    #[pyo3(signature = (index, markdown, images=None, dimensions=None))]
    #[new]
    pub fn new(
        index: u32,
        markdown: String,
        images: Option<Vec<OcrImage>>,
        dimensions: Option<PageDimensions>,
    ) -> Self {
        Self {
            index,
            markdown,
            images,
            dimensions,
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct OcrImage {
    /// Unique image identifier.
    #[pyo3(get)]
    pub id: String,
    /// Base64-encoded image data.
    #[pyo3(get)]
    pub image_base64: Option<String>,
}

#[pymethods]
impl OcrImage {
    #[must_use]
    #[pyo3(signature = (id, image_base64=None))]
    #[new]
    pub fn new(id: String, image_base64: Option<String>) -> Self {
        Self { id, image_base64 }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct PageDimensions {
    /// Width in pixels.
    #[pyo3(get)]
    pub width: u32,
    /// Height in pixels.
    #[pyo3(get)]
    pub height: u32,
}

#[pymethods]
impl PageDimensions {
    #[must_use]
    #[pyo3(signature = (width, height))]
    #[new]
    pub fn new(width: u32, height: u32) -> Self {
        Self { width, height }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ModelsListResponse {
    /// Always `"list"` from OpenAI-compatible APIs.  Stored as a plain
    /// `String` so non-standard provider values do not break deserialization.
    #[pyo3(get)]
    pub object: String,
    #[pyo3(get)]
    pub data: Vec<ModelObject>,
}

#[pymethods]
impl ModelsListResponse {
    #[must_use]
    #[pyo3(signature = (object=None, data=None))]
    #[new]
    pub fn new(object: Option<String>, data: Option<Vec<ModelObject>>) -> Self {
        Self {
            object: object.unwrap_or_default(),
            data: data.unwrap_or_default(),
        }
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct ModelObject {
    #[pyo3(get)]
    pub id: String,
    /// Always `"model"` from OpenAI-compatible APIs.  Stored as a plain
    /// `String` so non-standard provider values do not break deserialization.
    #[pyo3(get)]
    pub object: String,
    #[pyo3(get)]
    pub created: u64,
    #[pyo3(get)]
    pub owned_by: String,
}

#[pymethods]
impl ModelObject {
    #[must_use]
    #[pyo3(signature = (id=None, object=None, created=None, owned_by=None))]
    #[new]
    pub fn new(id: Option<String>, object: Option<String>, created: Option<u64>, owned_by: Option<String>) -> Self {
        Self {
            id: id.unwrap_or_default(),
            object: object.unwrap_or_default(),
            created: created.unwrap_or_default(),
            owned_by: owned_by.unwrap_or_default(),
        }
    }
}

#[derive(Clone)]
#[pyclass(frozen, from_py_object)]
pub struct DefaultClient {
    inner: Arc<liter_llm::client::DefaultClient>,
}

#[pymethods]
impl DefaultClient {
    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn chat<'py>(&self, py: Python<'py>, req: ChatCompletionRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.chat",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn chat_stream<'py>(&self, py: Python<'py>, req: ChatCompletionRequest) -> PyResult<Bound<'py, PyAny>> {
        let inner = self.inner.clone();
        let stream = pyo3_async_runtimes::tokio::get_runtime()
            .block_on(inner.chat_stream(req.into()))
            .map_err(|e| PyErr::new::<pyo3::exceptions::PyRuntimeError, _>(e.to_string()))?;
        let iter = ChatStreamIterator {
            inner: Arc::new(tokio::sync::Mutex::new(stream)),
        };
        Ok(Bound::new(py, iter)?.into_any())
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn embed<'py>(&self, py: Python<'py>, req: EmbeddingRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.embed",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = ())]
    pub fn list_models<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.list_models",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn image_generate<'py>(&self, py: Python<'py>, req: CreateImageRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.image_generate",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn transcribe<'py>(&self, py: Python<'py>, req: CreateTranscriptionRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.transcribe",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn moderate<'py>(&self, py: Python<'py>, req: ModerationRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.moderate",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn rerank<'py>(&self, py: Python<'py>, req: RerankRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.rerank",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn search<'py>(&self, py: Python<'py>, req: SearchRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.search",
        ))
    }

    #[allow(clippy::missing_errors_doc)]
    #[pyo3(signature = (req))]
    pub fn ocr<'py>(&self, py: Python<'py>, req: OcrRequest) -> PyResult<Bound<'py, PyAny>> {
        let _ = req;
        Err(pyo3::exceptions::PyNotImplementedError::new_err(
            "Not implemented: DefaultClient.ocr",
        ))
    }
}

#[derive(Clone, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(frozen, from_py_object)]
pub struct CustomProviderConfig {
    /// Unique name for this provider (e.g., "my-provider").
    #[pyo3(get)]
    pub name: String,
    /// Base URL for the provider's API (e.g., "https://api.my-provider.com/v1").
    #[pyo3(get)]
    pub base_url: String,
    /// Authentication header format.
    #[pyo3(get)]
    pub auth_header: AuthHeaderFormat,
    /// Model name prefixes that route to this provider (e.g., ["my-"]).
    #[pyo3(get)]
    pub model_prefixes: Vec<String>,
}

#[pymethods]
impl CustomProviderConfig {
    #[must_use]
    #[pyo3(signature = (name, base_url, auth_header, model_prefixes))]
    #[new]
    pub fn new(name: String, base_url: String, auth_header: AuthHeaderFormat, model_prefixes: Vec<String>) -> Self {
        Self {
            name,
            base_url,
            auth_header,
            model_prefixes,
        }
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct Message {
    pub(crate) inner: liter_llm::types::Message,
}

#[pymethods]
impl Message {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::Message = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid Message: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<Message> for liter_llm::types::Message {
    fn from(val: Message) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::Message> for Message {
    fn from(val: liter_llm::types::Message) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for Message {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for Message {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::Message::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct UserContent {
    pub(crate) inner: liter_llm::types::UserContent,
}

#[pymethods]
impl UserContent {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::UserContent = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid UserContent: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<UserContent> for liter_llm::types::UserContent {
    fn from(val: UserContent) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::UserContent> for UserContent {
    fn from(val: liter_llm::types::UserContent) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for UserContent {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for UserContent {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::UserContent::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct ContentPart {
    pub(crate) inner: liter_llm::types::ContentPart,
}

#[pymethods]
impl ContentPart {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::ContentPart = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid ContentPart: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<ContentPart> for liter_llm::types::ContentPart {
    fn from(val: ContentPart) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::ContentPart> for ContentPart {
    fn from(val: liter_llm::types::ContentPart) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for ContentPart {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for ContentPart {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::ContentPart::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ImageDetail {
    #[default]
    Low = 0,
    High = 1,
    Auto = 2,
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ToolType {
    #[default]
    Function = 0,
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct ToolChoice {
    pub(crate) inner: liter_llm::types::ToolChoice,
}

#[pymethods]
impl ToolChoice {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::ToolChoice = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid ToolChoice: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<ToolChoice> for liter_llm::types::ToolChoice {
    fn from(val: ToolChoice) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::ToolChoice> for ToolChoice {
    fn from(val: liter_llm::types::ToolChoice) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for ToolChoice {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for ToolChoice {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::ToolChoice::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ToolChoiceMode {
    #[default]
    Auto = 0,
    Required = 1,
    #[pyo3(name = "None_")]
    None = 2,
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct ResponseFormat {
    pub(crate) inner: liter_llm::types::ResponseFormat,
}

#[pymethods]
impl ResponseFormat {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::ResponseFormat = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid ResponseFormat: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<ResponseFormat> for liter_llm::types::ResponseFormat {
    fn from(val: ResponseFormat) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::ResponseFormat> for ResponseFormat {
    fn from(val: liter_llm::types::ResponseFormat) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for ResponseFormat {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for ResponseFormat {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::ResponseFormat::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct StopSequence {
    pub(crate) inner: liter_llm::types::StopSequence,
}

#[pymethods]
impl StopSequence {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::StopSequence = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid StopSequence: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<StopSequence> for liter_llm::types::StopSequence {
    fn from(val: StopSequence) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::StopSequence> for StopSequence {
    fn from(val: liter_llm::types::StopSequence) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for StopSequence {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for StopSequence {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::StopSequence::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(eq, eq_int, from_py_object)]
pub enum FinishReason {
    #[default]
    Stop = 0,
    Length = 1,
    ToolCalls = 2,
    ContentFilter = 3,
    FunctionCall = 4,
    Other = 5,
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(eq, eq_int, from_py_object)]
pub enum ReasoningEffort {
    #[default]
    Low = 0,
    Medium = 1,
    High = 2,
}

#[derive(Clone, PartialEq, Default, serde::Serialize, serde::Deserialize)]
#[pyclass(eq, eq_int, from_py_object)]
pub enum EmbeddingFormat {
    #[default]
    Float = 0,
    Base64 = 1,
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct EmbeddingInput {
    pub(crate) inner: liter_llm::types::EmbeddingInput,
}

#[pymethods]
impl EmbeddingInput {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::EmbeddingInput = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid EmbeddingInput: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<EmbeddingInput> for liter_llm::types::EmbeddingInput {
    fn from(val: EmbeddingInput) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::EmbeddingInput> for EmbeddingInput {
    fn from(val: liter_llm::types::EmbeddingInput) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for EmbeddingInput {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for EmbeddingInput {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::EmbeddingInput::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct ModerationInput {
    pub(crate) inner: liter_llm::types::ModerationInput,
}

#[pymethods]
impl ModerationInput {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::ModerationInput = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid ModerationInput: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<ModerationInput> for liter_llm::types::ModerationInput {
    fn from(val: ModerationInput) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::ModerationInput> for ModerationInput {
    fn from(val: liter_llm::types::ModerationInput) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for ModerationInput {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for ModerationInput {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::ModerationInput::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct RerankDocument {
    pub(crate) inner: liter_llm::types::RerankDocument,
}

#[pymethods]
impl RerankDocument {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::RerankDocument = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid RerankDocument: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<RerankDocument> for liter_llm::types::RerankDocument {
    fn from(val: RerankDocument) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::RerankDocument> for RerankDocument {
    fn from(val: liter_llm::types::RerankDocument) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for RerankDocument {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for RerankDocument {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::RerankDocument::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct OcrDocument {
    pub(crate) inner: liter_llm::types::OcrDocument,
}

#[pymethods]
impl OcrDocument {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::types::OcrDocument = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid OcrDocument: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<OcrDocument> for liter_llm::types::OcrDocument {
    fn from(val: OcrDocument) -> Self {
        val.inner
    }
}

impl From<liter_llm::types::OcrDocument> for OcrDocument {
    fn from(val: liter_llm::types::OcrDocument) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for OcrDocument {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for OcrDocument {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::types::OcrDocument::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[derive(Clone)]
#[pyclass(frozen)]
#[derive(Default)]
pub struct AuthHeaderFormat {
    pub(crate) inner: liter_llm::provider::custom::AuthHeaderFormat,
}

#[pymethods]
impl AuthHeaderFormat {
    #[new]
    fn new(py: Python<'_>, value: &Bound<'_, pyo3::types::PyDict>) -> PyResult<Self> {
        let json_mod = py.import("json")?;
        let json_str: String = json_mod.call_method1("dumps", (value,))?.extract()?;
        let inner: liter_llm::provider::custom::AuthHeaderFormat = serde_json::from_str(&json_str)
            .map_err(|e| pyo3::exceptions::PyValueError::new_err(format!("Invalid AuthHeaderFormat: {e}")))?;
        Ok(Self { inner })
    }
}

impl From<AuthHeaderFormat> for liter_llm::provider::custom::AuthHeaderFormat {
    fn from(val: AuthHeaderFormat) -> Self {
        val.inner
    }
}

impl From<liter_llm::provider::custom::AuthHeaderFormat> for AuthHeaderFormat {
    fn from(val: liter_llm::provider::custom::AuthHeaderFormat) -> Self {
        Self { inner: val }
    }
}

impl serde::Serialize for AuthHeaderFormat {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        self.inner.serialize(serializer)
    }
}


impl<'de> serde::Deserialize<'de> for AuthHeaderFormat {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let inner = liter_llm::provider::custom::AuthHeaderFormat::deserialize(deserializer)?;
        Ok(Self { inner })
    }
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (api_key, base_url=None, timeout_secs=None, max_retries=None, model_hint=None))]
pub fn create_client(
    api_key: String,
    base_url: Option<String>,
    timeout_secs: Option<u64>,
    max_retries: Option<u32>,
    model_hint: Option<String>,
) -> PyResult<DefaultClient> {
    liter_llm::bindings::create_client(api_key, base_url, timeout_secs, max_retries, model_hint)
        .map(|val| DefaultClient { inner: Arc::new(val) })
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (json))]
pub fn create_client_from_json(json: String) -> PyResult<DefaultClient> {
    liter_llm::bindings::create_client_from_json(&json)
        .map(|val| DefaultClient { inner: Arc::new(val) })
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (config))]
pub fn register_custom_provider(config: CustomProviderConfig) -> PyResult<()> {
    let config_core: liter_llm::CustomProviderConfig = config.into();
    liter_llm::provider::custom::register_custom_provider(config_core)
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

#[allow(clippy::missing_errors_doc)]
#[pyfunction]
#[pyo3(signature = (name))]
pub fn unregister_custom_provider(name: String) -> PyResult<bool> {
    liter_llm::provider::custom::unregister_custom_provider(&name)
        .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(e.to_string()))
}

// Error types
pyo3::create_exception!(_internal_bindings, AuthenticationError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, RateLimitedError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, BadRequestError, pyo3::exceptions::PyException);
pyo3::create_exception!(
    _internal_bindings,
    ContextWindowExceededError,
    pyo3::exceptions::PyException
);
pyo3::create_exception!(_internal_bindings, ContentPolicyError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, NotFoundError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, ServerError, pyo3::exceptions::PyException);
pyo3::create_exception!(
    _internal_bindings,
    ServiceUnavailableError,
    pyo3::exceptions::PyException
);
pyo3::create_exception!(_internal_bindings, LiterLlmTimeoutError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, StreamingError, pyo3::exceptions::PyException);
pyo3::create_exception!(
    _internal_bindings,
    EndpointNotSupportedError,
    pyo3::exceptions::PyException
);
pyo3::create_exception!(_internal_bindings, InvalidHeaderError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, SerializationError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, BudgetExceededError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, HookRejectedError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, InternalError, pyo3::exceptions::PyException);
pyo3::create_exception!(_internal_bindings, LiterLlmError, pyo3::exceptions::PyException);

/// Convert a `liter_llm::error::LiterLlmError` error to a Python exception.
fn liter_llm_error_to_py_err(e: liter_llm::error::LiterLlmError) -> pyo3::PyErr {
    let msg = e.to_string();
    #[allow(unreachable_patterns)]
    match &e {
        liter_llm::error::LiterLlmError::Authentication { .. } => AuthenticationError::new_err(msg),
        liter_llm::error::LiterLlmError::RateLimited { .. } => RateLimitedError::new_err(msg),
        liter_llm::error::LiterLlmError::BadRequest { .. } => BadRequestError::new_err(msg),
        liter_llm::error::LiterLlmError::ContextWindowExceeded { .. } => ContextWindowExceededError::new_err(msg),
        liter_llm::error::LiterLlmError::ContentPolicy { .. } => ContentPolicyError::new_err(msg),
        liter_llm::error::LiterLlmError::NotFound { .. } => NotFoundError::new_err(msg),
        liter_llm::error::LiterLlmError::ServerError { .. } => ServerError::new_err(msg),
        liter_llm::error::LiterLlmError::ServiceUnavailable { .. } => ServiceUnavailableError::new_err(msg),
        liter_llm::error::LiterLlmError::Timeout => LiterLlmTimeoutError::new_err(msg),
        liter_llm::error::LiterLlmError::Streaming { .. } => StreamingError::new_err(msg),
        liter_llm::error::LiterLlmError::EndpointNotSupported { .. } => EndpointNotSupportedError::new_err(msg),
        liter_llm::error::LiterLlmError::InvalidHeader { .. } => InvalidHeaderError::new_err(msg),
        liter_llm::error::LiterLlmError::Serialization(..) => SerializationError::new_err(msg),
        liter_llm::error::LiterLlmError::BudgetExceeded { .. } => BudgetExceededError::new_err(msg),
        liter_llm::error::LiterLlmError::HookRejected { .. } => HookRejectedError::new_err(msg),
        liter_llm::error::LiterLlmError::InternalError { .. } => InternalError::new_err(msg),
        _ => LiterLlmError::new_err(msg),
    }
}

impl From<SystemMessage> for liter_llm::types::SystemMessage {
    fn from(val: SystemMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
        }
    }
}

impl From<liter_llm::types::SystemMessage> for SystemMessage {
    fn from(val: liter_llm::types::SystemMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
        }
    }
}

impl From<UserMessage> for liter_llm::types::UserMessage {
    fn from(val: UserMessage) -> Self {
        Self {
            content: val.content.into(),
            name: val.name,
        }
    }
}

impl From<liter_llm::types::UserMessage> for UserMessage {
    fn from(val: liter_llm::types::UserMessage) -> Self {
        Self {
            content: val.content.into(),
            name: val.name,
        }
    }
}

impl From<ImageUrl> for liter_llm::types::ImageUrl {
    fn from(val: ImageUrl) -> Self {
        Self {
            url: val.url,
            detail: val.detail.map(Into::into),
        }
    }
}

impl From<liter_llm::types::ImageUrl> for ImageUrl {
    fn from(val: liter_llm::types::ImageUrl) -> Self {
        Self {
            url: val.url,
            detail: val.detail.map(Into::into),
        }
    }
}

impl From<DocumentContent> for liter_llm::types::DocumentContent {
    fn from(val: DocumentContent) -> Self {
        Self {
            data: val.data,
            media_type: val.media_type,
        }
    }
}

impl From<liter_llm::types::DocumentContent> for DocumentContent {
    fn from(val: liter_llm::types::DocumentContent) -> Self {
        Self {
            data: val.data,
            media_type: val.media_type,
        }
    }
}

impl From<AudioContent> for liter_llm::types::AudioContent {
    fn from(val: AudioContent) -> Self {
        Self {
            data: val.data,
            format: val.format,
        }
    }
}

impl From<liter_llm::types::AudioContent> for AudioContent {
    fn from(val: liter_llm::types::AudioContent) -> Self {
        Self {
            data: val.data,
            format: val.format,
        }
    }
}

impl From<AssistantMessage> for liter_llm::types::AssistantMessage {
    fn from(val: AssistantMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
            tool_calls: val.tool_calls.map(|v| v.into_iter().map(Into::into).collect()),
            refusal: val.refusal,
            function_call: val.function_call.map(Into::into),
        }
    }
}

impl From<liter_llm::types::AssistantMessage> for AssistantMessage {
    fn from(val: liter_llm::types::AssistantMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
            tool_calls: val.tool_calls.map(|v| v.into_iter().map(Into::into).collect()),
            refusal: val.refusal,
            function_call: val.function_call.map(Into::into),
        }
    }
}

impl From<ToolMessage> for liter_llm::types::ToolMessage {
    fn from(val: ToolMessage) -> Self {
        Self {
            content: val.content,
            tool_call_id: val.tool_call_id,
            name: val.name,
        }
    }
}

impl From<liter_llm::types::ToolMessage> for ToolMessage {
    fn from(val: liter_llm::types::ToolMessage) -> Self {
        Self {
            content: val.content,
            tool_call_id: val.tool_call_id,
            name: val.name,
        }
    }
}

impl From<DeveloperMessage> for liter_llm::types::DeveloperMessage {
    fn from(val: DeveloperMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
        }
    }
}

impl From<liter_llm::types::DeveloperMessage> for DeveloperMessage {
    fn from(val: liter_llm::types::DeveloperMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
        }
    }
}

impl From<FunctionMessage> for liter_llm::types::FunctionMessage {
    fn from(val: FunctionMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
        }
    }
}

impl From<liter_llm::types::FunctionMessage> for FunctionMessage {
    fn from(val: liter_llm::types::FunctionMessage) -> Self {
        Self {
            content: val.content,
            name: val.name,
        }
    }
}

impl From<ChatCompletionTool> for liter_llm::types::ChatCompletionTool {
    fn from(val: ChatCompletionTool) -> Self {
        Self {
            tool_type: val.tool_type.into(),
            function: val.function.into(),
        }
    }
}

impl From<liter_llm::types::ChatCompletionTool> for ChatCompletionTool {
    fn from(val: liter_llm::types::ChatCompletionTool) -> Self {
        Self {
            tool_type: val.tool_type.into(),
            function: val.function.into(),
        }
    }
}

impl From<FunctionDefinition> for liter_llm::types::FunctionDefinition {
    fn from(val: FunctionDefinition) -> Self {
        Self {
            name: val.name,
            description: val.description,
            parameters: val.parameters.as_ref().and_then(|s| serde_json::from_str(s).ok()),
            strict: val.strict,
        }
    }
}

impl From<liter_llm::types::FunctionDefinition> for FunctionDefinition {
    fn from(val: liter_llm::types::FunctionDefinition) -> Self {
        Self {
            name: val.name,
            description: val.description,
            parameters: val.parameters.as_ref().map(ToString::to_string),
            strict: val.strict,
        }
    }
}

impl From<ToolCall> for liter_llm::types::ToolCall {
    fn from(val: ToolCall) -> Self {
        Self {
            id: val.id,
            call_type: val.call_type.into(),
            function: val.function.into(),
        }
    }
}

impl From<liter_llm::types::ToolCall> for ToolCall {
    fn from(val: liter_llm::types::ToolCall) -> Self {
        Self {
            id: val.id,
            call_type: val.call_type.into(),
            function: val.function.into(),
        }
    }
}

impl From<FunctionCall> for liter_llm::types::FunctionCall {
    fn from(val: FunctionCall) -> Self {
        Self {
            name: val.name,
            arguments: val.arguments,
        }
    }
}

impl From<liter_llm::types::FunctionCall> for FunctionCall {
    fn from(val: liter_llm::types::FunctionCall) -> Self {
        Self {
            name: val.name,
            arguments: val.arguments,
        }
    }
}

impl From<SpecificToolChoice> for liter_llm::types::SpecificToolChoice {
    fn from(val: SpecificToolChoice) -> Self {
        Self {
            choice_type: val.choice_type.into(),
            function: val.function.into(),
        }
    }
}

impl From<liter_llm::types::SpecificToolChoice> for SpecificToolChoice {
    fn from(val: liter_llm::types::SpecificToolChoice) -> Self {
        Self {
            choice_type: val.choice_type.into(),
            function: val.function.into(),
        }
    }
}

impl From<SpecificFunction> for liter_llm::types::SpecificFunction {
    fn from(val: SpecificFunction) -> Self {
        Self { name: val.name }
    }
}

impl From<liter_llm::types::SpecificFunction> for SpecificFunction {
    fn from(val: liter_llm::types::SpecificFunction) -> Self {
        Self { name: val.name }
    }
}

impl From<JsonSchemaFormat> for liter_llm::types::JsonSchemaFormat {
    fn from(val: JsonSchemaFormat) -> Self {
        Self {
            name: val.name,
            description: val.description,
            schema: serde_json::from_str(&val.schema).unwrap_or_default(),
            strict: val.strict,
        }
    }
}

impl From<liter_llm::types::JsonSchemaFormat> for JsonSchemaFormat {
    fn from(val: liter_llm::types::JsonSchemaFormat) -> Self {
        Self {
            name: val.name,
            description: val.description,
            schema: val.schema.to_string(),
            strict: val.strict,
        }
    }
}

impl From<Usage> for liter_llm::types::Usage {
    fn from(val: Usage) -> Self {
        Self {
            prompt_tokens: val.prompt_tokens,
            completion_tokens: val.completion_tokens,
            total_tokens: val.total_tokens,
        }
    }
}

impl From<liter_llm::types::Usage> for Usage {
    fn from(val: liter_llm::types::Usage) -> Self {
        Self {
            prompt_tokens: val.prompt_tokens,
            completion_tokens: val.completion_tokens,
            total_tokens: val.total_tokens,
        }
    }
}

impl From<ChatCompletionRequest> for liter_llm::types::ChatCompletionRequest {
    fn from(val: ChatCompletionRequest) -> Self {
        Self {
            model: val.model,
            messages: val.messages.into_iter().map(Into::into).collect(),
            temperature: val.temperature,
            top_p: val.top_p,
            n: val.n,
            stream: val.stream,
            stop: val.stop.map(Into::into),
            max_tokens: val.max_tokens,
            presence_penalty: val.presence_penalty,
            frequency_penalty: val.frequency_penalty,
            logit_bias: val.logit_bias.map(|m| m.into_iter().collect()),
            user: val.user,
            tools: val.tools.map(|v| v.into_iter().map(Into::into).collect()),
            tool_choice: val.tool_choice.map(Into::into),
            parallel_tool_calls: val.parallel_tool_calls,
            response_format: val.response_format.map(Into::into),
            stream_options: val.stream_options.map(Into::into),
            seed: val.seed,
            reasoning_effort: val.reasoning_effort.map(Into::into),
            extra_body: val.extra_body.as_ref().and_then(|s| serde_json::from_str(s).ok()),
        }
    }
}

impl From<liter_llm::types::ChatCompletionRequest> for ChatCompletionRequest {
    fn from(val: liter_llm::types::ChatCompletionRequest) -> Self {
        Self {
            model: val.model,
            messages: val.messages.into_iter().map(Into::into).collect(),
            temperature: val.temperature,
            top_p: val.top_p,
            n: val.n,
            stream: val.stream,
            stop: val.stop.map(Into::into),
            max_tokens: val.max_tokens,
            presence_penalty: val.presence_penalty,
            frequency_penalty: val.frequency_penalty,
            logit_bias: val.logit_bias.map(|m| m.into_iter().collect()),
            user: val.user,
            tools: val.tools.map(|v| v.into_iter().map(Into::into).collect()),
            tool_choice: val.tool_choice.map(Into::into),
            parallel_tool_calls: val.parallel_tool_calls,
            response_format: val.response_format.map(Into::into),
            stream_options: val.stream_options.map(Into::into),
            seed: val.seed,
            reasoning_effort: val.reasoning_effort.map(Into::into),
            extra_body: val.extra_body.as_ref().map(ToString::to_string),
        }
    }
}

impl From<StreamOptions> for liter_llm::types::StreamOptions {
    fn from(val: StreamOptions) -> Self {
        Self {
            include_usage: val.include_usage,
        }
    }
}

impl From<liter_llm::types::StreamOptions> for StreamOptions {
    fn from(val: liter_llm::types::StreamOptions) -> Self {
        Self {
            include_usage: val.include_usage,
        }
    }
}

impl From<ChatCompletionResponse> for liter_llm::types::ChatCompletionResponse {
    fn from(val: ChatCompletionResponse) -> Self {
        Self {
            id: val.id,
            object: val.object,
            created: val.created,
            model: val.model,
            choices: val.choices.into_iter().map(Into::into).collect(),
            usage: val.usage.map(Into::into),
            system_fingerprint: val.system_fingerprint,
            service_tier: val.service_tier,
        }
    }
}

impl From<liter_llm::types::ChatCompletionResponse> for ChatCompletionResponse {
    fn from(val: liter_llm::types::ChatCompletionResponse) -> Self {
        Self {
            id: val.id,
            object: val.object,
            created: val.created,
            model: val.model,
            choices: val.choices.into_iter().map(Into::into).collect(),
            usage: val.usage.map(Into::into),
            system_fingerprint: val.system_fingerprint,
            service_tier: val.service_tier,
        }
    }
}

impl From<Choice> for liter_llm::types::Choice {
    fn from(val: Choice) -> Self {
        Self {
            index: val.index,
            message: val.message.into(),
            finish_reason: val.finish_reason.map(Into::into),
        }
    }
}

impl From<liter_llm::types::Choice> for Choice {
    fn from(val: liter_llm::types::Choice) -> Self {
        Self {
            index: val.index,
            message: val.message.into(),
            finish_reason: val.finish_reason.map(Into::into),
        }
    }
}

impl From<liter_llm::types::ChatCompletionChunk> for ChatCompletionChunk {
    fn from(val: liter_llm::types::ChatCompletionChunk) -> Self {
        Self {
            id: val.id,
            object: val.object,
            created: val.created,
            model: val.model,
            choices: val.choices.into_iter().map(Into::into).collect(),
            usage: val.usage.map(Into::into),
            system_fingerprint: val.system_fingerprint,
            service_tier: val.service_tier,
        }
    }
}

impl From<liter_llm::types::StreamChoice> for StreamChoice {
    fn from(val: liter_llm::types::StreamChoice) -> Self {
        Self {
            index: val.index,
            delta: val.delta.into(),
            finish_reason: val.finish_reason.map(Into::into),
        }
    }
}

impl From<liter_llm::types::StreamDelta> for StreamDelta {
    fn from(val: liter_llm::types::StreamDelta) -> Self {
        Self {
            role: val.role,
            content: val.content,
            tool_calls: val.tool_calls.map(|v| v.into_iter().map(Into::into).collect()),
            function_call: val.function_call.map(Into::into),
            refusal: val.refusal,
        }
    }
}

impl From<liter_llm::types::StreamToolCall> for StreamToolCall {
    fn from(val: liter_llm::types::StreamToolCall) -> Self {
        Self {
            index: val.index,
            id: val.id,
            call_type: val.call_type.map(Into::into),
            function: val.function.map(Into::into),
        }
    }
}

impl From<liter_llm::types::StreamFunctionCall> for StreamFunctionCall {
    fn from(val: liter_llm::types::StreamFunctionCall) -> Self {
        Self {
            name: val.name,
            arguments: val.arguments,
        }
    }
}

impl From<EmbeddingRequest> for liter_llm::types::EmbeddingRequest {
    fn from(val: EmbeddingRequest) -> Self {
        Self {
            model: val.model,
            input: val.input.into(),
            encoding_format: val.encoding_format.map(Into::into),
            dimensions: val.dimensions,
            user: val.user,
        }
    }
}

impl From<liter_llm::types::EmbeddingRequest> for EmbeddingRequest {
    fn from(val: liter_llm::types::EmbeddingRequest) -> Self {
        Self {
            model: val.model,
            input: val.input.into(),
            encoding_format: val.encoding_format.map(Into::into),
            dimensions: val.dimensions,
            user: val.user,
        }
    }
}

impl From<EmbeddingResponse> for liter_llm::types::EmbeddingResponse {
    fn from(val: EmbeddingResponse) -> Self {
        Self {
            object: val.object,
            data: val.data.into_iter().map(Into::into).collect(),
            model: val.model,
            usage: val.usage.map(Into::into),
        }
    }
}

impl From<liter_llm::types::EmbeddingResponse> for EmbeddingResponse {
    fn from(val: liter_llm::types::EmbeddingResponse) -> Self {
        Self {
            object: val.object,
            data: val.data.into_iter().map(Into::into).collect(),
            model: val.model,
            usage: val.usage.map(Into::into),
        }
    }
}

impl From<EmbeddingObject> for liter_llm::types::EmbeddingObject {
    fn from(val: EmbeddingObject) -> Self {
        Self {
            object: val.object,
            embedding: val.embedding,
            index: val.index,
        }
    }
}

impl From<liter_llm::types::EmbeddingObject> for EmbeddingObject {
    fn from(val: liter_llm::types::EmbeddingObject) -> Self {
        Self {
            object: val.object,
            embedding: val.embedding,
            index: val.index,
        }
    }
}

impl From<CreateImageRequest> for liter_llm::types::CreateImageRequest {
    fn from(val: CreateImageRequest) -> Self {
        Self {
            prompt: val.prompt,
            model: val.model,
            n: val.n,
            size: val.size,
            quality: val.quality,
            style: val.style,
            response_format: val.response_format,
            user: val.user,
        }
    }
}

impl From<liter_llm::types::CreateImageRequest> for CreateImageRequest {
    fn from(val: liter_llm::types::CreateImageRequest) -> Self {
        Self {
            prompt: val.prompt,
            model: val.model,
            n: val.n,
            size: val.size,
            quality: val.quality,
            style: val.style,
            response_format: val.response_format,
            user: val.user,
        }
    }
}

impl From<ImagesResponse> for liter_llm::types::ImagesResponse {
    fn from(val: ImagesResponse) -> Self {
        Self {
            created: val.created,
            data: val.data.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<liter_llm::types::ImagesResponse> for ImagesResponse {
    fn from(val: liter_llm::types::ImagesResponse) -> Self {
        Self {
            created: val.created,
            data: val.data.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<Image> for liter_llm::types::Image {
    fn from(val: Image) -> Self {
        Self {
            url: val.url,
            b64_json: val.b64_json,
            revised_prompt: val.revised_prompt,
        }
    }
}

impl From<liter_llm::types::Image> for Image {
    fn from(val: liter_llm::types::Image) -> Self {
        Self {
            url: val.url,
            b64_json: val.b64_json,
            revised_prompt: val.revised_prompt,
        }
    }
}

impl From<liter_llm::types::CreateSpeechRequest> for CreateSpeechRequest {
    fn from(val: liter_llm::types::CreateSpeechRequest) -> Self {
        Self {
            model: val.model,
            input: val.input,
            voice: val.voice,
            response_format: val.response_format,
            speed: val.speed,
        }
    }
}

impl From<CreateTranscriptionRequest> for liter_llm::types::CreateTranscriptionRequest {
    fn from(val: CreateTranscriptionRequest) -> Self {
        Self {
            model: val.model,
            file: val.file,
            language: val.language,
            prompt: val.prompt,
            response_format: val.response_format,
            temperature: val.temperature,
        }
    }
}

impl From<liter_llm::types::CreateTranscriptionRequest> for CreateTranscriptionRequest {
    fn from(val: liter_llm::types::CreateTranscriptionRequest) -> Self {
        Self {
            model: val.model,
            file: val.file,
            language: val.language,
            prompt: val.prompt,
            response_format: val.response_format,
            temperature: val.temperature,
        }
    }
}

impl From<TranscriptionResponse> for liter_llm::types::TranscriptionResponse {
    fn from(val: TranscriptionResponse) -> Self {
        Self {
            text: val.text,
            language: val.language,
            duration: val.duration,
            segments: val.segments.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

impl From<liter_llm::types::TranscriptionResponse> for TranscriptionResponse {
    fn from(val: liter_llm::types::TranscriptionResponse) -> Self {
        Self {
            text: val.text,
            language: val.language,
            duration: val.duration,
            segments: val.segments.map(|v| v.into_iter().map(Into::into).collect()),
        }
    }
}

impl From<TranscriptionSegment> for liter_llm::types::TranscriptionSegment {
    fn from(val: TranscriptionSegment) -> Self {
        Self {
            id: val.id,
            start: val.start,
            end: val.end,
            text: val.text,
        }
    }
}

impl From<liter_llm::types::TranscriptionSegment> for TranscriptionSegment {
    fn from(val: liter_llm::types::TranscriptionSegment) -> Self {
        Self {
            id: val.id,
            start: val.start,
            end: val.end,
            text: val.text,
        }
    }
}

impl From<ModerationRequest> for liter_llm::types::ModerationRequest {
    fn from(val: ModerationRequest) -> Self {
        Self {
            input: val.input.into(),
            model: val.model,
        }
    }
}

impl From<liter_llm::types::ModerationRequest> for ModerationRequest {
    fn from(val: liter_llm::types::ModerationRequest) -> Self {
        Self {
            input: val.input.into(),
            model: val.model,
        }
    }
}

impl From<ModerationResponse> for liter_llm::types::ModerationResponse {
    fn from(val: ModerationResponse) -> Self {
        Self {
            id: val.id,
            model: val.model,
            results: val.results.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<liter_llm::types::ModerationResponse> for ModerationResponse {
    fn from(val: liter_llm::types::ModerationResponse) -> Self {
        Self {
            id: val.id,
            model: val.model,
            results: val.results.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<ModerationResult> for liter_llm::types::ModerationResult {
    fn from(val: ModerationResult) -> Self {
        Self {
            flagged: val.flagged,
            categories: val.categories.into(),
            category_scores: val.category_scores.into(),
        }
    }
}

impl From<liter_llm::types::ModerationResult> for ModerationResult {
    fn from(val: liter_llm::types::ModerationResult) -> Self {
        Self {
            flagged: val.flagged,
            categories: val.categories.into(),
            category_scores: val.category_scores.into(),
        }
    }
}

impl From<ModerationCategories> for liter_llm::types::ModerationCategories {
    fn from(val: ModerationCategories) -> Self {
        Self {
            sexual: val.sexual,
            hate: val.hate,
            harassment: val.harassment,
            self_harm: val.self_harm,
            sexual_minors: val.sexual_minors,
            hate_threatening: val.hate_threatening,
            violence_graphic: val.violence_graphic,
            self_harm_intent: val.self_harm_intent,
            self_harm_instructions: val.self_harm_instructions,
            harassment_threatening: val.harassment_threatening,
            violence: val.violence,
        }
    }
}

impl From<liter_llm::types::ModerationCategories> for ModerationCategories {
    fn from(val: liter_llm::types::ModerationCategories) -> Self {
        Self {
            sexual: val.sexual,
            hate: val.hate,
            harassment: val.harassment,
            self_harm: val.self_harm,
            sexual_minors: val.sexual_minors,
            hate_threatening: val.hate_threatening,
            violence_graphic: val.violence_graphic,
            self_harm_intent: val.self_harm_intent,
            self_harm_instructions: val.self_harm_instructions,
            harassment_threatening: val.harassment_threatening,
            violence: val.violence,
        }
    }
}

impl From<ModerationCategoryScores> for liter_llm::types::ModerationCategoryScores {
    fn from(val: ModerationCategoryScores) -> Self {
        Self {
            sexual: val.sexual,
            hate: val.hate,
            harassment: val.harassment,
            self_harm: val.self_harm,
            sexual_minors: val.sexual_minors,
            hate_threatening: val.hate_threatening,
            violence_graphic: val.violence_graphic,
            self_harm_intent: val.self_harm_intent,
            self_harm_instructions: val.self_harm_instructions,
            harassment_threatening: val.harassment_threatening,
            violence: val.violence,
        }
    }
}

impl From<liter_llm::types::ModerationCategoryScores> for ModerationCategoryScores {
    fn from(val: liter_llm::types::ModerationCategoryScores) -> Self {
        Self {
            sexual: val.sexual,
            hate: val.hate,
            harassment: val.harassment,
            self_harm: val.self_harm,
            sexual_minors: val.sexual_minors,
            hate_threatening: val.hate_threatening,
            violence_graphic: val.violence_graphic,
            self_harm_intent: val.self_harm_intent,
            self_harm_instructions: val.self_harm_instructions,
            harassment_threatening: val.harassment_threatening,
            violence: val.violence,
        }
    }
}

impl From<RerankRequest> for liter_llm::types::RerankRequest {
    fn from(val: RerankRequest) -> Self {
        Self {
            model: val.model,
            query: val.query,
            documents: val.documents.into_iter().map(Into::into).collect(),
            top_n: val.top_n,
            return_documents: val.return_documents,
        }
    }
}

impl From<liter_llm::types::RerankRequest> for RerankRequest {
    fn from(val: liter_llm::types::RerankRequest) -> Self {
        Self {
            model: val.model,
            query: val.query,
            documents: val.documents.into_iter().map(Into::into).collect(),
            top_n: val.top_n,
            return_documents: val.return_documents,
        }
    }
}

impl From<RerankResponse> for liter_llm::types::RerankResponse {
    fn from(val: RerankResponse) -> Self {
        Self {
            id: val.id,
            results: val.results.into_iter().map(Into::into).collect(),
            meta: val.meta.as_ref().and_then(|s| serde_json::from_str(s).ok()),
        }
    }
}

impl From<liter_llm::types::RerankResponse> for RerankResponse {
    fn from(val: liter_llm::types::RerankResponse) -> Self {
        Self {
            id: val.id,
            results: val.results.into_iter().map(Into::into).collect(),
            meta: val.meta.as_ref().map(ToString::to_string),
        }
    }
}

impl From<RerankResult> for liter_llm::types::RerankResult {
    fn from(val: RerankResult) -> Self {
        Self {
            index: val.index,
            relevance_score: val.relevance_score,
            document: val.document.map(Into::into),
        }
    }
}

impl From<liter_llm::types::RerankResult> for RerankResult {
    fn from(val: liter_llm::types::RerankResult) -> Self {
        Self {
            index: val.index,
            relevance_score: val.relevance_score,
            document: val.document.map(Into::into),
        }
    }
}

impl From<RerankResultDocument> for liter_llm::types::RerankResultDocument {
    fn from(val: RerankResultDocument) -> Self {
        Self { text: val.text }
    }
}

impl From<liter_llm::types::RerankResultDocument> for RerankResultDocument {
    fn from(val: liter_llm::types::RerankResultDocument) -> Self {
        Self { text: val.text }
    }
}

impl From<SearchRequest> for liter_llm::types::SearchRequest {
    fn from(val: SearchRequest) -> Self {
        Self {
            model: val.model,
            query: val.query,
            max_results: val.max_results,
            search_domain_filter: val.search_domain_filter,
            country: val.country,
        }
    }
}

impl From<liter_llm::types::SearchRequest> for SearchRequest {
    fn from(val: liter_llm::types::SearchRequest) -> Self {
        Self {
            model: val.model,
            query: val.query,
            max_results: val.max_results,
            search_domain_filter: val.search_domain_filter,
            country: val.country,
        }
    }
}

impl From<SearchResponse> for liter_llm::types::SearchResponse {
    fn from(val: SearchResponse) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            model: val.model,
        }
    }
}

impl From<liter_llm::types::SearchResponse> for SearchResponse {
    fn from(val: liter_llm::types::SearchResponse) -> Self {
        Self {
            results: val.results.into_iter().map(Into::into).collect(),
            model: val.model,
        }
    }
}

impl From<SearchResult> for liter_llm::types::SearchResult {
    fn from(val: SearchResult) -> Self {
        Self {
            title: val.title,
            url: val.url,
            snippet: val.snippet,
            date: val.date,
        }
    }
}

impl From<liter_llm::types::SearchResult> for SearchResult {
    fn from(val: liter_llm::types::SearchResult) -> Self {
        Self {
            title: val.title,
            url: val.url,
            snippet: val.snippet,
            date: val.date,
        }
    }
}

impl From<OcrRequest> for liter_llm::types::OcrRequest {
    fn from(val: OcrRequest) -> Self {
        Self {
            model: val.model,
            document: val.document.into(),
            pages: val.pages,
            include_image_base64: val.include_image_base64,
        }
    }
}

impl From<liter_llm::types::OcrRequest> for OcrRequest {
    fn from(val: liter_llm::types::OcrRequest) -> Self {
        Self {
            model: val.model,
            document: val.document.into(),
            pages: val.pages,
            include_image_base64: val.include_image_base64,
        }
    }
}

impl From<OcrResponse> for liter_llm::types::OcrResponse {
    fn from(val: OcrResponse) -> Self {
        Self {
            pages: val.pages.into_iter().map(Into::into).collect(),
            model: val.model,
            usage: val.usage.map(Into::into),
        }
    }
}

impl From<liter_llm::types::OcrResponse> for OcrResponse {
    fn from(val: liter_llm::types::OcrResponse) -> Self {
        Self {
            pages: val.pages.into_iter().map(Into::into).collect(),
            model: val.model,
            usage: val.usage.map(Into::into),
        }
    }
}

impl From<OcrPage> for liter_llm::types::OcrPage {
    fn from(val: OcrPage) -> Self {
        Self {
            index: val.index,
            markdown: val.markdown,
            images: val.images.map(|v| v.into_iter().map(Into::into).collect()),
            dimensions: val.dimensions.map(Into::into),
        }
    }
}

impl From<liter_llm::types::OcrPage> for OcrPage {
    fn from(val: liter_llm::types::OcrPage) -> Self {
        Self {
            index: val.index,
            markdown: val.markdown,
            images: val.images.map(|v| v.into_iter().map(Into::into).collect()),
            dimensions: val.dimensions.map(Into::into),
        }
    }
}

impl From<OcrImage> for liter_llm::types::OcrImage {
    fn from(val: OcrImage) -> Self {
        Self {
            id: val.id,
            image_base64: val.image_base64,
        }
    }
}

impl From<liter_llm::types::OcrImage> for OcrImage {
    fn from(val: liter_llm::types::OcrImage) -> Self {
        Self {
            id: val.id,
            image_base64: val.image_base64,
        }
    }
}

impl From<PageDimensions> for liter_llm::types::PageDimensions {
    fn from(val: PageDimensions) -> Self {
        Self {
            width: val.width,
            height: val.height,
        }
    }
}

impl From<liter_llm::types::PageDimensions> for PageDimensions {
    fn from(val: liter_llm::types::PageDimensions) -> Self {
        Self {
            width: val.width,
            height: val.height,
        }
    }
}

impl From<ModelsListResponse> for liter_llm::types::ModelsListResponse {
    fn from(val: ModelsListResponse) -> Self {
        Self {
            object: val.object,
            data: val.data.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<liter_llm::types::ModelsListResponse> for ModelsListResponse {
    fn from(val: liter_llm::types::ModelsListResponse) -> Self {
        Self {
            object: val.object,
            data: val.data.into_iter().map(Into::into).collect(),
        }
    }
}

impl From<ModelObject> for liter_llm::types::ModelObject {
    fn from(val: ModelObject) -> Self {
        Self {
            id: val.id,
            object: val.object,
            created: val.created,
            owned_by: val.owned_by,
        }
    }
}

impl From<liter_llm::types::ModelObject> for ModelObject {
    fn from(val: liter_llm::types::ModelObject) -> Self {
        Self {
            id: val.id,
            object: val.object,
            created: val.created,
            owned_by: val.owned_by,
        }
    }
}

impl From<CustomProviderConfig> for liter_llm::provider::custom::CustomProviderConfig {
    fn from(val: CustomProviderConfig) -> Self {
        Self {
            name: val.name,
            base_url: val.base_url,
            auth_header: val.auth_header.into(),
            model_prefixes: val.model_prefixes,
        }
    }
}

impl From<liter_llm::provider::custom::CustomProviderConfig> for CustomProviderConfig {
    fn from(val: liter_llm::provider::custom::CustomProviderConfig) -> Self {
        Self {
            name: val.name,
            base_url: val.base_url,
            auth_header: val.auth_header.into(),
            model_prefixes: val.model_prefixes,
        }
    }
}

impl From<ImageDetail> for liter_llm::types::ImageDetail {
    fn from(val: ImageDetail) -> Self {
        match val {
            ImageDetail::Low => Self::Low,
            ImageDetail::High => Self::High,
            ImageDetail::Auto => Self::Auto,
        }
    }
}

impl From<liter_llm::types::ImageDetail> for ImageDetail {
    fn from(val: liter_llm::types::ImageDetail) -> Self {
        match val {
            liter_llm::types::ImageDetail::Low => Self::Low,
            liter_llm::types::ImageDetail::High => Self::High,
            liter_llm::types::ImageDetail::Auto => Self::Auto,
        }
    }
}

impl From<ToolType> for liter_llm::types::ToolType {
    fn from(val: ToolType) -> Self {
        match val {
            ToolType::Function => Self::Function,
        }
    }
}

impl From<liter_llm::types::ToolType> for ToolType {
    fn from(val: liter_llm::types::ToolType) -> Self {
        match val {
            liter_llm::types::ToolType::Function => Self::Function,
        }
    }
}

impl From<ToolChoiceMode> for liter_llm::types::ToolChoiceMode {
    fn from(val: ToolChoiceMode) -> Self {
        match val {
            ToolChoiceMode::Auto => Self::Auto,
            ToolChoiceMode::Required => Self::Required,
            ToolChoiceMode::None => Self::None,
        }
    }
}

impl From<liter_llm::types::ToolChoiceMode> for ToolChoiceMode {
    fn from(val: liter_llm::types::ToolChoiceMode) -> Self {
        match val {
            liter_llm::types::ToolChoiceMode::Auto => Self::Auto,
            liter_llm::types::ToolChoiceMode::Required => Self::Required,
            liter_llm::types::ToolChoiceMode::None => Self::None,
        }
    }
}

impl From<FinishReason> for liter_llm::types::FinishReason {
    fn from(val: FinishReason) -> Self {
        match val {
            FinishReason::Stop => Self::Stop,
            FinishReason::Length => Self::Length,
            FinishReason::ToolCalls => Self::ToolCalls,
            FinishReason::ContentFilter => Self::ContentFilter,
            FinishReason::FunctionCall => Self::FunctionCall,
            FinishReason::Other => Self::Other,
        }
    }
}

impl From<liter_llm::types::FinishReason> for FinishReason {
    fn from(val: liter_llm::types::FinishReason) -> Self {
        match val {
            liter_llm::types::FinishReason::Stop => Self::Stop,
            liter_llm::types::FinishReason::Length => Self::Length,
            liter_llm::types::FinishReason::ToolCalls => Self::ToolCalls,
            liter_llm::types::FinishReason::ContentFilter => Self::ContentFilter,
            liter_llm::types::FinishReason::FunctionCall => Self::FunctionCall,
            liter_llm::types::FinishReason::Other => Self::Other,
        }
    }
}

impl From<ReasoningEffort> for liter_llm::types::ReasoningEffort {
    fn from(val: ReasoningEffort) -> Self {
        match val {
            ReasoningEffort::Low => Self::Low,
            ReasoningEffort::Medium => Self::Medium,
            ReasoningEffort::High => Self::High,
        }
    }
}

impl From<liter_llm::types::ReasoningEffort> for ReasoningEffort {
    fn from(val: liter_llm::types::ReasoningEffort) -> Self {
        match val {
            liter_llm::types::ReasoningEffort::Low => Self::Low,
            liter_llm::types::ReasoningEffort::Medium => Self::Medium,
            liter_llm::types::ReasoningEffort::High => Self::High,
        }
    }
}

impl From<EmbeddingFormat> for liter_llm::types::EmbeddingFormat {
    fn from(val: EmbeddingFormat) -> Self {
        match val {
            EmbeddingFormat::Float => Self::Float,
            EmbeddingFormat::Base64 => Self::Base64,
        }
    }
}

impl From<liter_llm::types::EmbeddingFormat> for EmbeddingFormat {
    fn from(val: liter_llm::types::EmbeddingFormat) -> Self {
        match val {
            liter_llm::types::EmbeddingFormat::Float => Self::Float,
            liter_llm::types::EmbeddingFormat::Base64 => Self::Base64,
        }
    }
}

#[pyfunction]
pub fn init_async_runtime() -> PyResult<()> {
    // Tokio runtime auto-initializes on first future_into_py call
    Ok(())
}

#[pymodule]
pub fn _internal_bindings(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_function(wrap_pyfunction!(init_async_runtime, m)?)?;
    m.add_class::<SystemMessage>()?;
    m.add_class::<UserMessage>()?;
    m.add_class::<ImageUrl>()?;
    m.add_class::<DocumentContent>()?;
    m.add_class::<AudioContent>()?;
    m.add_class::<AssistantMessage>()?;
    m.add_class::<ToolMessage>()?;
    m.add_class::<DeveloperMessage>()?;
    m.add_class::<FunctionMessage>()?;
    m.add_class::<ChatCompletionTool>()?;
    m.add_class::<FunctionDefinition>()?;
    m.add_class::<ToolCall>()?;
    m.add_class::<FunctionCall>()?;
    m.add_class::<SpecificToolChoice>()?;
    m.add_class::<SpecificFunction>()?;
    m.add_class::<JsonSchemaFormat>()?;
    m.add_class::<Usage>()?;
    m.add_class::<ChatCompletionRequest>()?;
    m.add_class::<StreamOptions>()?;
    m.add_class::<ChatCompletionResponse>()?;
    m.add_class::<Choice>()?;
    m.add_class::<ChatCompletionChunk>()?;
    m.add_class::<StreamChoice>()?;
    m.add_class::<StreamDelta>()?;
    m.add_class::<StreamToolCall>()?;
    m.add_class::<StreamFunctionCall>()?;
    m.add_class::<EmbeddingRequest>()?;
    m.add_class::<EmbeddingResponse>()?;
    m.add_class::<EmbeddingObject>()?;
    m.add_class::<CreateImageRequest>()?;
    m.add_class::<ImagesResponse>()?;
    m.add_class::<Image>()?;
    m.add_class::<CreateSpeechRequest>()?;
    m.add_class::<CreateTranscriptionRequest>()?;
    m.add_class::<TranscriptionResponse>()?;
    m.add_class::<TranscriptionSegment>()?;
    m.add_class::<ModerationRequest>()?;
    m.add_class::<ModerationResponse>()?;
    m.add_class::<ModerationResult>()?;
    m.add_class::<ModerationCategories>()?;
    m.add_class::<ModerationCategoryScores>()?;
    m.add_class::<RerankRequest>()?;
    m.add_class::<RerankResponse>()?;
    m.add_class::<RerankResult>()?;
    m.add_class::<RerankResultDocument>()?;
    m.add_class::<SearchRequest>()?;
    m.add_class::<SearchResponse>()?;
    m.add_class::<SearchResult>()?;
    m.add_class::<OcrRequest>()?;
    m.add_class::<OcrResponse>()?;
    m.add_class::<OcrPage>()?;
    m.add_class::<OcrImage>()?;
    m.add_class::<PageDimensions>()?;
    m.add_class::<ModelsListResponse>()?;
    m.add_class::<ModelObject>()?;
    m.add_class::<DefaultClient>()?;
    m.add_class::<CustomProviderConfig>()?;
    m.add_class::<Message>()?;
    m.add_class::<UserContent>()?;
    m.add_class::<ContentPart>()?;
    m.add_class::<ImageDetail>()?;
    m.add_class::<ToolType>()?;
    m.add_class::<ToolChoice>()?;
    m.add_class::<ToolChoiceMode>()?;
    m.add_class::<ResponseFormat>()?;
    m.add_class::<StopSequence>()?;
    m.add_class::<FinishReason>()?;
    m.add_class::<ReasoningEffort>()?;
    m.add_class::<EmbeddingFormat>()?;
    m.add_class::<EmbeddingInput>()?;
    m.add_class::<ModerationInput>()?;
    m.add_class::<RerankDocument>()?;
    m.add_class::<OcrDocument>()?;
    m.add_class::<AuthHeaderFormat>()?;
    m.add_function(wrap_pyfunction!(create_client, m)?)?;
    m.add_function(wrap_pyfunction!(create_client_from_json, m)?)?;
    m.add_function(wrap_pyfunction!(register_custom_provider, m)?)?;
    m.add_function(wrap_pyfunction!(unregister_custom_provider, m)?)?;
    m.add("AuthenticationError", m.py().get_type::<AuthenticationError>())?;
    m.add("RateLimitedError", m.py().get_type::<RateLimitedError>())?;
    m.add("BadRequestError", m.py().get_type::<BadRequestError>())?;
    m.add(
        "ContextWindowExceededError",
        m.py().get_type::<ContextWindowExceededError>(),
    )?;
    m.add("ContentPolicyError", m.py().get_type::<ContentPolicyError>())?;
    m.add("NotFoundError", m.py().get_type::<NotFoundError>())?;
    m.add("ServerError", m.py().get_type::<ServerError>())?;
    m.add("ServiceUnavailableError", m.py().get_type::<ServiceUnavailableError>())?;
    m.add("LiterLlmTimeoutError", m.py().get_type::<LiterLlmTimeoutError>())?;
    m.add("StreamingError", m.py().get_type::<StreamingError>())?;
    m.add(
        "EndpointNotSupportedError",
        m.py().get_type::<EndpointNotSupportedError>(),
    )?;
    m.add("InvalidHeaderError", m.py().get_type::<InvalidHeaderError>())?;
    m.add("SerializationError", m.py().get_type::<SerializationError>())?;
    m.add("BudgetExceededError", m.py().get_type::<BudgetExceededError>())?;
    m.add("HookRejectedError", m.py().get_type::<HookRejectedError>())?;
    m.add("InternalError", m.py().get_type::<InternalError>())?;
    m.add("LiterLlmError", m.py().get_type::<LiterLlmError>())?;
    Ok(())
}
