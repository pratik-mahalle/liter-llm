# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: chat

test_that("developer_message: Chat request that includes a developer role message alongside user messages", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_equal(trimws(result$choices[["0"]]$message$content), "s[::-1]")
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "stop")
})

test_that("finish_reason_content_filter: Chat response stopped by content filter with finish_reason of content_filter and null content", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "content_filter")
})

test_that("finish_reason_length: Chat response truncated due to max_tokens limit with finish_reason of length", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "length")
})

test_that("multi_turn_conversation: Multi-turn conversation with system, user, assistant, and follow-up user messages", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_equal(trimws(result$choices[["0"]]$message$content), "4 + 4 equals 8.")
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "stop")
})

test_that("parallel_tool_calls: Chat request that results in parallel tool calls in the response", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_true(if (is.character(result$choices[["0"]]$message$tool_calls)) nchar(result$choices[["0"]]$message$tool_calls) > 0 else length(result$choices[["0"]]$message$tool_calls) > 0)
  # TODO: unsupported assertion type: count_equals
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "tool_calls")
})

test_that("response_format_json_object: Chat request with response_format json_object that returns valid JSON content", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_true(if (is.character(result$choices[["0"]]$message$content)) nchar(result$choices[["0"]]$message$content) > 0 else length(result$choices[["0"]]$message$content) > 0)
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "stop")
})

test_that("response_format_json_schema: Chat request with response_format json_schema that validates the output structure", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_true(if (is.character(result$choices[["0"]]$message$content)) nchar(result$choices[["0"]]$message$content) > 0 else length(result$choices[["0"]]$message$content) > 0)
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "stop")
})

test_that("seed_parameter: Chat request with seed parameter for deterministic output; response includes system_fingerprint", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "stop")
  expect_true(if (is.character(result$system_fingerprint)) nchar(result$system_fingerprint) > 0 else length(result$system_fingerprint) > 0)
})

test_that("stop_sequences: Chat request with custom stop sequences that terminates generation at a stop token", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "stop")
})

test_that("tool_choice_required: Chat request with tool_choice set to required forces the model to call a tool", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_true(if (is.character(result$choices[["0"]]$message$tool_calls)) nchar(result$choices[["0"]]$message$tool_calls) > 0 else length(result$choices[["0"]]$message$tool_calls) > 0)
  expect_equal(trimws(result$choices[["0"]]$message$tool_calls[["0"]]$function$name), "get_weather")
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "tool_calls")
})

test_that("tool_choice_specific: Chat request with tool_choice specifying a particular function to call", {
  result <- chat()
  # TODO: unsupported assertion type: count_equals
  expect_true(if (is.character(result$choices[["0"]]$message$tool_calls)) nchar(result$choices[["0"]]$message$tool_calls) > 0 else length(result$choices[["0"]]$message$tool_calls) > 0)
  expect_equal(trimws(result$choices[["0"]]$message$tool_calls[["0"]]$function$name), "get_weather")
  expect_equal(trimws(result$choices[["0"]]$finish_reason), "tool_calls")
})
