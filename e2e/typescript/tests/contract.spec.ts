// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { startMockServer, type MockServer, type MockRoute } from "./helpers";
// @ts-ignore – binding not yet implemented; tests compile and fail at runtime
import { LlmClient } from "@kreuzberg/liter-llm";

describe("contract", () => {
	// Verify all bindings expose the full API surface — constructor accepts all config options and every method exists
	it("binding_api_parity", async () => {
		const routes: MockRoute[] = [
			{
				path: "/chat/completions",
				method: "POST",
				status: 200,
				body: `{"choices":[{"finish_reason":"stop","index":0,"message":{"content":"OK","role":"assistant"}}],"created":1234567890,"id":"chatcmpl-contract","model":"gpt-4o","object":"chat.completion","usage":{"completion_tokens":1,"prompt_tokens":5,"total_tokens":6}}`,
				streamChunks: [],
			},
		];

		const server = await startMockServer(routes);
		try {
			const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url });

			const response = await client.chat(
				JSON.parse(`{"messages":[{"content":"Contract test","role":"user"}],"model":"openai/gpt-4o"}`),
			);

			expect(response.choices.length, "Choices count mismatch").toBe(1);
			expect(response.choices[0].finishReason, "finish_reason mismatch").toBe("stop");
			expect(response.choices[0].message.content, "message content mismatch").toBe("OK");
			expect(response.usage, "Expected usage object").toBeTruthy();
			expect(response.usage.totalTokens, "total_tokens mismatch").toBe(6);
			expect(response.model, "model mismatch").toBe("gpt-4o");
		} finally {
			server.close();
		}
	});

	// Verify ocr() method exists in all bindings
	it("contract_ocr", async () => {
		const routes: MockRoute[] = [
			{
				path: "/chat/completions",
				method: "POST",
				status: 200,
				body: `{"model":"mistral/mistral-ocr-latest","pages":[{"index":0,"markdown":"# Contract Test\\n\\nThis verifies OCR method exists."}]}`,
				streamChunks: [],
			},
		];

		const server = await startMockServer(routes);
		try {
			const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url });

			// TODO: unknown method "ocr"
		} finally {
			server.close();
		}
	});

	// Verify search() method exists in all bindings
	it("contract_search", async () => {
		const routes: MockRoute[] = [
			{
				path: "/chat/completions",
				method: "POST",
				status: 200,
				body: `{"model":"brave/web-search","results":[{"snippet":"Test result","title":"Test","url":"https://example.com"}]}`,
				streamChunks: [],
			},
		];

		const server = await startMockServer(routes);
		try {
			const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url });

			// TODO: unknown method "search"
		} finally {
			server.close();
		}
	});
});
