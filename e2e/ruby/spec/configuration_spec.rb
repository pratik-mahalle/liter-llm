# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
# frozen_string_literal: true

require "spec_helper"

RSpec.describe "configuration" do
  it "custom_base_url" do
    # Client configured with a custom base URL routes all requests to that endpoint
    route = E2EHelpers::MockRoute.new(
      path: "/chat/completions",
      method: "POST",
      status: 200,
      body: '{"choices":[{"finish_reason":"stop","index":0,"message":{"content":"Hi there!","role":"assistant"}}],"created":1711000000,"id":"chatcmpl-local-001","model":"local-model","object":"chat.completion","usage":{"completion_tokens":4,"prompt_tokens":3,"total_tokens":7}}',
      stream_chunks: []
    )
    server = E2EHelpers::MockServer.new([route])

    response = post_json(server.url, "/chat/completions", '{"messages":[{"content":"Hello","role":"user"}],"model":"local-model"}')

    expect(response.code.to_i).to eq(200)

    body = JSON.parse(response.body)

    expect(body["choices"].size).to eq(1)
    expect(body.dig("choices", 0, "message", "content")).to eq('Hi there!')
    expect(body.dig("choices", 0, "finish_reason")).to eq('stop')
    expect(body["model"]).to eq('local-model')
    expect(body["usage"]).not_to be_nil
    expect(body.dig("usage", "total_tokens")).to eq(7)

  ensure
    server&.stop
  end

  it "extra_headers" do
    # Client configured with extra custom headers successfully completes a chat request
    route = E2EHelpers::MockRoute.new(
      path: "/chat/completions",
      method: "POST",
      status: 200,
      body: '{"choices":[{"finish_reason":"stop","index":0,"message":{"content":"Hello! How can I help you?","role":"assistant"}}],"created":1711000140,"id":"chatcmpl-headers001","model":"gpt-4","object":"chat.completion","usage":{"completion_tokens":8,"prompt_tokens":8,"total_tokens":16}}',
      stream_chunks: []
    )
    server = E2EHelpers::MockServer.new([route])

    response = post_json(server.url, "/chat/completions", '{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}')

    expect(response.code.to_i).to eq(200)

    body = JSON.parse(response.body)

    expect(body["choices"].size).to eq(1)
    expect(body.dig("choices", 0, "message", "content")).to eq('Hello! How can I help you?')
    expect(body.dig("choices", 0, "finish_reason")).to eq('stop')
    expect(body["model"]).to eq('gpt-4')
    expect(body["usage"]).not_to be_nil
    expect(body.dig("usage", "total_tokens")).to eq(16)

  ensure
    server&.stop
  end

end
