// This file is auto-generated by alef. DO NOT EDIT.
//! E2e tests for category: speech

use liter_llm::chat;
mod mock_server;
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_edge_speech_long_input() {
    // Speech generation with a very long input text
    let mock_route = MockRoute {
        path: "/v1/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock binary audio data for long text"}"#.to_string(),
        stream_chunks: vec![],
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json = serde_json::json!("This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. This is a long input text. End of input.");
    let request = serde_json::from_value(request_json).unwrap();
    let result = chat(request).await.expect("should succeed");
    assert!(!result.audio.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_error_speech_auth_401() {
    // 401 Unauthorized for speech generation with invalid API key
    let mock_route = MockRoute {
        path: "/v1/audio/speech",
        method: "POST",
        status: 401,
        body: r#"{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}"#.to_string(),
        stream_chunks: vec![],
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json = serde_json::json!("Hello");
    let request = serde_json::from_value(request_json).unwrap();
    let result = chat(request).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(result.as_ref().unwrap_err().to_string().contains("Authentication"), "error message mismatch");
}

#[tokio::test]
async fn test_error_speech_bad_model() {
    // 400 Bad Request for speech with unsupported model
    let mock_route = MockRoute {
        path: "/v1/audio/speech",
        method: "POST",
        status: 400,
        body: r#"{"error":{"code":"model_not_found","message":"The model 'tts-nonexistent' does not exist.","param":"model","type":"invalid_request_error"}}"#.to_string(),
        stream_chunks: vec![],
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json = serde_json::json!("Hello");
    let request = serde_json::from_value(request_json).unwrap();
    let result = chat(request).await;
    assert!(result.is_err(), "expected call to fail");
    assert!(result.as_ref().unwrap_err().to_string().contains("InvalidRequest"), "error message mismatch");
}

#[tokio::test]
async fn test_smoke_speech_basic() {
    // Basic text-to-speech generation
    let mock_route = MockRoute {
        path: "/v1/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock binary audio data"}"#.to_string(),
        stream_chunks: vec![],
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json = serde_json::json!("Hello, world!");
    let request = serde_json::from_value(request_json).unwrap();
    let result = chat(request).await.expect("should succeed");
    assert!(!result.audio.is_empty(), "expected non-empty value");
}

#[tokio::test]
async fn test_smoke_speech_mp3_format() {
    // Text-to-speech with explicit MP3 response format
    let mock_route = MockRoute {
        path: "/v1/audio/speech",
        method: "POST",
        status: 200,
        body: r#"{"_binary":true,"_content_type":"audio/mpeg","_description":"Mock MP3 audio data"}"#.to_string(),
        stream_chunks: vec![],
    };
    let mock_server = MockServer::start(vec![mock_route]).await;
    let request_json = serde_json::json!("The quick brown fox jumps over the lazy dog.");
    let request = serde_json::from_value(request_json).unwrap();
    let result = chat(request).await.expect("should succeed");
    assert!(!result.audio.is_empty(), "expected non-empty value");
}

