/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* List models response returns an empty data array when no models are available
 */
static void test_empty_model_list(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body = "{\"data\":[],\"object\":\"list\"}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/models", base_url);

    LiterLlmResponse *resp = liter_llm_http_get(url);
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_empty_model_list");
    liter_llm_assert_json_array_len(resp->body, "data", 0,
                                   "test_empty_model_list");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "data", 0,
                                   "test_empty_model_list");
  }
}

/* 401 Unauthorized error on list models request when API key is invalid */
static void test_list_models_error_401(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API "
      "key provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/models", base_url);

    LiterLlmResponse *resp = liter_llm_http_get(url);
    assert(resp != NULL);
    liter_llm_assert_status(resp, 401L, "test_list_models_error_401");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    (void)mock_body; /* error or stream test — skip offline assertions */
  }
}

int main(void) {
  test_empty_model_list();
  printf("PASS: List models response returns an empty data array when no "
         "models are available\n");
  test_list_models_error_401();
  printf("PASS: 401 Unauthorized error on list models request when API key is "
         "invalid\n");
  printf("All list_models tests passed.\n");
  return 0;
}
