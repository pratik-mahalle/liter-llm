/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* OCR request with a document URL input */
static void test_ocr_url_document(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"model\":\"mistral/"
      "mistral-ocr-latest\",\"pages\":[{\"index\":0,\"markdown\":\"# Test "
      "Document\\n\\nThis is a test.\"}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url,
        "{\"document\":{\"type\":\"document_url\",\"url\":\"https://"
        "example.com/doc.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_ocr_url_document");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_ocr_url_document();
  printf("PASS: OCR request with a document URL input\n");
  printf("All ocr tests passed.\n");
  return 0;
}
