/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Chat request that includes a developer role message alongside user messages
 */
static void test_developer_message(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"s[::-1]\",\"role\":\"assistant\"}}],\"created\":"
      "1711000130,\"id\":\"chatcmpl-dev001\",\"model\":\"gpt-4\",\"object\":"
      "\"chat.completion\",\"usage\":{\"completion_tokens\":5,\"prompt_"
      "tokens\":28,\"total_tokens\":33}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url,
        "{\"messages\":[{\"content\":\"You are a coding assistant. Always "
        "respond with concise code "
        "examples.\",\"role\":\"developer\"},{\"content\":\"How do I reverse a "
        "string in Python?\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_developer_message");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_developer_message");
    liter_llm_assert_json_field(resp->body, "content", "s[::-1]",
                               "test_developer_message");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_developer_message");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_developer_message");
    liter_llm_assert_json_field(mock_body, "content", "s[::-1]",
                               "test_developer_message");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_developer_message");
  }
}

/* Chat response stopped by content filter with finish_reason of content_filter
 * and null content */
static void test_finish_reason_content_filter(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"content_filter\",\"index\":0,"
      "\"message\":{\"content\":null,\"role\":\"assistant\"}}],\"created\":"
      "1711000100,\"id\":\"chatcmpl-filter001\",\"model\":\"gpt-4\",\"object\":"
      "\"chat.completion\",\"usage\":{\"completion_tokens\":0,\"prompt_"
      "tokens\":14,\"total_tokens\":14}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"Tell me something "
             "controversial\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_finish_reason_content_filter");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_finish_reason_content_filter");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_finish_reason_content_filter");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_finish_reason_content_filter");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_finish_reason_content_filter");
  }
}

/* Chat response truncated due to max_tokens limit with finish_reason of length
 */
static void test_finish_reason_length(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"length\",\"index\":0,\"message\":{"
      "\"content\":\"Once upon a "
      "time\",\"role\":\"assistant\"}}],\"created\":1711000090,\"id\":"
      "\"chatcmpl-length001\",\"model\":\"gpt-4\",\"object\":\"chat."
      "completion\",\"usage\":{\"completion_tokens\":5,\"prompt_tokens\":12,"
      "\"total_tokens\":17}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"max_tokens\":5,\"messages\":[{\"content\":\"Tell me a long "
             "story\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_finish_reason_length");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_finish_reason_length");
    liter_llm_assert_json_field(resp->body, "content", "Once upon a time",
                               "test_finish_reason_length");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_finish_reason_length");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_finish_reason_length");
    liter_llm_assert_json_field(mock_body, "content", "Once upon a time",
                               "test_finish_reason_length");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_finish_reason_length");
  }
}

/* Multi-turn conversation with system, user, assistant, and follow-up user
 * messages */
static void test_multi_turn_conversation(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"4 + 4 equals "
      "8.\",\"role\":\"assistant\"}}],\"created\":1711000030,\"id\":\"chatcmpl-"
      "multi001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\",\"usage\":"
      "{\"completion_tokens\":9,\"prompt_tokens\":45,\"total_tokens\":54}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"You are a helpful "
             "assistant.\",\"role\":\"system\"},{\"content\":\"What is 2 + "
             "2?\",\"role\":\"user\"},{\"content\":\"2 + 2 equals "
             "4.\",\"role\":\"assistant\"},{\"content\":\"And what is 4 + "
             "4?\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_multi_turn_conversation");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_multi_turn_conversation");
    liter_llm_assert_json_field(resp->body, "content", "4 + 4 equals 8.",
                               "test_multi_turn_conversation");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_multi_turn_conversation");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_multi_turn_conversation");
    liter_llm_assert_json_field(mock_body, "content", "4 + 4 equals 8.",
                               "test_multi_turn_conversation");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_multi_turn_conversation");
  }
}

/* Chat request that results in parallel tool calls in the response */
static void test_parallel_tool_calls(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"tool_calls\",\"index\":0,\"message\":"
      "{\"content\":null,\"role\":\"assistant\",\"tool_calls\":[{\"function\":{"
      "\"arguments\":\"{\\\"location\\\": \\\"New "
      "York\\\"}\",\"name\":\"get_weather\"},\"id\":\"call_par001\",\"type\":"
      "\"function\"},{\"function\":{\"arguments\":\"{\\\"location\\\": "
      "\\\"London\\\"}\",\"name\":\"get_weather\"},\"id\":\"call_par002\","
      "\"type\":\"function\"}]}}],\"created\":1711000060,\"id\":\"chatcmpl-"
      "parallel001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\","
      "\"usage\":{\"completion_tokens\":30,\"prompt_tokens\":65,\"total_"
      "tokens\":95}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"What is the weather in NYC and "
             "London?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"parallel_"
             "tool_calls\":true,\"tools\":[{\"function\":{\"description\":"
             "\"Get the current weather for a given "
             "location\",\"name\":\"get_weather\",\"parameters\":{"
             "\"properties\":{\"location\":{\"description\":\"The city "
             "name\",\"type\":\"string\"}},\"required\":[\"location\"],"
             "\"type\":\"object\"}},\"type\":\"function\"}]}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_parallel_tool_calls");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_parallel_tool_calls");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_parallel_tool_calls");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_parallel_tool_calls");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_parallel_tool_calls");
  }
}

/* Chat request with response_format json_object that returns valid JSON content
 */
static void test_response_format_json_object(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"{\\\"name\\\": \\\"Alice\\\", \\\"age\\\": "
      "30}\",\"role\":\"assistant\"}}],\"created\":1711000070,\"id\":"
      "\"chatcmpl-json001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\","
      "\"usage\":{\"completion_tokens\":12,\"prompt_tokens\":25,\"total_"
      "tokens\":37}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"Respond with JSON "
             "only.\",\"role\":\"system\"},{\"content\":\"Give me a user "
             "object with name and age "
             "fields.\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"response_"
             "format\":{\"type\":\"json_object\"}}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_response_format_json_object");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_response_format_json_object");
    liter_llm_assert_json_field(resp->body, "content",
                               "{\"name\": \"Alice\", \"age\": 30}",
                               "test_response_format_json_object");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_response_format_json_object");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_response_format_json_object");
    liter_llm_assert_json_field(mock_body, "content",
                               "{\"name\": \"Alice\", \"age\": 30}",
                               "test_response_format_json_object");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_response_format_json_object");
  }
}

/* Chat request with response_format json_schema that validates the output
 * structure */
static void test_response_format_json_schema(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"{\\\"temp\\\": "
      "18.5}\",\"role\":\"assistant\"}}],\"created\":1711000080,\"id\":"
      "\"chatcmpl-schema001\",\"model\":\"gpt-4\",\"object\":\"chat."
      "completion\",\"usage\":{\"completion_tokens\":8,\"prompt_tokens\":30,"
      "\"total_tokens\":38}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"What is the temperature in Paris "
             "today?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"response_"
             "format\":{\"json_schema\":{\"name\":\"weather\",\"schema\":{"
             "\"properties\":{\"temp\":{\"type\":\"number\"}},\"required\":["
             "\"temp\"],\"type\":\"object\"}},\"type\":\"json_schema\"}}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_response_format_json_schema");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_response_format_json_schema");
    liter_llm_assert_json_field(resp->body, "content", "{\"temp\": 18.5}",
                               "test_response_format_json_schema");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_response_format_json_schema");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_response_format_json_schema");
    liter_llm_assert_json_field(mock_body, "content", "{\"temp\": 18.5}",
                               "test_response_format_json_schema");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_response_format_json_schema");
  }
}

/* Chat request with seed parameter for deterministic output; response includes
 * system_fingerprint */
static void test_seed_parameter(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"7\",\"role\":\"assistant\"}}],\"created\":1711000120,"
      "\"id\":\"chatcmpl-seed001\",\"model\":\"gpt-4\",\"object\":\"chat."
      "completion\",\"system_fingerprint\":\"fp_abc12345\",\"usage\":{"
      "\"completion_tokens\":1,\"prompt_tokens\":12,\"total_tokens\":13}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"Pick a random "
             "number\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"seed\":42}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_seed_parameter");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_seed_parameter");
    liter_llm_assert_json_field(resp->body, "content", "7",
                               "test_seed_parameter");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_seed_parameter");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_seed_parameter");
    liter_llm_assert_json_field(mock_body, "content", "7",
                               "test_seed_parameter");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_seed_parameter");
  }
}

/* Chat request with custom stop sequences that terminates generation at a stop
 * token */
static void test_stop_sequences(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"Item 1\\nItem "
      "2\\n\",\"role\":\"assistant\"}}],\"created\":1711000110,\"id\":"
      "\"chatcmpl-stop001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\","
      "\"usage\":{\"completion_tokens\":7,\"prompt_tokens\":18,\"total_"
      "tokens\":25}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"List items until you see "
             "STOP\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"stop\":["
             "\"STOP\",\"END\"]}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_stop_sequences");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_stop_sequences");
    liter_llm_assert_json_field(resp->body, "content", "Item 1\nItem 2\n",
                               "test_stop_sequences");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_stop_sequences");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_stop_sequences");
    liter_llm_assert_json_field(mock_body, "content", "Item 1\nItem 2\n",
                               "test_stop_sequences");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_stop_sequences");
  }
}

/* Chat request with tool_choice set to required forces the model to call a tool
 */
static void test_tool_choice_required(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"tool_calls\",\"index\":0,\"message\":"
      "{\"content\":null,\"role\":\"assistant\",\"tool_calls\":[{\"function\":{"
      "\"arguments\":\"{\\\"location\\\": \\\"New "
      "York\\\"}\",\"name\":\"get_weather\"},\"id\":\"call_req001\",\"type\":"
      "\"function\"}]}}],\"created\":1711000040,\"id\":\"chatcmpl-"
      "required001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\","
      "\"usage\":{\"completion_tokens\":15,\"prompt_tokens\":60,\"total_"
      "tokens\":75}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"What is the weather "
             "today?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"tool_choice\":"
             "\"required\",\"tools\":[{\"function\":{\"description\":\"Get the "
             "current weather for a given "
             "location\",\"name\":\"get_weather\",\"parameters\":{"
             "\"properties\":{\"location\":{\"description\":\"The city "
             "name\",\"type\":\"string\"}},\"required\":[\"location\"],"
             "\"type\":\"object\"}},\"type\":\"function\"}]}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_tool_choice_required");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_tool_choice_required");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_tool_choice_required");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_tool_choice_required");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_tool_choice_required");
  }
}

/* Chat request with tool_choice specifying a particular function to call */
static void test_tool_choice_specific(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"tool_calls\",\"index\":0,\"message\":"
      "{\"content\":null,\"role\":\"assistant\",\"tool_calls\":[{\"function\":{"
      "\"arguments\":\"{\\\"location\\\": "
      "\\\"Paris\\\"}\",\"name\":\"get_weather\"},\"id\":\"call_spec001\","
      "\"type\":\"function\"}]}}],\"created\":1711000050,\"id\":\"chatcmpl-"
      "specific001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\","
      "\"usage\":{\"completion_tokens\":12,\"prompt_tokens\":75,\"total_"
      "tokens\":87}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"What is the weather in "
             "Paris?\",\"role\":\"user\"}],\"model\":\"gpt-4\",\"tool_choice\":"
             "{\"function\":{\"name\":\"get_weather\"},\"type\":\"function\"},"
             "\"tools\":[{\"function\":{\"description\":\"Get the current "
             "weather for a given "
             "location\",\"name\":\"get_weather\",\"parameters\":{"
             "\"properties\":{\"location\":{\"description\":\"The city "
             "name\",\"type\":\"string\"}},\"required\":[\"location\"],"
             "\"type\":\"object\"}},\"type\":\"function\"},{\"function\":{"
             "\"description\":\"Search the web for "
             "information\",\"name\":\"search_web\",\"parameters\":{"
             "\"properties\":{\"query\":{\"description\":\"The search "
             "query\",\"type\":\"string\"}},\"required\":[\"query\"],\"type\":"
             "\"object\"}},\"type\":\"function\"}]}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_tool_choice_specific");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_tool_choice_specific");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_tool_choice_specific");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_tool_choice_specific");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_tool_choice_specific");
  }
}

int main(void) {
  test_developer_message();
  printf("PASS: Chat request that includes a developer role message alongside "
         "user messages\n");
  test_finish_reason_content_filter();
  printf("PASS: Chat response stopped by content filter with finish_reason of "
         "content_filter and null content\n");
  test_finish_reason_length();
  printf("PASS: Chat response truncated due to max_tokens limit with "
         "finish_reason of length\n");
  test_multi_turn_conversation();
  printf("PASS: Multi-turn conversation with system, user, assistant, and "
         "follow-up user messages\n");
  test_parallel_tool_calls();
  printf("PASS: Chat request that results in parallel tool calls in the "
         "response\n");
  test_response_format_json_object();
  printf("PASS: Chat request with response_format json_object that returns "
         "valid JSON content\n");
  test_response_format_json_schema();
  printf("PASS: Chat request with response_format json_schema that validates "
         "the output structure\n");
  test_seed_parameter();
  printf("PASS: Chat request with seed parameter for deterministic output; "
         "response includes system_fingerprint\n");
  test_stop_sequences();
  printf("PASS: Chat request with custom stop sequences that terminates "
         "generation at a stop token\n");
  test_tool_choice_required();
  printf("PASS: Chat request with tool_choice set to required forces the model "
         "to call a tool\n");
  test_tool_choice_specific();
  printf("PASS: Chat request with tool_choice specifying a particular function "
         "to call\n");
  printf("All chat tests passed.\n");
  return 0;
}
