/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Request with all message role types (system, user, assistant, tool) to verify
 * round-trip serialization */
static void test_all_message_types(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"The weather in Paris is currently 18°C and partly "
      "cloudy.\",\"role\":\"assistant\"}}],\"created\":1711000003,\"id\":"
      "\"chatcmpl-types001\",\"model\":\"gpt-4\",\"object\":\"chat."
      "completion\",\"usage\":{\"completion_tokens\":18,\"prompt_tokens\":95,"
      "\"total_tokens\":113}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url,
        "{\"messages\":[{\"content\":\"You are a helpful "
        "assistant.\",\"role\":\"system\"},{\"content\":\"What is the weather "
        "in "
        "Paris?\",\"role\":\"user\"},{\"content\":null,\"role\":\"assistant\","
        "\"tool_calls\":[{\"function\":{\"arguments\":\"{\\\"location\\\": "
        "\\\"Paris, "
        "France\\\"}\",\"name\":\"get_weather\"},\"id\":\"call_xyz789\","
        "\"type\":\"function\"}]},{\"content\":\"{\\\"temperature\\\": 18, "
        "\\\"unit\\\": \\\"celsius\\\", \\\"description\\\": \\\"Partly "
        "cloudy\\\"}\",\"role\":\"tool\",\"tool_call_id\":\"call_xyz789\"}],"
        "\"model\":\"gpt-4\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_all_message_types");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_all_message_types");
    liter_llm_assert_json_field(
        resp->body, "content",
        "The weather in Paris is currently 18°C and partly cloudy.",
        "test_all_message_types");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_all_message_types");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_all_message_types");
    liter_llm_assert_json_field(
        mock_body, "content",
        "The weather in Paris is currently 18°C and partly cloudy.",
        "test_all_message_types");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_all_message_types");
  }
}

/* User message with mixed text and image_url content parts to verify multimodal
 * serialization */
static void test_multimodal_content(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"The image shows a transparent PNG demonstration with "
      "colored "
      "dice.\",\"role\":\"assistant\"}}],\"created\":1711000004,\"id\":"
      "\"chatcmpl-multi001\",\"model\":\"gpt-4o\",\"object\":\"chat."
      "completion\",\"usage\":{\"completion_tokens\":14,\"prompt_tokens\":855,"
      "\"total_tokens\":869}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"max_tokens\":100,\"messages\":[{\"content\":[{\"text\":\"What "
             "is in this "
             "image?\",\"type\":\"text\"},{\"image_url\":{\"detail\":\"low\","
             "\"url\":\"https://upload.wikimedia.org/wikipedia/commons/thumb/4/"
             "47/PNG_transparency_demonstration_1.png/"
             "280px-PNG_transparency_demonstration_1.png\"},\"type\":\"image_"
             "url\"}],\"role\":\"user\"}],\"model\":\"gpt-4o\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_multimodal_content");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_multimodal_content");
    liter_llm_assert_json_field(
        resp->body, "content",
        "The image shows a transparent PNG demonstration with colored dice.",
        "test_multimodal_content");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4o",
                               "test_multimodal_content");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_multimodal_content");
    liter_llm_assert_json_field(
        mock_body, "content",
        "The image shows a transparent PNG demonstration with colored dice.",
        "test_multimodal_content");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4o",
                               "test_multimodal_content");
  }
}

int main(void) {
  test_all_message_types();
  printf("PASS: Request with all message role types (system, user, assistant, "
         "tool) to verify round-trip serialization\n");
  test_multimodal_content();
  printf("PASS: User message with mixed text and image_url content parts to "
         "verify multimodal serialization\n");
  printf("All types tests passed.\n");
  return 0;
}
