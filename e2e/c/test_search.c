/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Basic web search request with a simple query */
static void test_search_basic(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"model\":\"brave/web-search\",\"results\":[{\"snippet\":\"A language "
      "empowering everyone to build reliable and efficient "
      "software.\",\"title\":\"Rust Programming "
      "Language\",\"url\":\"https://www.rust-lang.org\"}]}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"model\":\"brave/web-search\",\"query\":\"What is Rust "
             "programming language?\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_search_basic");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
  }
}

int main(void) {
  test_search_basic();
  printf("PASS: Basic web search request with a simple query\n");
  printf("All search tests passed.\n");
  return 0;
}
