/* Code generated by liter-llm-e2e-generator. DO NOT EDIT. */

#include "helpers.h"

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Client configured with a custom base URL routes all requests to that endpoint
 */
static void test_custom_base_url(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"Hi "
      "there!\",\"role\":\"assistant\"}}],\"created\":1711000000,\"id\":"
      "\"chatcmpl-local-001\",\"model\":\"local-model\",\"object\":\"chat."
      "completion\",\"usage\":{\"completion_tokens\":4,\"prompt_tokens\":3,"
      "\"total_tokens\":7}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp = liter_llm_http_post(
        url, "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],"
             "\"model\":\"local-model\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_custom_base_url");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_custom_base_url");
    liter_llm_assert_json_field(resp->body, "content", "Hi there!",
                               "test_custom_base_url");
    liter_llm_assert_json_field(resp->body, "model", "local-model",
                               "test_custom_base_url");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_custom_base_url");
    liter_llm_assert_json_field(mock_body, "content", "Hi there!",
                               "test_custom_base_url");
    liter_llm_assert_json_field(mock_body, "model", "local-model",
                               "test_custom_base_url");
  }
}

/* Client configured with extra custom headers successfully completes a chat
 * request */
static void test_extra_headers(void) {
  /* Pre-recorded mock response body. */
  const char *mock_body =
      "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{"
      "\"content\":\"Hello! How can I help "
      "you?\",\"role\":\"assistant\"}}],\"created\":1711000140,\"id\":"
      "\"chatcmpl-headers001\",\"model\":\"gpt-4\",\"object\":\"chat."
      "completion\",\"usage\":{\"completion_tokens\":8,\"prompt_tokens\":8,"
      "\"total_tokens\":16}}";

  const char *base_url = getenv("LITER_LLM_TEST_BASE_URL");
  if (base_url != NULL) {
    /* Live HTTP test against a real server. */
    char url[1024];
    snprintf(url, sizeof(url), "%s/chat/completions", base_url);

    LiterLlmResponse *resp =
        liter_llm_http_post(url, "{\"messages\":[{\"content\":\"Hello\","
                                "\"role\":\"user\"}],\"model\":\"gpt-4\"}");
    assert(resp != NULL);
    liter_llm_assert_status(resp, 200L, "test_extra_headers");
    liter_llm_assert_json_array_len(resp->body, "choices", 1,
                                   "test_extra_headers");
    liter_llm_assert_json_field(resp->body, "content",
                               "Hello! How can I help you?",
                               "test_extra_headers");
    liter_llm_assert_json_field(resp->body, "model", "gpt-4",
                               "test_extra_headers");
    liter_llm_response_free(resp);
  } else {
    /* Offline: assert against pre-recorded mock body. */
    liter_llm_assert_json_array_len(mock_body, "choices", 1,
                                   "test_extra_headers");
    liter_llm_assert_json_field(mock_body, "content",
                               "Hello! How can I help you?",
                               "test_extra_headers");
    liter_llm_assert_json_field(mock_body, "model", "gpt-4",
                               "test_extra_headers");
  }
}

int main(void) {
  test_custom_base_url();
  printf("PASS: Client configured with a custom base URL routes all requests "
         "to that endpoint\n");
  test_extra_headers();
  printf("PASS: Client configured with extra custom headers successfully "
         "completes a chat request\n");
  printf("All configuration tests passed.\n");
  return 0;
}
