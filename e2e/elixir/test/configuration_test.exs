# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
defmodule LiterLlmE2E.ConfigurationTest do
  use ExUnit.Case, async: true

  alias LiterLlmE2E.MockServer
  alias LiterLlmE2E.Helpers

  test "Client configured with a custom base URL routes all requests to that endpoint" do
    routes = [
      %{
        path: "/chat/completions",
        method: "POST",
        status: 200,
        body:
          "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hi there!\",\"role\":\"assistant\"}}],\"created\":1711000000,\"id\":\"chatcmpl-local-001\",\"model\":\"local-model\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":4,\"prompt_tokens\":3,\"total_tokens\":7}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/chat/completions",
        body:
          "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)

    assert length(doc["choices"]) == 1, "Expected 1 choice(s)"
    assert get_in(doc, ["choices", Access.at(0), "message", "content"]) == "Hi there!"
    assert get_in(doc, ["choices", Access.at(0), "finish_reason"]) == "stop"
    assert get_in(doc, ["usage", "total_tokens"]) == 7
    assert doc["model"] == "local-model"
  end

  test "Client configured with extra custom headers successfully completes a chat request" do
    routes = [
      %{
        path: "/chat/completions",
        method: "POST",
        status: 200,
        body:
          "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hello! How can I help you?\",\"role\":\"assistant\"}}],\"created\":1711000140,\"id\":\"chatcmpl-headers001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":8,\"prompt_tokens\":8,\"total_tokens\":16}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/chat/completions",
        body: "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)

    assert length(doc["choices"]) == 1, "Expected 1 choice(s)"

    assert get_in(doc, ["choices", Access.at(0), "message", "content"]) ==
             "Hello! How can I help you?"

    assert get_in(doc, ["choices", Access.at(0), "finish_reason"]) == "stop"
    assert get_in(doc, ["usage", "total_tokens"]) == 16
    assert doc["model"] == "gpt-4"
  end
end
