# Code generated by liter-llm-e2e-generator. DO NOT EDIT.
defmodule LiterLlmE2E.ContractTest do
  use ExUnit.Case, async: true

  alias LiterLlmE2E.MockServer
  alias LiterLlmE2E.Helpers

  test "Verify all bindings expose the full API surface — constructor accepts all config options and every method exists" do
    routes = [
      %{
        path: "/chat/completions",
        method: "POST",
        status: 200,
        body:
          "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"OK\",\"role\":\"assistant\"}}],\"created\":1234567890,\"id\":\"chatcmpl-contract\",\"model\":\"gpt-4o\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":1,\"prompt_tokens\":5,\"total_tokens\":6}}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/chat/completions",
        body:
          "{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)

    assert length(doc["choices"]) == 1, "Expected 1 choice(s)"
    assert get_in(doc, ["choices", Access.at(0), "message", "content"]) == "OK"
    assert get_in(doc, ["choices", Access.at(0), "finish_reason"]) == "stop"
    assert get_in(doc, ["usage", "total_tokens"]) == 6
    assert doc["model"] == "gpt-4o"
  end

  test "Verify ocr() method exists in all bindings" do
    routes = [
      %{
        path: "/chat/completions",
        method: "POST",
        status: 200,
        body:
          "{\"model\":\"mistral/mistral-ocr-latest\",\"pages\":[{\"index\":0,\"markdown\":\"# Contract Test\\n\\nThis verifies OCR method exists.\"}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/chat/completions",
        body:
          "{\"document\":{\"type\":\"document_url\",\"url\":\"https://example.com/contract-test.pdf\"},\"model\":\"mistral/mistral-ocr-latest\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end

  test "Verify search() method exists in all bindings" do
    routes = [
      %{
        path: "/chat/completions",
        method: "POST",
        status: 200,
        body:
          "{\"model\":\"brave/web-search\",\"results\":[{\"snippet\":\"Test result\",\"title\":\"Test\",\"url\":\"https://example.com\"}]}",
        stream_chunks: []
      }
    ]

    {:ok, base_url} = MockServer.start(routes)

    {:ok, resp} =
      Req.post(base_url <> "/chat/completions",
        body: "{\"model\":\"brave/web-search\",\"query\":\"contract test query\"}",
        headers: [{"content-type", "application/json"}],
        decode_body: false
      )

    assert resp.status == 200
    doc = Jason.decode!(resp.body)
  end
end
