// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
package dev.kreuzberg.literllm.e2e;

import static org.junit.jupiter.api.Assertions.*;

import com.fasterxml.jackson.databind.JsonNode;
import java.net.http.HttpResponse;
import java.util.List;
import org.junit.jupiter.api.Test;

/** E2E tests for category: list-models. */
class ListModelsTest {

  /** List models response returns an empty data array when no models are available */
  @Test
  void emptyModelList() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/models", "GET", 200, "{\"data\":[],\"object\":\"list\"}")))) {

      HttpResponse<String> resp = Helpers.getJson(server.url, "/models");

      assertEquals(200, resp.statusCode(), "HTTP status code");

      JsonNode body = Helpers.MAPPER.readTree(resp.body());
      assertEquals(0, body.get("data").size(), "model count");
    }
  }

  /** 401 Unauthorized error on list models request when API key is invalid */
  @Test
  void listModelsError401() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/models",
                    "GET",
                    401,
                    "{\"error\":{\"code\":\"invalid_api_key\",\"message\":\"Incorrect API key"
                        + " provided.\",\"param\":null,\"type\":\"invalid_request_error\"}}")))) {

      HttpResponse<String> resp = Helpers.getJson(server.url, "/models");

      assertEquals(401, resp.statusCode(), "HTTP status code");
      assertTrue(resp.statusCode() >= 400, "expected error status");
    }
  }
}
