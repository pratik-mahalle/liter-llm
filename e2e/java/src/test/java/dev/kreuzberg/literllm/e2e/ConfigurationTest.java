// Code generated by liter-llm-e2e-generator. DO NOT EDIT.
package dev.kreuzberg.literllm.e2e;

import static org.junit.jupiter.api.Assertions.*;

import com.fasterxml.jackson.databind.JsonNode;
import java.net.http.HttpResponse;
import java.util.List;
import org.junit.jupiter.api.Test;

/** E2E tests for category: configuration. */
class ConfigurationTest {

  /** Client configured with a custom base URL routes all requests to that endpoint */
  @Test
  void customBaseUrl() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/chat/completions",
                    "POST",
                    200,
                    "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hi"
                        + " there!\",\"role\":\"assistant\"}}],\"created\":1711000000,\"id\":\"chatcmpl-local-001\",\"model\":\"local-model\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":4,\"prompt_tokens\":3,\"total_tokens\":7}}")))) {

      HttpResponse<String> resp =
          Helpers.postJson(
              server.url,
              "/chat/completions",
              "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"local-model\"}");

      assertEquals(200, resp.statusCode(), "HTTP status code");

      JsonNode body = Helpers.MAPPER.readTree(resp.body());

      assertEquals(1, body.get("choices").size(), "choices count");
      assertEquals(
          "Hi there!", body.at("/choices/0/message/content").asText(), "first choice content");
      assertEquals("stop", body.at("/choices/0/finish_reason").asText(), "finish_reason");
      assertEquals("local-model", body.get("model").asText(), "model");
      assertNotNull(body.get("usage"), "usage object");
      assertEquals(7, body.at("/usage/total_tokens").asLong(), "total_tokens");
    }
  }

  /** Client configured with extra custom headers successfully completes a chat request */
  @Test
  void extraHeaders() throws Exception {
    try (Helpers.MockServer server =
        new Helpers.MockServer(
            List.of(
                new Helpers.MockRoute(
                    "/chat/completions",
                    "POST",
                    200,
                    "{\"choices\":[{\"finish_reason\":\"stop\",\"index\":0,\"message\":{\"content\":\"Hello!"
                        + " How can I help"
                        + " you?\",\"role\":\"assistant\"}}],\"created\":1711000140,\"id\":\"chatcmpl-headers001\",\"model\":\"gpt-4\",\"object\":\"chat.completion\",\"usage\":{\"completion_tokens\":8,\"prompt_tokens\":8,\"total_tokens\":16}}")))) {

      HttpResponse<String> resp =
          Helpers.postJson(
              server.url,
              "/chat/completions",
              "{\"messages\":[{\"content\":\"Hello\",\"role\":\"user\"}],\"model\":\"gpt-4\"}");

      assertEquals(200, resp.statusCode(), "HTTP status code");

      JsonNode body = Helpers.MAPPER.readTree(resp.body());

      assertEquals(1, body.get("choices").size(), "choices count");
      assertEquals(
          "Hello! How can I help you?",
          body.at("/choices/0/message/content").asText(),
          "first choice content");
      assertEquals("stop", body.at("/choices/0/finish_reason").asText(), "finish_reason");
      assertEquals("gpt-4", body.get("model").asText(), "model");
      assertNotNull(body.get("usage"), "usage object");
      assertEquals(16, body.at("/usage/total_tokens").asLong(), "total_tokens");
    }
  }
}
