// Code generated by liter-llm-e2e-generator. DO NOT EDIT.

import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { startMockServer } from "./helpers.js";
import type { MockServer } from "./helpers.js";

// Note: liter-llm-wasm must be built before running these tests.
// Run `wasm-pack build --target nodejs` in crates/liter-llm-wasm first.
import { LlmClient } from "liter-llm-wasm";

describe("List files when no files have been uploaded", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{ path: "/files", method: "GET", status: 200, body: '{"data":[],"object":"list"}', streamChunks: [] },
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("List files when no files have been uploaded", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "list_files"
	});
});

describe("Upload a large file successfully", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files",
				method: "POST",
				status: 200,
				body: '{"bytes":536870912,"created_at":1711000000,"filename":"large_training_data.jsonl","id":"file-large001","object":"file","purpose":"fine-tune","status":"processed"}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Upload a large file successfully", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "create_file"
	});
});

describe("401 Unauthorized when listing files with invalid API key", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files",
				method: "GET",
				status: 401,
				body: '{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("401 Unauthorized when listing files with invalid API key", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "list_files"
	});
});

describe("400 Bad Request when uploading a file with invalid purpose", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files",
				method: "POST",
				status: 400,
				body: '{"error":{"code":null,"message":"Invalid value for \'purpose\': \'invalid-purpose\'. Supported values are: \'fine-tune\', \'assistants\', \'batch\'.","param":"purpose","type":"invalid_request_error"}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("400 Bad Request when uploading a file with invalid purpose", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "create_file"
	});
});

describe("404 Not Found when retrieving a nonexistent file", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files/file-placeholder",
				method: "GET",
				status: 404,
				body: '{"error":{"code":null,"message":"No such File object: file-nonexistent","param":null,"type":"invalid_request_error"}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("404 Not Found when retrieving a nonexistent file", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "retrieve_file"
	});
});

describe("Upload a file for use with the API", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files",
				method: "POST",
				status: 200,
				body: '{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Upload a file for use with the API", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "create_file"
	});
});

describe("Delete an uploaded file", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files/file-placeholder",
				method: "POST",
				status: 200,
				body: '{"deleted":true,"id":"file-abc123","object":"file"}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Delete an uploaded file", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "delete_file"
	});
});

describe("Retrieve the content of an uploaded file", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files/file-placeholder/content",
				method: "GET",
				status: 200,
				body: '{"_binary":true,"_content_type":"application/octet-stream","_description":"Mock file content bytes"}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Retrieve the content of an uploaded file", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "file_content"
	});
});

describe("List all uploaded files", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files",
				method: "GET",
				status: 200,
				body: '{"data":[{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"},{"bytes":2048,"created_at":1711000100,"filename":"batch_input.jsonl","id":"file-def456","object":"file","purpose":"batch","status":"processed"}],"object":"list"}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("List all uploaded files", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "list_files"
	});
});

describe("Retrieve metadata for an uploaded file", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/files/file-placeholder",
				method: "GET",
				status: 200,
				body: '{"bytes":1024,"created_at":1711000000,"filename":"training_data.jsonl","id":"file-abc123","object":"file","purpose":"fine-tune","status":"processed"}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Retrieve metadata for an uploaded file", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "retrieve_file"
	});
});
