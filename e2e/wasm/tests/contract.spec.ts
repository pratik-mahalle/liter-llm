// Code generated by liter-llm-e2e-generator. DO NOT EDIT.

import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { startMockServer } from "./helpers.js";
import type { MockServer } from "./helpers.js";

// Note: liter-llm-wasm must be built before running these tests.
// Run `wasm-pack build --target nodejs` in crates/liter-llm-wasm first.
import { LlmClient } from "liter-llm-wasm";

describe("Verify all bindings expose the full API surface — constructor accepts all config options and every method exists", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/chat/completions",
				method: "POST",
				status: 200,
				body: '{"choices":[{"finish_reason":"stop","index":0,"message":{"content":"OK","role":"assistant"}}],"created":1234567890,"id":"chatcmpl-contract","model":"gpt-4o","object":"chat.completion","usage":{"completion_tokens":1,"prompt_tokens":5,"total_tokens":6}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Verify all bindings expose the full API surface — constructor accepts all config options and every method exists", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		const req = JSON.parse('{"messages":[{"content":"Contract test","role":"user"}],"model":"openai/gpt-4o"}');
		const response = await client.chat(req);
		expect((response as { choices: unknown[] }).choices).toHaveLength(1);
		expect((response as { choices: { message: { content: string } }[] }).choices[0].message.content).toBe("OK");
		expect((response as { model: string }).model).toBe("gpt-4o");
	});
});

describe("Verify ocr() method exists in all bindings", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/chat/completions",
				method: "POST",
				status: 200,
				body: '{"model":"mistral/mistral-ocr-latest","pages":[{"index":0,"markdown":"# Contract Test\\n\\nThis verifies OCR method exists."}]}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Verify ocr() method exists in all bindings", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "ocr"
	});
});

describe("Verify search() method exists in all bindings", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/chat/completions",
				method: "POST",
				status: 200,
				body: '{"model":"brave/web-search","results":[{"snippet":"Test result","title":"Test","url":"https://example.com"}]}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Verify search() method exists in all bindings", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "search"
	});
});
