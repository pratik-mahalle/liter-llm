// Code generated by liter-llm-e2e-generator. DO NOT EDIT.

import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { startMockServer } from "./helpers.js";
import type { MockServer } from "./helpers.js";

// Note: liter-llm-wasm must be built before running these tests.
// Run `wasm-pack build --target nodejs` in crates/liter-llm-wasm first.
import { LlmClient } from "liter-llm-wasm";

describe("Image generation returning base64-encoded data instead of URL", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 200,
				body: '{"created":1711000000,"data":[{"b64_json":"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==","revised_prompt":"A perfect blue circle on a white background"}]}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Image generation returning base64-encoded data instead of URL", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});

describe("Image generation with an empty prompt returns 400", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 400,
				body: '{"error":{"code":null,"message":"You must provide a prompt.","param":"prompt","type":"invalid_request_error"}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Image generation with an empty prompt returns 400", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});

describe("401 Unauthorized when generating images with invalid API key", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 401,
				body: '{"error":{"code":"invalid_api_key","message":"Incorrect API key provided.","param":null,"type":"invalid_request_error"}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("401 Unauthorized when generating images with invalid API key", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});

describe("400 Bad Request when image generation parameters are invalid", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 400,
				body: '{"error":{"code":null,"message":"Invalid value for \'size\': \'9999x9999\'. Supported values are: \'1024x1024\', \'1792x1024\', \'1024x1792\'.","param":"size","type":"invalid_request_error"}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("400 Bad Request when image generation parameters are invalid", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});

describe("429 Rate limit exceeded for image generation", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 429,
				body: '{"error":{"code":"rate_limit_exceeded","message":"Rate limit reached for images per minute. Limit: 5, Used: 5, Requested: 1.","param":null,"type":"rate_limit_error"}}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("429 Rate limit exceeded for image generation", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});

describe("Basic image generation with a text prompt", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 200,
				body: '{"created":1711000000,"data":[{"revised_prompt":"A fluffy white cat sitting peacefully on a sunlit windowsill","url":"https://example.com/images/img-abc123.png"}]}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Basic image generation with a text prompt", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});

describe("Image generation requesting multiple images", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 200,
				body: '{"created":1711000000,"data":[{"url":"https://example.com/images/img-001.png"},{"url":"https://example.com/images/img-002.png"},{"url":"https://example.com/images/img-003.png"}]}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Image generation requesting multiple images", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});

describe("Image generation with explicit size parameter", () => {
	let server: MockServer;

	beforeAll(async () => {
		server = await startMockServer([
			{
				path: "/images/generations",
				method: "POST",
				status: 200,
				body: '{"created":1711000000,"data":[{"revised_prompt":"A breathtaking sunset over snow-capped mountains","url":"https://example.com/images/img-def456.png"}]}',
				streamChunks: [],
			},
		]);
	});

	afterAll(() => {
		server.close();
	});

	it("Image generation with explicit size parameter", async () => {
		const client = new LlmClient({ apiKey: "test-key", baseUrl: server.url, maxRetries: 0 });

		// TODO: unknown method "image_generate"
	});
});
