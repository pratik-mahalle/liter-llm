// Code generated by liter-llm-e2e-generator. DO NOT EDIT.

package literllm_e2e

import (
	"bytes"
	"encoding/json"
	"net/http"
	"testing"
)

func TestContract(t *testing.T) {
	t.Run("binding_api_parity", func(t *testing.T) {
		// Verify all bindings expose the full API surface — constructor accepts all config options and every method exists
		server := NewMockServer([]MockRoute{
			{
				Path:         "/chat/completions",
				Method:       "POST",
				Status:       200,
				Body:         `{"choices":[{"finish_reason":"stop","index":0,"message":{"content":"OK","role":"assistant"}}],"created":1234567890,"id":"chatcmpl-contract","model":"gpt-4o","object":"chat.completion","usage":{"completion_tokens":1,"prompt_tokens":5,"total_tokens":6}}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		reqBody := bytes.NewBufferString("{\"messages\":[{\"content\":\"Contract test\",\"role\":\"user\"}],\"model\":\"openai/gpt-4o\"}")
		resp, err := http.Post(server.URL+"/chat/completions", "application/json", reqBody)
		if err != nil {
			t.Fatalf("http.Post failed: %v", err)
		}
		defer resp.Body.Close()

		AssertEqual(t, "status code", 200, resp.StatusCode)

		var result map[string]interface{}
		if err := json.NewDecoder(resp.Body).Decode(&result); err != nil {
			t.Fatalf("decode response: %v", err)
		}

		choices, _ := result["choices"].([]interface{})
		AssertEqual(t, "choices count", 1, len(choices))

		if len(choices) > 0 {
			choice, _ := choices[0].(map[string]interface{})
			message, _ := choice["message"].(map[string]interface{})
			AssertEqual(t, "first choice content", "OK", message["content"])
		}
		AssertEqual(t, "model", "gpt-4o", result["model"])

		usage, _ := result["usage"].(map[string]interface{})
		AssertTrue(t, "usage not nil", usage != nil)
		AssertEqual(t, "total_tokens", float64(6), usage["total_tokens"])
	})

	t.Run("contract_ocr", func(t *testing.T) {
		// Verify ocr() method exists in all bindings
		server := NewMockServer([]MockRoute{
			{
				Path:         "/chat/completions",
				Method:       "POST",
				Status:       200,
				Body:         `{"model":"mistral/mistral-ocr-latest","pages":[{"index":0,"markdown":"# Contract Test\n\nThis verifies OCR method exists."}]}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		// TODO: unknown method "ocr"
	})

	t.Run("contract_search", func(t *testing.T) {
		// Verify search() method exists in all bindings
		server := NewMockServer([]MockRoute{
			{
				Path:         "/chat/completions",
				Method:       "POST",
				Status:       200,
				Body:         `{"model":"brave/web-search","results":[{"snippet":"Test result","title":"Test","url":"https://example.com"}]}`,
				StreamChunks: nil,
			},
		})
		defer server.Close()

		// TODO: unknown method "search"
	})

}
