#!/usr/bin/env bash
# This file is auto-generated by alef. DO NOT EDIT.
# E2e tests for category: streaming
set -euo pipefail

test_anthropic_stream() {
    # Streaming chat completion via the Anthropic provider (claude-3-5-sonnet-20241022) yielding multiple SSE chunks
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 3 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" 'One Two Three' 'stream_content'
    # TODO: unsupported assertion type: is_true
}

test_azure_stream() {
    # Streaming chat completion via Azure OpenAI — verifies the azure/ prefix routes correctly and SSE chunks are delivered in the standard OpenAI chat.completion.chunk shape
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 3 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" '1 2 3' 'stream_content'
    # TODO: unsupported assertion type: is_true
}

test_basic_stream() {
    # Streaming chat completion that produces content across multiple SSE chunks
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 3 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" '1 2 3' 'stream_content'
}

test_bedrock_stream() {
    # Streaming chat completion via the AWS Bedrock provider using the bedrock/ prefix — verifies SSE chunks are yielded and assembled correctly from the Converse streaming API
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 2 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" 'One Two Three' 'stream_content'
    # TODO: unsupported assertion type: is_true
}

test_empty_stream() {
    # Streaming chat completion that produces no content chunks before the DONE signal
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 0 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" '' 'stream_content'
}

test_local_stream_ollama() {
    # Streaming chat completion via Ollama local provider with SSE chunks
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 3 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" '1 2 3' 'stream_content'
    # TODO: unsupported assertion type: is_true
}

test_stream_done_signal() {
    # Verify that the [DONE] sentinel signal properly terminates the stream
    local output
    output=$(liter_llm chat)

    # TODO: unsupported assertion type: is_true
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" 'Done' 'stream_content'
    # TODO: unsupported assertion type: is_true
}

test_stream_error_401() {
    # 401 Unauthorized error on stream initiation before any chunks are received
    if liter_llm chat >/dev/null 2>&1; then
        echo 'FAIL [error]: expected command to fail but it succeeded' >&2
        return 1
    fi
}

test_stream_with_tool_calls() {
    # Streaming chat completion where the assistant responds with a tool call across multiple chunks
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 4 'chunks'
    local val_finish_reason
    val_finish_reason=$(echo "$output" | jq -r '.finish_reason')
    assert_equals "$val_finish_reason" 'tool_calls' 'finish_reason'
    local val_tool_calls
    val_tool_calls=$(echo "$output" | jq -r '.tool_calls')
    assert_not_empty "$val_tool_calls" 'tool_calls'
    local val_tool_calls_0__function_name
    val_tool_calls_0__function_name=$(echo "$output" | jq -r '.tool_calls[0].function.name')
    assert_equals "$val_tool_calls_0__function_name" 'get_weather' 'tool_calls[0].function.name'
}

test_stream_with_usage() {
    # Streaming chat completion that includes a usage summary in the final chunk
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 3 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" 'Hi there!' 'stream_content'
    local val_usage_total_tokens
    val_usage_total_tokens=$(echo "$output" | jq -r '.usage.total_tokens')
    assert_equals "$val_usage_total_tokens" '18' 'usage.total_tokens'
}

test_vertex_stream() {
    # Streaming chat completion via the Google Vertex AI provider using the vertex_ai/ prefix — verifies SSE chunks from the Gemini streaming endpoint are yielded and assembled correctly
    local output
    output=$(liter_llm chat)

    local count_chunks
    count_chunks=$(echo "$output" | jq '.chunks | length')
    assert_count_min "$count_chunks" 2 'chunks'
    local val_stream_content
    val_stream_content=$(echo "$output" | jq -r '.stream_content')
    assert_equals "$val_stream_content" 'One Two Three' 'stream_content'
    # TODO: unsupported assertion type: is_true
}

run_tests_streaming() {
    run_test test_anthropic_stream
    run_test test_azure_stream
    run_test test_basic_stream
    run_test test_bedrock_stream
    run_test test_empty_stream
    run_test test_local_stream_ollama
    run_test test_stream_done_signal
    run_test test_stream_error_401
    run_test test_stream_with_tool_calls
    run_test test_stream_with_usage
    run_test test_vertex_stream
}
