[package]
name = "liter-llm-smoke-test"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
liter-llm = { version = "1.0.0-rc.8", features = ["native-http", "tower"] }
tokio = { version = "1", features = ["full"] }
futures = "0.3"
