[workspace]

[package]
name = "liter-lm-rb"
version = "0.1.0"
edition = "2024"
authors = ["Na'aman Hirschfeld <naaman@kreuzberg.dev>"]
license = "MIT"
repository = "https://github.com/kreuzberg-dev/liter-lm"
homepage = "https://kreuzberg.dev"
description = "Ruby bindings (Magnus) for liter-lm universal LM API client"
keywords = ["ruby", "magnus", "llm", "api-client", "bindings"]
categories = ["api-bindings"]

[lib]
name = "liter_lm_rb"
crate-type = ["cdylib", "rlib"]

[dependencies]
base64 = "0.22"
liter-lm = { path = "../../../../../crates/liter-lm" }
magnus = { version = "0.8", features = ["rb-sys"] }
rb-sys = { version = "0.9", default-features = false, features = [
    "stable-api-compiled-fallback",
] }
serde_json = "1"
tokio = { version = "1", features = ["rt", "rt-multi-thread", "macros"] }

[dev-dependencies]
pretty_assertions = "1"

[package.metadata.cargo-machete]
ignored = ["rb-sys"]
