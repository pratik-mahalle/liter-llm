// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Google Vertex AI OAuth2 credential provider using the service-account
 * JWT assertion flow (two-legged OAuth).
 *
 * Signs a JWT with the service account's private key, then exchanges it
 * at `https://oauth2.googleapis.com/token` for a short-lived access token.
 */
public class VertexOAuthCredentialProvider implements AutoCloseable {
    private final MemorySegment handle;

    VertexOAuthCredentialProvider(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_VERTEX_O_AUTH_CREDENTIAL_PROVIDER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free VertexOAuthCredentialProvider: " + e.getMessage(), e);
            }
        }
    }
}
