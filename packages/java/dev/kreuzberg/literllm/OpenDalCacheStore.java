// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Cache store backed by an [`opendal::Operator`].
 *
 * Entries are stored as JSON files under `{prefix}/{key}`. TTL is embedded
 * in the stored entry and checked on read. Backend failures are non-fatal:
 * they log a warning and behave as a cache miss / no-op.
 */
public class OpenDalCacheStore implements AutoCloseable {
    private final MemorySegment handle;

    OpenDalCacheStore(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_OPEN_DAL_CACHE_STORE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free OpenDalCacheStore: " + e.getMessage(), e);
            }
        }
    }
}
