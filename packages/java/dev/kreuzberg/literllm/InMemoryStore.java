// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * In-memory LRU cache store.
 *
 * This is the default [`CacheStore`] backend used by [`CacheLayer::new`].
 * It uses a [`HashMap`] with a [`VecDeque`] for LRU eviction order.
 */
public class InMemoryStore implements AutoCloseable {
    private final MemorySegment handle;

    InMemoryStore(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_IN_MEMORY_STORE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free InMemoryStore: " + e.getMessage(), e);
            }
        }
    }
}
