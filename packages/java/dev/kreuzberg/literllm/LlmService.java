// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

public class LlmService implements AutoCloseable {
    private final MemorySegment handle;

    LlmService(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_LLM_SERVICE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free LlmService: " + e.getMessage(), e);
            }
        }
    }
}
