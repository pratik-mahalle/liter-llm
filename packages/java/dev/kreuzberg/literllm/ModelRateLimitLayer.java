// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Tower [`Layer`] that enforces per-model rate limits.
 */
public class ModelRateLimitLayer implements AutoCloseable {
    private final MemorySegment handle;

    ModelRateLimitLayer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_MODEL_RATE_LIMIT_LAYER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free ModelRateLimitLayer: " + e.getMessage(), e);
            }
        }
    }
}
