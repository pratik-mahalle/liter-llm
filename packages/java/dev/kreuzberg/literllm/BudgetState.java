// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Shared, thread-safe budget accumulator.
 *
 * All values are stored in **microcents** (USD * 1_000_000) as `AtomicU64` to
 * avoid floating-point atomics while retaining sub-cent precision.
 */
public class BudgetState implements AutoCloseable {
    private final MemorySegment handle;

    BudgetState(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_BUDGET_STATE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free BudgetState: " + e.getMessage(), e);
            }
        }
    }
}
