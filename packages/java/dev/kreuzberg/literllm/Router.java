// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

public class Router implements AutoCloseable {
    private final MemorySegment handle;

    Router(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_ROUTER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free Router: " + e.getMessage(), e);
            }
        }
    }
}
