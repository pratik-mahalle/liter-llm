// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * A static credential provider that always returns the same bearer token.
 * Useful for testing or when tokens are managed externally.
 */
public class StaticTokenProvider implements AutoCloseable {
    private final MemorySegment handle;

    StaticTokenProvider(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_STATIC_TOKEN_PROVIDER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free StaticTokenProvider: " + e.getMessage(), e);
            }
        }
    }
}
