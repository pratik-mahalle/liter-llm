// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * AWS STS Web Identity credential provider.
 *
 * Reads a JWT from a token file, sends it to the STS
 * `AssumeRoleWithWebIdentity` endpoint, and returns temporary AWS credentials
 * suitable for SigV4 signing.
 */
public class WebIdentityCredentialProvider implements AutoCloseable {
    private final MemorySegment handle;

    WebIdentityCredentialProvider(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_WEB_IDENTITY_CREDENTIAL_PROVIDER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free WebIdentityCredentialProvider: " + e.getMessage(), e);
            }
        }
    }
}
