// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Tower [`Layer`] that records estimated USD cost on the current tracing span.
 *
 * After each successful response the layer calls [`cost::completion_cost`] and
 * records the result as `gen_ai.usage.cost` using
 * [`tracing::Span::record`].  If the model is not in the pricing registry the
 * attribute is simply omitted.
 */
public class CostTrackingLayer implements AutoCloseable {
    private final MemorySegment handle;

    CostTrackingLayer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_COST_TRACKING_LAYER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free CostTrackingLayer: " + e.getMessage(), e);
            }
        }
    }
}
