// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * GitHub Copilot credential provider using the OAuth Device Flow.
 *
 * Maintains two caches:
 * - A disk-persisted GitHub OAuth access token (long-lived, survives restarts).
 * - An in-memory Copilot API key (short-lived, refreshed automatically).
 *
 * On first use the user is prompted to visit a URL and enter a code.
 * Subsequent runs reuse the cached access token and API key transparently.
 */
public class GithubCopilotCredentialProvider implements AutoCloseable {
    private final MemorySegment handle;

    GithubCopilotCredentialProvider(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_GITHUB_COPILOT_CREDENTIAL_PROVIDER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free GithubCopilotCredentialProvider: " + e.getMessage(), e);
            }
        }
    }
}
