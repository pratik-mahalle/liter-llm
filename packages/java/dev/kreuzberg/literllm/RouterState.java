// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Shared state tracking per-deployment metrics, keyed by deployment index.
 */
public class RouterState implements AutoCloseable {
    private final MemorySegment handle;

    RouterState(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_ROUTER_STATE_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free RouterState: " + e.getMessage(), e);
            }
        }
    }
}
