// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Tower [`Layer`] that applies a cooldown period after transient errors.
 */
public class CooldownLayer implements AutoCloseable {
    private final MemorySegment handle;

    CooldownLayer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_COOLDOWN_LAYER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free CooldownLayer: " + e.getMessage(), e);
            }
        }
    }
}
