// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Tower [`Layer`] that attaches [`LlmHook`] callbacks to a service.
 *
 * Hooks are stored behind `Arc` so that the layer and all services it
 * produces share the same hook instances without cloning them.
 */
public class HooksLayer implements AutoCloseable {
    private final MemorySegment handle;

    HooksLayer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_HOOKS_LAYER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free HooksLayer: " + e.getMessage(), e);
            }
        }
    }
}
