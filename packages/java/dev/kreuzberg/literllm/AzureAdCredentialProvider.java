// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Azure AD OAuth2 credential provider using the client-credentials grant.
 *
 * Obtains bearer tokens from `https://login.microsoftonline.com/{tenant}/oauth2/v2.0/token`
 * and caches them until they are within 5 minutes of expiry.
 */
public class AzureAdCredentialProvider implements AutoCloseable {
    private final MemorySegment handle;

    AzureAdCredentialProvider(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_AZURE_AD_CREDENTIAL_PROVIDER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free AzureAdCredentialProvider: " + e.getMessage(), e);
            }
        }
    }
}
