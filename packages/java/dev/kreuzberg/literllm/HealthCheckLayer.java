// DO NOT EDIT - auto-generated by alef
package dev.kreuzberg.literllm;

import java.lang.foreign.MemorySegment;

/**
 * Tower [`Layer`] that monitors service health via periodic probes.
 *
 * The background health-check task is spawned when the layer wraps a service
 * (i.e. when [`Layer::layer`] is called).  The task runs until the
 * [`HealthCheckService`] (and all its clones) are dropped.
 */
public class HealthCheckLayer implements AutoCloseable {
    private final MemorySegment handle;

    HealthCheckLayer(MemorySegment handle) {
        this.handle = handle;
    }

    MemorySegment handle() {
        return this.handle;
    }

    @Override
    public void close() {
        if (handle != null && !handle.equals(MemorySegment.NULL)) {
            try {
                NativeLib.LITERLLM_HEALTH_CHECK_LAYER_FREE.invoke(handle);
            } catch (Throwable e) {
                throw new RuntimeException("Failed to free HealthCheckLayer: " + e.getMessage(), e);
            }
        }
    }
}
