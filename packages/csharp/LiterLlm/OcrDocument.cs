// This file is auto-generated by alef. DO NOT EDIT.
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace LiterLlm;

/// <summary>
/// Document input for OCR — either a URL or inline base64 data.
/// </summary>
[JsonConverter(typeof(OcrDocumentJsonConverter))]
public abstract record OcrDocument
{
    /// <summary>
    /// A publicly accessible document URL.
    /// </summary>
    public sealed record Url(
        [property: JsonPropertyName("url")] string Url
    ) : OcrDocument;

    /// <summary>
    /// Inline base64-encoded document data.
    /// </summary>
    public sealed record Base64(
        [property: JsonPropertyName("data")] string Data,
        [property: JsonPropertyName("media_type")] string MediaType
    ) : OcrDocument;

}

/// <summary>Custom JSON converter for <see cref="OcrDocument"/> that reads the "type" discriminator from any position.</summary>
internal sealed class OcrDocumentJsonConverter : JsonConverter<OcrDocument>
{
    public override OcrDocument Read(ref Utf8JsonReader reader, Type typeToConvert, JsonSerializerOptions options)
    {
        using var doc = JsonDocument.ParseValue(ref reader);
        var root = doc.RootElement;
        if (!root.TryGetProperty("type", out var tagEl))
            throw new JsonException("OcrDocument: missing \"type\" discriminator");
        var tag = tagEl.GetString();
        var json = root.GetRawText();
        return tag switch
        {
            "document_url" => JsonSerializer.Deserialize<OcrDocument.Url>(json, options)!
                ?? throw new JsonException("Failed to deserialize OcrDocument.Url"),
            "base64" => JsonSerializer.Deserialize<OcrDocument.Base64>(json, options)!
                ?? throw new JsonException("Failed to deserialize OcrDocument.Base64"),
            _ => throw new JsonException($"Unknown OcrDocument discriminator: {tag}")
        };
    }

    public override void Write(Utf8JsonWriter writer, OcrDocument value, JsonSerializerOptions options)
    {
        // Serialize the concrete type, then inject the discriminator
        switch (value)
        {
            case OcrDocument.Url v:
            {
                var doc = JsonSerializer.SerializeToDocument(v, options);
                writer.WriteStartObject();
                writer.WriteString("type", "document_url");
                foreach (var prop in doc.RootElement.EnumerateObject())
                    if (prop.Name != "type") prop.WriteTo(writer);
                writer.WriteEndObject();
                break;
            }
            case OcrDocument.Base64 v:
            {
                var doc = JsonSerializer.SerializeToDocument(v, options);
                writer.WriteStartObject();
                writer.WriteString("type", "base64");
                foreach (var prop in doc.RootElement.EnumerateObject())
                    if (prop.Name != "type") prop.WriteTo(writer);
                writer.WriteEndObject();
                break;
            }
            default: throw new JsonException($"Unknown OcrDocument subtype: {value.GetType().Name}");
        }
    }
}
